## 2026-07-06T06:05:12Z — Pi_CCS/FE stage audit + terminal DEC plane-overlap rejected

- Stage audit: the current goal prompt asked to start with `fold.superneo.pi_ccs.oracle` and `fold.superneo.pi_ccs.sumcheck.fe`. Fresh baseline `benchmark-results/gpuprof/20260706T060032Z-gpu-stage-baseline.json` reported CPU median `2135.7ms`, CUDA median `471.5ms`, speedup `4.53x`, proof byte-identical, residency clean. `gpuscope analyze /tmp/gpuprof-cuiaasgm` reconciled cleanly (`max_reconciliation_error_ms=0`) and ranked exclusive Pi_CCS levers low: `oracle.Eval` recoverable `2.57ms`, `oracle.F` `0.53ms`, `oracle.Q` `1.75ms`, and `sumcheck.fe.row_download` only `0.39ms` exclusive despite the raw gpuprof leaf showing `~25.9ms`. This matches the existing negative history: standalone FE row-log download, pinned staging, deferral, and overlap variants only move proof materialization around. Treat narrow Pi_CCS/FE row-download work as parked unless the proof/output consumer boundary changes.
- Dependency DAG tested next: after `Pi_DEC split`, two read-only consumers are independent: terminal full-witness D2H of `split.planes`, and child y/commit/opening work from the same resident planes. Hypothesis: start the full plane D2H on a forked stream immediately after split, then join only before terminal witness assembly. Expected value: overlap terminal export with child GPU work while preserving the full benchmark contract and byte-identical witnesses.
- Diff summary: temporarily changed `DeviceDec::prove` to enqueue `split.planes.copy_to_pinned_host_async` into the existing `planes_host` buffer on a forked stream before `eval_and_commit_children`, then synchronize that stream in `fold.superneo.pi_dec.emit.planes`. The attempt was reverted after measurement.
- Gates before measurement: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Rejected result: `benchmark-results/gpuprof/20260706T060311Z-dec-terminal-planes-overlap.json`; CPU median `2139.4ms`, CUDA median `472.3ms`, speedup `4.53x`, proof byte-identical, residency clean. Strict check against `benchmark-results/gpuprof/20260706T-dec-forms-workspace-preserve-order.json` passed by tolerance (`469.2ms -> 472.3ms`, `27` comparisons, `0` regressions), but the candidate did not beat the accepted median. It reduced `pi_dec.emit.planes` from about `20.6ms` on restore to `17.9ms`, but the online wall stayed flat and this is below the acceptance threshold.
- Finding: full-plane export can be mechanically overlapped, but copy placement alone does not remove the terminal witness/output boundary. The remaining terminal DEC work needs a wider consumer/contract change, such as a GPU terminal consumer/decider or a larger graph/egress unit, not another standalone D2H scheduling tweak.
- Restore check: after reverting, `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; and `timeout 300s ./target/release/parity nifs` passed. Restore benchmark `benchmark-results/gpuprof/20260706T060440Z-dec-terminal-planes-overlap-reverted.json` reported CPU median `2205.2ms`, CUDA median `470.9ms`, speedup `4.68x`, proof byte-identical, residency clean. Strict check against `benchmark-results/gpuprof/20260706T-dec-forms-workspace-preserve-order.json` passed with `27` comparisons and `0` regressions.
- Verdict: rejected and reverted. Accepted baseline remains `benchmark-results/gpuprof/20260706T-dec-forms-workspace-preserve-order.json`. Do not retry terminal full-plane D2H overlap as a standalone slice; it must be paired with a real terminal consumer/proof-output boundary change.

## 2026-07-06T06:15:32Z — DEC matrix-form stream DAG accepted

- Stage audit: NCU representative samples showed `forms_from_bar_csr` as underfilled/low-throughput, and the current `DeviceBarMatrices` form builder serialized every matrix form row pair on one stream even though each `bar(M_j)^T chi` computation depends only on the already-built `chi` table and writes a disjoint `[2][blocks][D]` slice of the forms buffer. The dependency graph is `chi -> forms_j -> child y_ring`, with all `forms_j` independent after `chi`.
- Hypothesis: keep graph-captured whole-FE on the existing single-stream builder, but add an explicit non-graph concurrent form builder and use it for DEC forms. Expected value: expose legal matrix-level concurrency without changing proof bytes, forms layout, or the benchmark contract.
- Diff summary: `DeviceBarMatrices` now has `build_forms_from_device_challenges_into_concurrent`, which builds `chi` on the prover stream, forks one stream per additional matrix, launches the existing `forms_from_bar_csr` kernel for each matrix output slice, then joins all forked streams before returning. `DecFormsWorkspace::build` uses this concurrent entrypoint. The original graph-safe `build_forms_from_device_challenges_into` remains unchanged.
- Gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Sanitizer: `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` passed with `ERROR SUMMARY: 0 errors`, covering the new concurrent disjoint writes in the DEC form path.
- Result: `benchmark-results/gpuprof/20260706T061431Z-dec-forms-matrix-streams.json`; CPU median `2134.3ms`, CUDA median `463.1ms`, speedup `4.61x`, proof byte-identical, residency clean. Repeat values were `[462.3, 464.2, 463.1]`.
- Check: strict check against `benchmark-results/gpuprof/20260706T-dec-forms-workspace-preserve-order.json` passed clean (`469.2ms -> 463.1ms`, `27` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-99ffqdyv` reconciled cleanly (`max_reconciliation_error_ms=0`) and reported the next top lever as terminal `fold.superneo.pi_dec.emit.planes`.
- Verdict: accepted. Accepted baseline is now `benchmark-results/gpuprof/20260706T061431Z-dec-forms-matrix-streams.json`. Next meaningful work should target a wider DEC split/emit/proof-output boundary or the FE proof-output consumer boundary; do not spend another slice on standalone DEC forms scheduling unless a new profile makes it top-ranked again.

## 2026-07-06T06:45:00Z — DEC split/forms stream overlap rejected

- Stage audit: `Pi_DEC split` and DEC ring-form construction are independent in the SuperNeo DAG: split depends on the parent witness, while forms depend on `parent.r` and the static bar matrices. The profiler still labels `pi_dec.split` as the largest recoverable row, but the split kernel itself is tiny, so this tested whether the row was mostly artificial serialization through the old CPU-shaped call order.
- Hypothesis: enqueue split on a forked stream before building forms, keep the temporary uploaded parent witness alive until split completion, then join before child y/commit kernels read the split planes. Expected value: overlap split with forms without changing active-child semantics or proof material.
- Diff summary: temporarily introduced `PendingSplit` in `reduce/dec.rs`, split the stage into `fold.superneo.pi_dec.split.enqueue` and `fold.superneo.pi_dec.split.finish`, and joined the split stream before child evaluation. The attempt was reverted after measurement.
- Gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec`.
- Rejected result: `benchmark-results/gpuprof/20260706T-dec-split-stream-overlap.json`; CPU median `2134.1ms`, CUDA median `472.1ms`, speedup `4.52x`, proof byte-identical, residency clean. The new `split.finish` still carried about `37.8ms`; online time regressed from accepted `463.1ms`.
- Restore check: after reverting and rebuilding, `benchmark-results/gpuprof/20260706T-dec-split-stream-overlap-reverted.json` reported CPU median `2146.9ms`, CUDA median `466.9ms`, speedup `4.59x`, proof byte-identical, residency clean; fastest repeat was `463.6ms`.
- Verdict: rejected and reverted. Do not retry simple split/forms stream overlap. The real DEC split issue is host-owned active-child materialization and/or the downstream terminal proof-output boundary, not launch order between split and forms.

## 2026-07-06T06:51:30Z — FE row proof-log deferral rejected

- Stage audit: fresh baseline `benchmark-results/gpuprof/20260706T063339Z-gpu-stage-baseline.json` reported CPU median `2147.2ms`, CUDA median `465.0ms`, speedup `4.62x`, proof byte-identical, residency clean. `gpuscope analyze /tmp/gpuprof-jcwj4no7` reconciled cleanly and ranked terminal `fold.superneo.pi_dec.emit.planes` as the top exclusive lever, while gpuprof still showed `pi_ccs.sumcheck.fe.row_download` at about `25.5ms` inside the Pi_CCS row path.
- Dependency DAG: FE row rounds produce coefficient logs and challenges for proof bytes, but Pi_RLC only needs terminal output claims/digest and resident K surfaces. The existing reductions API already supports a row-trace deferred proof where terminal state advances first and FE row proof coefficients remain backend-owned until proof assembly.
- Hypothesis: switch the default row-trace adapter path from ready Pi_CCS proof assembly to deferred FE-row proof export. Start an async pinned-host copy of the retained FE row coeff log at Pi_CCS exit, return FE statics/workspaces to the session immediately, and finish proof assembly from that pending export at egress. Expected value: remove the immediate `row_download` barrier from Pi_CCS and let Pi_RLC/Pi_DEC proceed while proof-log D2H is in flight.
- Diff summary: temporarily added `PendingFeRowRoundExport` in `reduce/ccs/fe_rows.rs`, a crate export in `reduce/ccs/mod.rs`, and a `DeferredFeRows` proof-state branch in `adapter.rs`. The first live-backend design failed the cuda build because it borrowed `self.session` across mutable DEC/RLC work; the second pending-export design compiled and passed gates. The attempt was reverted after measurement.
- Gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity ccs_prove`.
- Rejected result: `benchmark-results/gpuprof/20260706T064316Z-fe-row-deferred-export.json`; CPU median `2136.0ms`, CUDA median `481.5ms`, speedup `4.44x`, proof byte-identical, residency clean. `row_download` disappeared from Pi_CCS, but the cost reappeared as `fold.egress.export` plus a larger aggregate FE row, and the online wall regressed.
- Restore check: after reverting and rebuilding, `benchmark-results/gpuprof/20260706T064605Z-fe-row-deferred-export-reverted.json` reported CPU median `2129.2ms`, CUDA median `470.4ms`, speedup `4.53x`, proof byte-identical, residency clean.
- Verdict: rejected and reverted. Do not retry FE row proof-log deferral as a standalone slice. It only moves host proof materialization from Pi_CCS to egress. A future FE proof-output change must be part of a wider device-owned proof assembly or whole-fold command boundary.

## 2026-07-06T06:56:30Z — Pi_CCS f-var row-table stream split rejected

- Stage audit: `fold.superneo.pi_ccs.oracle.F` builds f-var row tables from a resident witness plane and resident row CSR matrices. The dependency graph permits table-level concurrency: after the plane and row matrices are resident, each requested matrix table reads the same plane and writes a distinct output buffer.
- Hypothesis: add a `DeviceRowMatrices` helper that launches independent row-table kernels on forked streams and use it from `DeviceFeBackend::device_mcs_row_tables`. Expected value: expose valid matrix-level parallelism in the Pi_CCS oracle path without changing transcript/proof semantics.
- Diff summary: temporarily added `build_row_tables_device_concurrent` in `ring_forms.rs` and routed resident-plane f-var table construction through it. The attempt was reverted after measurement.
- Gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity ccs_prove`.
- Rejected result: `benchmark-results/gpuprof/20260706T065238Z-pi-ccs-row-tables-streams.json`; CPU median `2133.7ms`, CUDA median `468.1ms`, speedup `4.56x`, proof byte-identical, residency clean. The stream split increased `row_table_from_csr` attributed busy/API weight (`~13ms` vs `~3ms` on restore) and did not beat the accepted `463.1ms` baseline.
- Restore check: after reverting and rebuilding, `benchmark-results/gpuprof/20260706T065424Z-pi-ccs-row-tables-streams-reverted.json` reported CPU median `2145.5ms`, CUDA median `468.2ms`, speedup `4.58x`, proof byte-identical, residency clean. Source search confirmed the temporary helper and call site were removed.
- Verdict: rejected and reverted. Do not retry simple per-table stream splitting for Pi_CCS oracle.F; the table kernels are too small/underfilled for this stream grain. A future Pi_CCS oracle change needs a coarser execution grain or a different output/consumer boundary.

## 2026-07-06T07:01:00Z — DEC split pinned flag readback rejected

- Stage audit: `gpuprof` attributed `fold.superneo.pi_dec.split` at about `38ms` wall while the `dec_split` kernel itself was only about `0.04ms` per fold. Inspecting the nsys-backed JSON showed the apparent cost was `cuMemcpyDtoHAsync_v2` for the tiny split flag readback. The first hypothesis was that `flags.to_host_vec()` used pageable host memory and created an avoidable host/device transfer barrier.
- Hypothesis: reuse a pinned split-flag landing buffer inside `DeviceDec` and replace the pageable `to_host_vec()` with `copy_to_pinned_host`. Expected value: remove the tiny pageable D2H barrier while preserving the host-side out-of-range check and active-child list.
- Diff summary: temporarily added `split_flags_host` to `DeviceDec`, ensured it before `split_parent`, and copied flags into that pinned buffer. The attempt was reverted after measurement.
- Gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec`.
- Rejected result: `benchmark-results/gpuprof/20260706T065821Z-dec-split-pinned-flags.json`; CPU median `2132.3ms`, CUDA median `471.1ms`, speedup `4.51x`, proof byte-identical, residency clean. The e2e split stage remained about `39ms`; the isolated DEC gate improved, but the full e2e path did not.
- Finding: the `pi_dec.split` row is mostly attribution from previous fire-and-forget DEC form work queued before the split flag readback, not a standalone pageable flag-copy bottleneck. Pinning the flags does not change the real online critical path.
- Restore check: after reverting and rebuilding, `benchmark-results/gpuprof/20260706T070003Z-dec-split-pinned-flags-reverted.json` reported CPU median `2147.3ms`, CUDA median `466.5ms`, speedup `4.61x`, proof byte-identical, residency clean. Source search confirmed the temporary pinned flag fields/helpers were removed.
- Verdict: rejected and reverted. Do not retry split flag pinning as a standalone slice. The real DEC split/emit work needs a wider device-owned active-child/output boundary or a profiling taxonomy fix that stops charging earlier async work to the flag readback.

## 2026-07-06T07:09:02Z — NCU/gpuscope stage audit, no code accepted

- Stage audit: refreshed the active goal and checked the current restored artifact `benchmark-results/gpuprof/20260706T070003Z-dec-split-pinned-flags-reverted.json`; CPU median `2147.3ms`, CUDA median `466.5ms`, speedup `4.61x`, proof byte-identical, residency clean. The accepted baseline remains `benchmark-results/gpuprof/20260706T061431Z-dec-forms-matrix-streams.json`.
- NVIDIA tooling: `ncu` is now usable on this machine. A targeted `fe_round_partials` pass exported `benchmark-results/ncu-fe-round-partials-20260706.ncu-rep`. It confirms FE row rounds are still underfilled after the earlier chunk-size win: launch grids shrink `49 -> 25 -> 13 -> 7 -> 4 -> 2 -> 1` blocks on a 128-SM GPU, with `128` registers/thread and late-round active warps down near `2-3%`. This validates the CUDA-core-utilization concern, but the measured cooperative single-round/all-round FE shapes are already parked because they replace the ladder with heavier underfilled kernels and lose the repeat-3 gate.
- gpuscope: `python3 scripts/gpuscope/cli.py analyze /tmp/gpuprof-tfhv0sv2 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T070003Z-dec-split-pinned-flags-reverted.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `gpuprof_validation=ok`). Its top exclusive lever is `fold.superneo.pi_dec.emit.planes` (`recoverable_ms=18.870`), not `pi_dec.split`. This confirms the split row is an attribution artifact from earlier async work and that the current real output boundary is terminal private-witness plane export.
- Dependency decision: no code accepted in this slice. Standalone FE row-download/log deferral, split flag readback, split/forms overlap, terminal plane D2H overlap, and simple row-table stream splitting are all parked by current evidence. The next useful implementation must change the terminal-output consumer/contract or add a real downstream GPU consumer/decider so the private terminal planes stay resident; otherwise `emit.planes` is required by the current full-audit benchmark contract and any copy-placement change only moves the same boundary.
- Next recommendation: either (1) define and implement an explicit GPU-fast terminal contract where the timed path exports final proof/public material only and full terminal-witness parity runs outside timing, or (2) start the larger GPU decider/compression consumer so terminal planes are consumed on device. Do not mutate `e2e_bench` silently; if the contract changes, create a new baseline and keep the existing full-audit parity coverage.

## 2026-07-06T07:30:00Z — DEC flag-aware canonical children rejected and reverted

- Stage audit: fresh baseline `benchmark-results/gpuprof/20260706T071248Z-gpu-stage-baseline.json` reported CPU median `2148.8ms`, CUDA median `464.0ms`, speedup `4.64x`, proof byte-identical, residency clean. `gpuscope analyze /tmp/gpuprof-e_1cepq5 --validate-gpuprof` reconciled cleanly (`max_reconciliation_error_ms=0`) and ranked `fold.superneo.pi_dec.emit.planes` as the top exclusive lever, while gpuprof still showed `pi_dec.split` as a large host-join row. The dependency issue is the split flag/active-child boundary: the split kernel produces device flags, but the current fast path downloads them immediately to construct a dense host active-child list before y/commit work.
- Hypothesis: keep split flags on device and make downstream child opening/commit kernels flag-aware in canonical child order. Expected value: remove the early host split-flag join while avoiding the previous all-child explosion by skipping inactive child work inside kernels.
- Diff summary: temporarily added flag-aware Ajtai mat-vec and y_zcol kernels plus a `commit_planes_with_flags_device_output` path, changed `SplitPlanes` to keep `flags: DeviceBuffer<u64>`, and made `eval_and_commit_children` produce canonical child slots directly. The attempt was reverted after measurement; the code is back to host active-child extraction plus dense active-plane compaction.
- Gates before measurement: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- Rejected result: `benchmark-results/gpuprof/20260706T072032Z-dec-flag-aware-children.json`; CPU median `2138.9ms`, CUDA median `470.3ms`, speedup `4.54x`, proof byte-identical, residency clean. The change did collapse `pi_dec.split` from about `37ms` to about `0.3ms`, proving the host flag join was removed, but `fold.superneo.pi_dec.open_children.y_zcol` became the new top wall (`~50.7ms`) through the flag-aware canonical path. This lost against both the fresh `464.0ms` baseline and the accepted `463.1ms` line.
- Restore check: after reverting, `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; and `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` passed. Restore benchmark `benchmark-results/gpuprof/20260706T072747Z-dec-flag-aware-children-reverted.json` reported CPU median `2129.2ms`, CUDA median `467.9ms`, speedup `4.56x`, proof byte-identical, residency clean; strict check against `benchmark-results/gpuprof/20260706T071248Z-gpu-stage-baseline.json` passed by tolerance (`464.0ms -> 467.9ms`, `27` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-wjvwiu45 --validate-gpuprof` was clean (`max_reconciliation_error_ms=0`, `stage_mismatches=0`).
- Verdict: rejected and reverted. Do not retry flag-masked canonical child openings/commitments as a standalone design. The useful structural lesson is narrower: removing the split flag host join is valuable only if downstream work remains dense. A future DEC active-child slice must compact active children into a dense device prefix and carry a device active-index/active-count surface through y/commit/scatter, or else change the terminal consumer/decider contract so the current full-audit child materialization boundary disappears.

## 2026-07-06T07:52:00Z — DEC forms/split/commit DAG stream reorder rejected and reverted

- Stage audit: fast-terminal baseline `benchmark-results/gpuprof/20260706T073624Z-e2e-gpu-fast-terminal.json` reported CPU median `2135.7ms`, CUDA median `439.7ms`, speedup `4.86x`, proof byte-identical, residency clean. `ncu` showed the actual `dec_split` kernel is only `43.94us` with `80.94%` achieved occupancy, so the visible `pi_dec.split` wall is host/API attribution around a wider DEC boundary, not a slow split kernel.
- Hypothesis: build DEC forms on a forked device stream, split on the main stream, enqueue child commitments before y-ring, then join forms only before y-ring consumes them. This follows the local DAG (`forms` independent of `split`, and `commit_children` independent of `forms`) and should expose legal overlap without changing child compaction or proof bytes.
- Diff summary: temporarily added `Device::fork` / `Device::join`, routed `device_ring_forms` through a forked `Device`, passed that device into `eval_and_commit_children`, moved `commit_children` ahead of `y_ring`, and added `fold.superneo.pi_dec.open_children.forms_join`. The attempt was reverted after measurement.
- Gates before measurement: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec`.
- Rejected result: `benchmark-results/gpuprof/20260706T074323Z-dec-dag-streams-fast-terminal.json`; CPU median `2141.0ms`, CUDA median `442.7ms`, speedup `4.84x`, proof byte-identical, residency clean. Strict check against the fast baseline passed by tolerance, but the candidate regressed `+3.0ms`, increased the DEC open-children host joins from `72` to `76`, and kept `pi_dec.split` around `38.7ms`.
- Gpuscope: `python3 scripts/gpuscope/cli.py analyze /tmp/gpuprof-nruqldjz --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T074323Z-dec-dag-streams-fast-terminal.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`) and ranked `fold.superneo.pi_dec.commit_children` as the next exclusive lever, not the split/forms reorder.
- Restore check: after reverting, `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; and the three parity gates passed. Restore benchmark `benchmark-results/gpuprof/20260706T075200Z-dec-dag-streams-reverted-fast-terminal.json` reported CPU median `2137.2ms`, CUDA median `437.8ms`, speedup `4.88x`, proof byte-identical, residency clean.
- Verdict: rejected and reverted. Do not retry DEC forms/split/commit stream reordering as a standalone slice. The local DAG is valid, but this stream grain adds joins and does not remove the real proof/output or active-child boundary.

## 2026-07-06T08:00:53Z — DEC child-commit stream overlap rejected and reverted

- Stage audit: after the previous restore, `gpuscope` ranked `fold.superneo.pi_dec.commit_children` as the next exclusive lever (`recoverable_ms=13.877`). The local DAG permits `commit_children`, `open_children.y_ring`, and `open_children.y_zcol` to run after active-plane compaction because all three read the compact active planes and write disjoint outputs.
- Hypothesis: enqueue child commitments on a forked stream immediately after active-plane compaction, run `y_ring` on the main stream and `y_zcol` on its existing forked stream, then finish the commitment download at the existing emit boundary. Expected value: overlap a real DEC child branch instead of preserving the CPU-shaped child order.
- Diff summary: temporarily added `DeviceAjtai::commit_planes_device_on_stream`, `PendingChildCommitments`, and `enqueue_active_plane_commitments`, with commitments produced on a forked stream and synchronized at `fold.superneo.pi_dec.emit.download`. The attempt was reverted after measurement.
- Gates before measurement: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- Rejected result: `benchmark-results/gpuprof/20260706T075746Z-dec-commit-stream-fast-terminal.json`; CPU median `2137.3ms`, CUDA median `445.3ms`, speedup `4.83x`, proof byte-identical, residency clean. Strict check against `benchmark-results/gpuprof/20260706T075200Z-dec-dag-streams-reverted-fast-terminal.json` failed: `fold.superneo.pi_dec.open_children.y_zcol` wall regressed `12.9ms -> 16.5ms`, with `dec_y_zcol` kernel time rising `12.1ms -> 15.4ms`.
- Gpuscope: `python3 scripts/gpuscope/cli.py analyze /tmp/gpuprof-p2yy23w2 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T075746Z-dec-commit-stream-fast-terminal.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`) and still ranked `fold.superneo.pi_dec.commit_children`, confirming the lever is real but this overlap grain contends with sibling kernels instead of reducing the critical path.
- Restore check: after reverting, `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; and restore benchmark `benchmark-results/gpuprof/20260706T080053Z-dec-commit-stream-reverted-fast-terminal.json` reported CPU median `2135.6ms`, CUDA median `443.2ms`, speedup `4.82x`, proof byte-identical, residency clean. Strict check against the fast baseline passed clean (`437.8ms -> 443.2ms`, `24` comparisons, `0` regressions), and `gpuscope analyze /tmp/gpuprof-yyfmkkkt --validate-gpuprof` reconciled cleanly.
- Verdict: rejected and reverted. Do not retry simple child-commit/y-ring/y-zcol stream overlap. The legal DAG exists, but these branches are large memory-bandwidth-heavy kernels over the same resident planes and contend on the GPU. A future DEC child-commit improvement needs graph capture, a coarser fused child-output kernel, or a changed downstream consumer boundary; not more streams around the same three kernels.

## 2026-07-06T08:09:39Z — DEC y_zcol column-axis chunking accepted

- Stage audit: the failed child-commit stream attempt made `dec_y_zcol` contention visible, so `ncu --set basic --kernel-name regex:dec_y_zcol ./target/release/parity dec` was run against the restored direct kernel. It showed only `3` blocks on a `128`-SM GPU, `16.57%` achieved occupancy, and an explicit grid-underfill warning. The old implementation exposed only `(child, lane)` parallelism and hid the column axis inside each long-loop thread.
- Dependency DAG: each `y_zcol[child][lane]` is an ascending sum over columns congruent to that lane. Column chunks are independent partial sums; the only dependency is the final ascending chunk reduction for that `(child, lane)`.
- Hypothesis: split `dec_y_zcol` into `(child, lane, column_chunk)` partial kernels plus a deterministic ascending reduction. Expected value: expose legal column-axis CUDA work while preserving byte-identical field order and avoiding sibling-stream bandwidth contention.
- Diff summary: `crates/neo-prover-cuda/src/kernels/pi_dec.rs` now uses the direct one-kernel path only for tiny shapes. Larger shapes allocate an uninitialized partial buffer, launch `dec_y_zcol_partials`, then launch `dec_y_zcol_reduce`. The partial kernels use `weight.scale_base(value)` and preserve ascending-column semantics inside each chunk.
- Gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- Accepted result: `benchmark-results/gpuprof/20260706T080910Z-dec-y-zcol-chunked-fast-terminal.json`; CPU median `2138.4ms`, CUDA median `433.7ms`, speedup `4.94x`, proof byte-identical, residency clean. Strict check against `benchmark-results/gpuprof/20260706T075200Z-dec-dag-streams-reverted-fast-terminal.json` passed clean (`437.8ms -> 433.7ms`, `24` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-9ihfkabt --validate-gpuprof` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`).
- Finding: `fold.superneo.pi_dec.open_children.y_zcol` is no longer a major DEC leaf (`~1.8ms` wall in the accepted e2e run). The top exclusive lever moved back to `fold.superneo.pi_dec.commit_children` (`recoverable_ms=14.286`, `fix_class=graph_capture`), but prior stream overlap around the same child planes already showed memory-bandwidth contention. Treat further DEC child work as a graph/fused-output or downstream-consumer boundary problem, not another simple stream split.
- Verdict: accepted as the new fast-terminal baseline for this branch. This is a structural CUDA-utilization fix, not a benchmark-contract change.

## 2026-07-06T08:14:04Z — DEC y_zcol chunk size tuned with ncu

- Follow-up audit: `ncu --set basic --kernel-name regex:dec_y_zcol_partials ./target/release/parity dec` on the accepted 16-column chunks showed the new partial kernel was still underfilled (`71` blocks for `128` SMs, `14.88us`, `16.28%` achieved occupancy).
- Hypothesis: reduce the chunk width from `16` to `8` columns so the same legal `(child, lane, column_chunk)` work launches at least one block per SM on the SHA fixture, while retaining the deterministic ascending reduction.
- Diff summary: changed `Y_ZCOL_CHUNK_COLS` in `crates/neo-prover-cuda/src/kernels/pi_dec.rs` from `16` to `8`.
- Gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- NVIDIA tooling: the adjusted partial kernel launches `139` blocks, runs in `9.98us`, and raises achieved occupancy to `17.98%`. `ncu` still reports a small-grid/occupancy warning, but the direction is correct and the kernel is no longer below one SM wave.
- Accepted result: `benchmark-results/gpuprof/20260706T081335Z-dec-y-zcol-chunked8-fast-terminal.json`; CPU median `2136.6ms`, CUDA median `430.1ms`, speedup `4.97x`, proof byte-identical, residency clean. Strict check against `benchmark-results/gpuprof/20260706T080910Z-dec-y-zcol-chunked-fast-terminal.json` passed clean (`433.7ms -> 430.1ms`, `23` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-oxq0dei2 --validate-gpuprof` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`).
- Verdict: accepted as the new fast-terminal baseline. This is still a local DEC kernel fix; the next structural frontier remains Pi_CCS row proof-output/FE ownership or a larger whole-fold command boundary.

## 2026-07-06T08:26:51Z — Pi_CCS FE row proof-log deferral rejected and reverted

- Stage audit: accepted baseline `benchmark-results/gpuprof/20260706T081335Z-dec-y-zcol-chunked8-fast-terminal.json` reported CPU median `2136.6ms`, CUDA median `430.1ms`, speedup `4.97x`, proof byte-identical, residency clean. The top real Pi_CCS lever was `fold.superneo.pi_ccs.sumcheck.fe.row_download` (`25.9ms` recoverable), caused by downloading FE row proof-log coefficients in the middle of the row-trace path.
- Dependency DAG: the row proof-log coefficients are not consumed by Π_RLC or Π_DEC; they are proof material. In principle they can leave the Pi_CCS critical path if the final proof assembly/export owns the download. That does not, by itself, remove a CPU/GPU crossing.
- Hypothesis: convert the row-trace path to keep a deferred Pi_CCS proof plus an owned FE-row proof-log exporter, then finish proof assembly at egress. Expected value: eliminate the mid-Pi_CCS row-download join without changing proof bytes.
- Diff summary: temporarily added `DeviceFeRowProofLogExporter`, a `DeferredFeRows` adapter state, and changed the RowTrace call site to `defer_from_parts_with_device_backends_and_transcript_mode`. The attempt was reverted after measurement; the path is back to immediate `prove_from_parts_with_backends_and_transcript_mode`.
- Gates before measurement: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Rejected result: `benchmark-results/gpuprof/20260706T082242Z-fe-row-deferred-exporter-fast-terminal.json`; CPU median `2136.8ms`, CUDA median `440.3ms`, speedup `4.83x`, proof byte-identical, residency clean. The explicit `row_download` leaf collapsed from `25.9ms` to `0`, but `fold.egress.export` grew by `13.7ms`, D2H copies rose `4 -> 8`, and total online CUDA regressed by `+10.2ms`. `gpuscope analyze /tmp/gpuprof-eqw87a_1 --validate-gpuprof` reconciled cleanly and still ranked `fold.superneo.pi_ccs.sumcheck.fe` as the top lever.
- Restore check: after reverting, `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; and `timeout 300s ./target/release/parity nifs` passed. Restore benchmark `benchmark-results/gpuprof/20260706T082621Z-revert-fe-row-deferred-exporter-fast-terminal.json` reported CPU median `2141.4ms`, CUDA median `428.9ms`, speedup `4.99x`, proof byte-identical, residency clean; strict check against the accepted baseline passed clean (`430.1ms -> 428.9ms`, `23` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-c9h2di76 --validate-gpuprof` reconciled cleanly.
- Verdict: rejected and reverted. Do not retry FE row proof-log deferral as a standalone proof-assembly relocation. It proves the row log is proof material, but merely moving the download from Pi_CCS to egress is not GPU ownership. The next Pi_CCS attempt must either make the row/tail proof material device-owned until a real downstream GPU consumer/proof exporter can consume it without a new timed D2H, or attack the FE stage as a graph/launch-bound scheduling problem instead of proof-log placement.
## 2026-07-06T08:49:14Z — Pi_CCS oracle row-table stream split rejected and reverted

- Stage audit: fresh full-audit baseline `benchmark-results/gpuprof/20260706T083321Z-gpu-stage-baseline.json` reported CPU median `2148.7ms`, CUDA median `458.7ms`, speedup `4.68x`, proof byte-identical, residency clean. `gpuscope analyze /tmp/gpuprof-bv9qu98o --validate-gpuprof` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`). Pi_CCS oracle still had host/API idle, and the local DAG allows resident MCS row tables to be built independently from carried Eval table work before the FE oracle consumes both.
- Hypothesis: build the resident MCS row tables on forked CUDA streams and join them only when `DeviceFeOracle` consumes the deferred tables. Expected value: overlap independent Pi_CCS oracle F-table work with later oracle work instead of preserving the CPU-shaped row-table order.
- Diff summary: temporarily added `DeviceRowMatrices::build_row_table_device_on_stream`, tracked producer streams in `DeferredMcsRowTables`, forked one stream per resident row table, and joined all table streams before `DeviceFeOracle::from_snapshot_with_deferred_and_workspace` assembled the FE table sources. The attempt was reverted after measurement.
- Gates before measurement: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Rejected result: `benchmark-results/gpuprof/20260706T083856Z-fe-oracle-row-table-streams.json`; CPU median `2119.8ms`, CUDA median `456.9ms`, speedup `4.63x`, proof byte-identical, residency clean. Strict check against the fresh baseline passed by tolerance (`458.7ms -> 456.9ms`, `25` comparisons, `0` regressions), but the improvement was only `1.8ms`, below noise, and no boundary was removed. Pi_CCS oracle idle improved by about `1.3ms`, but FE stayed flat/slightly worse and the stage still carried the same H2D/D2H/sync surfaces.
- Restore check: after reverting, `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; and `timeout 300s ./target/release/parity nifs` passed. Restore benchmark `benchmark-results/gpuprof/20260706T084914Z-fe-oracle-row-table-streams-reverted.json` reported CPU median `2131.2ms`, CUDA median `455.4ms`, speedup `4.68x`, proof byte-identical, residency clean. Strict check against the fresh baseline passed clean (`458.7ms -> 455.4ms`, `25` comparisons, `0` regressions), and `gpuscope analyze /tmp/gpuprof-0xjo5l07 --validate-gpuprof` reconciled cleanly.
- Verdict: rejected and reverted. Do not retry per-row-table stream splitting as a standalone Pi_CCS oracle slice. It follows a valid local DAG, but the execution grain is too small to create a meaningful e2e win and does not reduce CPU/GPU crossings. The next Pi_CCS oracle/FE attempt should change the wider command/proof-output boundary or persistent execution grain, not add more fine-grained streams around row-table construction.

## 2026-07-06T08:57:00Z — Pi_CCS FE ncu audit

- Tooling: saved one-launch Nsight Compute reports for the two FE hot-kernel classes:
  - `benchmark-results/ncu-fe-round-partials-20260706T0856.ncu-rep`
  - `benchmark-results/ncu-p2-transcript-absorb-device-challenge-20260706T0857.ncu-rep`
- `fe_round_partials` finding: first profiled launch was `49` blocks x `256` threads on a `128`-SM GPU, `128` registers/thread, `33.33%` theoretical occupancy, `16.46%` achieved occupancy, `0.19` waves/SM, `94.27us`, `14.04%` compute throughput, `2.49%` DRAM throughput. Later FE rounds shrink further, eventually to one-block launches.
- `p2_transcript_absorb_device_challenge` finding: profiled launch was `1` block x `256` threads, `62` registers/thread, `66.67%` theoretical occupancy, `2.14%` achieved occupancy, `0.00` waves/SM, `199.94us`, `0.07%` compute throughput. This is serial Fiat-Shamir transcript state work; the issue is execution grain and launch count, not a missing row/column parallel axis inside that single transcript state.
- Decision: Pi_CCS FE row work is underfilled and register-limited, but the standalone kernel body is not the next 20x lever. The bigger structural problem is the FE command/proof-output boundary: many tiny transcript/round launches and a required row proof-log materialization under the current full-audit proof contract. Previously measured standalone proof-log deferral only moved the same D2H to egress and regressed. Do not retry FE row-kernel micro-tuning or transcript-kernel-only changes as standalone work; any future FE attempt must change the wider command/proof-output/consumer boundary or capture a whole dependency-correct command stream.

## 2026-07-06T09:03:31Z — DEC fused child-output mat-vec rejected and reverted

- Dependency DAG: after active child-plane compaction, `open_children.y_ring` and `commit_children` both read the same resident child planes and write disjoint outputs. Earlier stream overlap was legal but lost to memory/mat-vec contention, so this tested a coarser execution grain instead of another stream split.
- Hypothesis: concatenate the DEC form rows and Ajtai PP rows into one device matrix, run one child-output ring mat-vec, then split the output into `y_ring` words and child commitment words. Expected value: remove a duplicate ring-mat-vec launch sequence over the same active planes while preserving dense active-child arithmetic and byte-identical proof material.
- Diff summary: temporarily added a DEC child-output workspace, exposed the device Ajtai PP buffer, fused `y_ring` + commitment rows into one `ring_mat_vec`, extracted canonical commitment words from the fused output, and decoded child claims from the fused host download. The attempt was reverted after measurement.
- Gates before measurement: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity dec`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`. All proof surfaces were byte-identical.
- Rejected result: `benchmark-results/gpuprof/20260706T085916Z-dec-fused-child-output-fast.json`; fast-terminal CPU median `2135.1ms`, CUDA median `430.6ms`, speedup `4.98x`, proof byte-identical, residency clean. `gpuprof.py check benchmark-results/gpuprof/20260706T090016Z-e2e-gpu-fast-contract-current.json ...` failed: `fold.superneo.pi_dec.open_children` regressed `28.9ms -> 59.0ms`, `open_children.y_ring` regressed `26.2ms -> 56.2ms`, `mat_vec_coeff_partials` moved `25.3ms -> 54.5ms`, and a new `plane_copy_slice` cost appeared.
- Restore: reverted the fused child-output changes and rebuilt. Restore snapshot `benchmark-results/gpuprof/20260706T090307Z-dec-fused-child-output-reverted-fast.json`; CPU median `2147.1ms`, CUDA median `426.2ms`, speedup `5.04x`, proof byte-identical, residency clean. Check against `20260706T090016Z-e2e-gpu-fast-contract-current.json` passed with 0 regressions. `gpuscope analyze /tmp/gpuprof-6vdcf68y --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T090307Z-dec-fused-child-output-reverted-fast.json` reconciled cleanly and again ranked `fold.superneo.pi_dec.commit_children` as the top fast-contract lever.
- Finding: row-concatenating `y_ring` and child commitments is not a useful fusion. The two outputs remain separate row products, so the fused matrix increases the `y_ring` mat-vec body instead of eliminating arithmetic. Do not retry matrix-row concatenation as a standalone DEC child-output design. A future DEC child-output improvement needs a different algorithmic resource model, graph capture that attacks API overhead without growing mat-vec work, or a downstream consumer boundary change.
- Verdict: rejected and restored. No code from this attempt remains.

## 2026-07-06T09:22:03Z — Pi_CCS FE row readback packing rejected and reverted

- Stage audit: fresh full-audit baseline `benchmark-results/gpuprof/20260706T090719Z-gpu-stage-baseline.json` reported CPU median `2131.0ms`, CUDA median `455.7ms`, speedup `4.66x`, proof byte-identical, residency clean. The visible Pi_CCS row lever was still `fold.superneo.pi_ccs.sumcheck.fe.row_download` at about `25.4ms`, with `12` small D2H copies for transcript state, row coeff log, and row challenges.
- Hypothesis: pack transcript state, coeff log, and challenge log into one reusable device buffer, then perform one pinned host download per fold. Expected value: reduce the row-download crossing count without changing the full proof-output contract.
- Diff summary: temporarily added a `row_readback_words`/`row_readback_host` workspace in `FePhaseWorkspace`, plus `download_row_trace` and `download_row_summary` helpers that used `plane_copy_slice` to pack row data before one D2H. The attempt was reverted after measurement.
- Gates before measurement: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Rejected result: `benchmark-results/gpuprof/20260706T091809Z-fe-row-trace-packed.json`; CPU median `2143.8ms`, CUDA median `460.9ms`, speedup `4.65x`, proof byte-identical, residency clean. The D2H count inside `row_download` fell from `12` to `4`, but the path added `12` `plane_copy_slice` launches and regressed `+5.2ms` from the fresh baseline. `gpuscope analyze /tmp/gpuprof-9c50qp27 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T091809Z-fe-row-trace-packed.json` reconciled cleanly.
- Restore check: after reverting, `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; and `timeout 300s ./target/release/parity nifs` passed. Restore benchmark `benchmark-results/gpuprof/20260706T092203Z-fe-row-trace-pack-reverted.json` reported CPU median `2148.2ms`, CUDA median `458.9ms`, speedup `4.69x`, proof byte-identical, residency clean.
- Finding: packing small row downloads with extra device copy launches is not GPU ownership; it changes copy shape but leaves the same proof-log materialization boundary in place and makes the online median worse. Do not retry FE row readback packing as a standalone slice. The next Pi_CCS FE move must change the consumer/proof boundary or execution grain, not repack the same host export.
- Verdict: rejected and reverted. No code from this attempt remains.

## 2026-07-06T09:24:33Z — Whole-FE diagnostic refreshed after row-readback revert

- Diagnostic command: `timeout 300s python3 scripts/gpuprof/gpuprof.py run e2e_whole_fe_bench --repeat 3 --assert-residency --json benchmark-results/gpuprof/20260706T092433Z-whole-fe-diagnostic-current.json`.
- Result: CPU median `2142.7ms`, CUDA median `485.5ms`, speedup `4.42x`, proof byte-identical. The command exited nonzero because the residency gate correctly failed: `fold.superneo.pi_ccs.sumcheck` D2H was `0.36MB` against the `0.25MB` budget.
- Gpuscope: `python3 scripts/gpuscope/cli.py analyze /tmp/gpuprof-t6w9nv9b --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T092433Z-whole-fe-diagnostic-current.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`).
- Finding: whole-FE remains architecturally pointed in the right direction but still wrong as a hot path. It reduces the visible default row-download leaf to about `6.5ms`, but pays `1050` FE launches, extra `p2_transcript_io_ops`, a `fold.egress.export` proof-log export of about `13.9ms`, and the Pi_CCS sumcheck D2H budget violation. The remaining blocker is not row-download packing; it is the proof/output seam that still requires full host `CeClaim` materialization and digest computation before Π_RLC.
- Next valid direction: introduce a real Pi_CCS light-shell/device-surface handoff across `neo-reductions` and `neo-fold-clean`, so Π_RLC can consume the compact output digest/shell plus resident K surfaces while full verifier-facing `CeClaim` outputs materialize only at proof egress. Do not promote whole-FE or graph variants until that seam removes the summary/output D2H budget violation.

## 2026-07-06T09:34:52Z — Pi_CCS terminal output shell ownership seam

- Stage audit: the whole-FE diagnostic showed the remaining blocker is the Pi_CCS proof/output boundary, not row-readback packing. The adapter already had resident `Y_eval`/NC surfaces for Pi_RLC, but the deferred proof still exposed only full host `CeClaim` outputs plus scattered metadata accessors. That kept Pi_RLC shape metadata partially reconstructed from the caller's fresh/running inputs instead of owned by the Pi_CCS terminal state.
- Dependency DAG: `Pi_CCS terminal state -> Pi_RLC claim shell` needs output count, `m_in`, row challenge point, column challenge point, `y_zcol` presence, and fold digest. Full verifier-facing `CeClaim` output materialization is proof/verification material and should not be the authority for this small RLC handoff once the device surfaces are resident.
- Hypothesis: introduce a compact `PiCcsTerminalOutputShell` owned by `neo-reductions`, expose it through the paper-layer deferred proof, and make the CUDA adapter build `ClaimShellMetadata` directly from that shell. Expected value: move ownership of Pi_CCS output metadata to the Pi_CCS terminal state, preparing the next step where full host `CeClaim` materialization can be delayed to proof egress. No speedup was claimed for this slice.
- Diff summary: `PiCcsReplayTerminalState` now carries `PiCcsTerminalOutputShell`; `PiCcsDeferredProof` exposes `output_shell()` and its metadata accessors read the shell; `neo-fold-clean::paper::reductions::pi_ccs::DeferredProof` exposes the shell; `crates/neo-prover-cuda/src/adapter.rs` removed the caller-derived `first_pi_ccs_output_m_in` path and `PiCcsProofState::claim_shell_metadata()` now sources deferred metadata from the shell.
- Gates: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Benchmark: `benchmark-results/gpuprof/20260706T093419Z-pi-ccs-output-shell.json`; CPU median `2137.9ms`, CUDA median `462.2ms`, speedup `4.63x`, proof byte-identical, residency clean. `gpuprof.py check benchmark-results/gpuprof/20260706T092203Z-fe-row-trace-pack-reverted.json benchmark-results/gpuprof/20260706T093419Z-pi-ccs-output-shell.json` passed tolerance (`458.9ms -> 462.2ms`, `26` comparisons, `0` regressions), but the median moved the wrong way by `+3.3ms`; no accepted performance baseline ratchet. `gpuscope analyze /tmp/gpuprof-e2bksusj` reconciled cleanly (`max_reconciliation_error_ms=0`, `idle_overlap_clamp_ms=0`) and ranked `fold.superneo.pi_dec.emit.planes` as the top exclusive lever under the current full-audit contract.
- Verdict: kept as a structural ownership prerequisite, no speedup claim. The next Pi_CCS output-seam slice must use this shell to make full host `CeClaim` materialization optional/deferred and to remove the remaining Pi_CCS summary/output D2H pressure; otherwise this seam is just cleaner metadata ownership.

## 2026-07-06T09:56:35Z — Device Pi_CCS output digest for rho sampling rejected and reverted

- Stage audit: `PiCcsTerminalOutputShell` made Pi_CCS output metadata owned by the Pi_CCS terminal state, but the timed adapter still bound Π_RLC rhos from the host-computed `pi_ccs_proof_state.outputs_digest()`. The next architectural question was whether Π_RLC could bind a device-computed Pi_CCS output digest directly from resident K surfaces and the new shell metadata.
- Dependency DAG tested: resident Pi_CCS K surfaces + shell metadata -> device Pi_CCS output digest -> device transcript absorb -> device rho sampling. This is directionally aligned with the full-GPU goal because full verifier-facing `CeClaim` materialization should eventually be proof-egress material, not online Π_RLC authority.
- Hypothesis: compute the Pi_CCS output digest on device from `PiCcsOutputDigestShell` plus resident K surfaces, absorb that digest through the device transcript, and sample rhos without host-owned output digest binding. Expected value: remove another host-owned Pi_CCS -> Pi_RLC protocol boundary.
- Diff summary: temporarily wired `DevicePiCcsOutputsDigest::compute_from_shells` into the timed adapter path and added a `sample_rhos_device_from_output_digest_deferred` helper that absorbed the device digest before sampling rhos. The attempt was reverted after measurement; the reusable shell/digest parity infrastructure from the earlier slice remains.
- Gates before restore: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Rejected result: `benchmark-results/gpuprof/20260706T095059Z-device-output-digest-rhos.json`; CPU median `2128.4ms`, CUDA median `584.7ms`, speedup `3.66x`, proof byte-identical, residency clean. The new `fold.superneo.pi_ccs.output.digest.device` stage cost about `126ms` across four folds, with `p2_hash_fields` taking about `123ms` in eight underfilled one-block hash instances. The boundary was correct, but the execution grain was wrong: generic device hashing of full output-claim preimages is slower than the current host digest path.
- Restore check: reverted the timed adapter activation and temporary RLC device-digest sampling helper. `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; and `timeout 300s ./target/release/parity nifs` passed. Restore benchmark `benchmark-results/gpuprof/20260706T095635Z-device-output-digest-rhos-reverted.json` reported CPU median `2138.9ms`, CUDA median `457.8ms`, speedup `4.67x`, proof byte-identical, residency clean. Strict check against `benchmark-results/gpuprof/20260706T092203Z-fe-row-trace-pack-reverted.json` passed clean (`458.9ms -> 457.8ms`, `26` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-675uej3b --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T095635Z-device-output-digest-rhos-reverted.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`).
- Verdict: rejected and reverted. Do not retry device Pi_CCS output digest by hashing full claim preimages with generic `p2_hash_fields` in the hot path. A future Pi_CCS output-digest move must either fuse/batch digest construction with output materialization at a much coarser grain, or wait until a whole-phase/whole-fold proof-output consumer can keep the digest/proof material on device without adding serial underfilled Poseidon2 work.

## 2026-07-06T10:03:46Z — DEC forms/split stream overlap rejected and reverted

- Stage audit: after the device-output-digest restore, `gpuscope analyze /tmp/gpuprof-675uej3b --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T095635Z-device-output-digest-rhos-reverted.json` reconciled cleanly and ranked `fold.superneo.pi_dec.emit.planes` as the top exclusive lever. `fold.superneo.pi_dec.split` still showed a large stage wall around the tiny split kernel because the host needs split flags before active-child compaction.
- Dependency DAG tested: `device_ring_forms` depends on the DEC opening point and resident forms inputs; `dec_split` depends on the parent witness and writes child planes plus flags. Locally those two branches are independent until child openings consume both compact child planes and ring forms.
- Hypothesis: fork a split stream before DEC forms, enqueue parent split plus flag download on the split stream, build forms on the main stream, and join the split stream before active-child compaction. Expected value: expose legal forms/split overlap without changing the active-child proof contract.
- Diff summary: temporarily routed `split_parent` through a forked stream while keeping the accepted DEC forms workspace and active-child compaction path intact. The attempt was reverted after measurement.
- Gates before measurement: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- Rejected result: `benchmark-results/gpuprof/20260706T100149Z-dec-forms-split-stream.json`; CPU median `2128.3ms`, CUDA median `458.5ms`, speedup `4.64x`, proof byte-identical, residency clean. Strict check against `benchmark-results/gpuprof/20260706T095635Z-device-output-digest-rhos-reverted.json` passed by tolerance (`457.8ms -> 458.5ms`, `26` comparisons, `0` regressions), but the candidate did not improve the accepted line and `pi_dec.split` stayed around `38ms`.
- Restore check: after reverting, `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; and restore benchmark `benchmark-results/gpuprof/20260706T100346Z-dec-forms-split-stream-reverted.json` reported CPU median `2145.7ms`, CUDA median `462.1ms`, speedup `4.64x`, proof byte-identical, residency clean. `gpuprof.py check benchmark-results/gpuprof/20260706T095635Z-device-output-digest-rhos-reverted.json benchmark-results/gpuprof/20260706T100346Z-dec-forms-split-stream-reverted.json` passed (`457.8ms -> 462.1ms`, `26` comparisons, `0` regressions), and `gpuscope analyze /tmp/gpuprof-fidre8w6 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T100346Z-dec-forms-split-stream-reverted.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`).
- Finding: the local forms/split DAG is valid, but this stream split does not remove the real active-child flag boundary. The costly visible split stage is still a host-observed compaction/flags join, not a slow split kernel or a forms serialization issue.
- Verdict: rejected and reverted. Do not retry DEC forms/split stream overlap as a standalone slice. The next DEC split improvement must make downstream kernels consume device flags or a device active-child list, or otherwise remove the host flag/readback dependency; simply moving split to another stream is not GPU ownership.

## 2026-07-06T10:10:00Z — Fast GPU-resident benchmark contract refreshed

- Contract clarification: the active full-GPU goal targets a fast prover boundary where terminal private planes stay on device for a later device consumer/decider and the CPU receives proof/public material only. `e2e_bench` remains the full-audit terminal-witness guard; `e2e_gpu_fast_bench` is the optimization target for GPU-resident prover work.
- Plan update: `crates/neo-prover-cuda/PLAN-full-gpu.md` now names both benchmark contracts explicitly and points future optimization loops at `e2e_gpu_fast_bench` while preserving `e2e_bench` as the full witness parity guard.
- Measurement: `benchmark-results/gpuprof/20260706T1010-e2e-gpu-fast-contract-refresh.json`; CPU median `2138.9ms`, CUDA median `426.9ms`, speedup `5.02x`, claims-only audit byte-identical, residency clean. Repeat values were `[434.1, 426.9, 426.5]`.
- Gpuscope: `python3 scripts/gpuscope/cli.py analyze /tmp/gpuprof-klcie03c --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T1010-e2e-gpu-fast-contract-refresh.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`). It ranked `fold.superneo.pi_dec.commit_children` as the top exclusive lever, while gpuprof's leaf table still shows the first visible join at `fold.superneo.pi_dec.split`.
- Boundary signal: under the fast contract, terminal `emit.planes` is gone. Remaining structural pressure is DEC active-child/child-output scheduling, Pi_CCS FE row proof-log materialization, and Pi_CCS oracle/FE command grain. Do not spend more work on full terminal private-witness export unless a GPU decider/compression consumer is being implemented.

## 2026-07-06T10:23:00Z — NVIDIA counter audit for current fast-path frontier

- Objective refresh: the active prompt requires stage-by-stage GPU ownership and dependency-DAG scheduling. Current evidence says the local Pi_CCS oracle/FE queue is mostly exhausted as a standalone target: row-table stream splitting, FE row proof-log deferral, row-readback packing, generic device output digest, and whole-FE/graph variants were all measured and either parked or kept as structural-only prerequisites.
- Counter artifacts saved:
  - `benchmark-results/ncu-p2-transcript-20260706.ncu-rep`
  - `benchmark-results/ncu-mat-vec-coeff-partials-20260706.ncu-rep`
- `p2_transcript_absorb_device_challenge` counters: one block × 256 threads on a 128-SM GPU, `62` registers/thread, achieved occupancy about `2.1%`, compute throughput about `0.08%`, DRAM throughput about `0.02%`. This is real underuse, but it is the single Fiat-Shamir sponge state. Do not try to "parallelize" one transcript chain by adding blocks; the meaningful fix is to reduce launch/host submission cost with a wider command stream/graph, or overlap independent work around the transcript dependency.
- `mat_vec_coeff_partials` counters: representative DEC child-output launch has `422` blocks × 256 threads, `54` registers/thread, `0.82` waves/SM, achieved occupancy about `31%`, compute throughput about `36%`, DRAM throughput below `1%`, and visible SM/SMSP imbalance. The kernel is underfilled/register/imbalance limited, not a simple PCIe or DRAM-bandwidth bottleneck. Earlier stream splits and row-concatenation fusions around this work already lost; a future attempt needs a different execution grain or stable graph-friendly resident buffers, not another local stream split.
- DEC graph prerequisite: current `split_parent` allocates fresh child-plane buffers and returns them as resident `SplitPlanes` for the next fold. That is why a naive persistent scratch buffer inside `DeviceDec` is wrong: the planes are not scratch, they are cross-fold state. A graph-safe DEC child path would need a session-owned ping-pong child-plane arena or equivalent lifetime model before graph replay can be trustworthy.
- Compile/fold overlap audit: `R1csChainBuilder::append_chunk_inner` already calls `prepare_next_fold` before `compile_chunk`, and `prepare_next_fold` writes `ctx.fold_for_step`, `pending_audit`, and adapter-derived post summaries that the compiler/deposit path consumes. Current stderr shows recursive compile is typically `0.01s-0.04s` on this SHA shape. Pipelining compile against GPU fold is not the next main lever unless the compiler is split around fold authority; the payoff is capped on this benchmark and it does not move protocol ownership to the GPU.
- Current next implementation target: widen GPU ownership at a real boundary. The best remaining candidates are (1) a Pi_CCS proof/output consumer seam that removes FE row proof-log / output materialization as online authority, or (2) a session-owned DEC child-plane arena that makes active-child/output work graph-safe without breaking cross-fold residency. Do not retry small copy/packing/stream variants already parked above.

## 2026-07-06T10:21:24Z — Fast-contract stage audit, no code accepted

- Fresh fast-contract baseline: `benchmark-results/gpuprof/20260706T102100Z-gpu-stage-baseline.json`; CPU median `2138.0ms`, CUDA median `428.9ms`, speedup `4.98x`, proof byte-identical, residency clean. `gpuscope analyze /tmp/gpuprof-uagi7ek8 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T102100Z-gpu-stage-baseline.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`) and ranked `fold.superneo.pi_dec.commit_children` as the top exclusive lever (`fix_class=graph_capture`).
- Pi_CCS oracle/FE DAG status: row-table forms, weighted Eval tables, row rounds, row proof-log download, and `Y_eval` output are already GPU-owned where they do arithmetic. The remaining FE row-download is proof material, not an input to Pi_RLC/Pi_DEC, but earlier deferral/packing attempts only moved the same proof-log boundary to egress or added copy launches. The valid next Pi_CCS move is not another row-table stream split or row-log copy shape; it is a wider proof/output consumer seam where downstream phases consume device-owned terminal state and proof logs materialize only at proof egress.
- DEC child DAG status: after active-plane compaction, `commit_children`, `open_children.y_ring`, and `open_children.y_zcol` are independent readers of the same resident child planes. Multiple stream-overlap attempts proved that the DAG is legal but the execution grain is wrong: the branches contend on the same ring mat-vec resources and regress. Do not retry child-commit/y-ring/y_zcol stream overlap, row concatenation, or flag-masked all-child evaluation as standalone changes.
- Current structural target: remove the host active-child/commitment boundary without discarding dense active-child compaction. A viable future DEC slice needs a device-owned active-index/active-count surface or graph-safe session-owned child-plane/commitment arenas so the child-output path can be captured or consumed as one boundary. If that cannot be made dense and graph-safe, prefer the Pi_CCS proof/output consumer seam.
- Verdict: no code accepted in this audit slice. The next code slice must change ownership or execution grain at a boundary; legal local concurrency alone is insufficient evidence because the current ring mat-vec branches are resource-contending, not idle independent work.

## 2026-07-06T10:29:43Z — Fast-contract DAG refresh and split attribution audit

- Fresh fast-contract measurement: `benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json`; CPU median `2146.7ms`, CUDA median `429.8ms`, speedup `5.00x`, proof byte-identical, residency clean. Repeat values were `[432.3, 427.5, 429.8]`.
- Gpuscope reconciliation: `python3 scripts/gpuscope/cli.py analyze /tmp/gpuprof-73t5jm87 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json` wrote `analysis/stages.json`, reconciled with `max_reconciliation_error_ms=0`, and ranked `fold.superneo.pi_dec.commit_children` as the top exclusive lever (`recoverable_ms=13.774`, `fix_class=graph_capture`).
- DEC split DAG: `device_ring_forms` and `dec_split` are independent until child openings consume both ring forms and active child planes. Current code enqueues forms before split, then `split_parent` downloads flags and syncs. The visible `fold.superneo.pi_dec.split` wall therefore includes a host join that waits on earlier same-stream forms work, not a slow split kernel (`dec_split` is tiny). The previous forked forms/split attempt already tested the legal local overlap and did not improve the accepted line.
- Interpretation: do not treat the fresh `pi_dec.split` leaf as a standalone performance target. Optimizing it by moving the flag sync or retiming the NVTX ranges would mostly change accounting. The real structural problem is still the host-owned active-child list plus graph-unsafe child-plane lifetime: `SplitPlanes` owns device planes that become the next fold's retained running witnesses, so they cannot be converted into temporary `DeviceDec` scratch without a session-owned arena or an explicit reclaim path.
- Pi_CCS output-seam inspection: `PiCcsDeferredProof` defers FE/NC proof rounds only. The current `PiCcsReplayTerminalState` still owns full host `me_outputs`, and `optimized_engine/prove.rs` materializes those CE claims before deriving `PiCcsTerminalOutputShell`. That means the shell is a metadata ownership prerequisite, not yet a device-resident output handoff. A small accessor/helper patch would not remove the host output-materialization boundary.
- Next valid code slices remain boundary-sized: either (1) redesign Pi_CCS terminal outputs so downstream CUDA code can consume a light host shell plus backend/device-owned K surfaces while full host `CeClaim` outputs materialize only at proof egress, or (2) introduce graph-safe DEC child-plane ownership with a session-owned ping-pong/reclaim model plus a device-owned active-index surface. Do not retry local DEC split retiming, child-output stream splitting, row concatenation, FE row-log packing, or generic device output-digest hashing as standalone changes.
- Verdict: no code accepted. This audit prevents a false split-target iteration and preserves the stage-by-stage goal's rule: code only when it changes CPU/GPU ownership, removes a real crossing, or changes execution grain at a dependency boundary.

## 2026-07-06T10:32:06Z — DEC split activity flags retained on device

- Hypothesis: preserve the device-written child activity flags after `dec_split` so the next DEC slice has a real device-owned activity surface to consume. This is a graph/active-index prerequisite, not a speedup claim.
- DAG change: `dec_split -> activity_flags` now remains resident in `SplitPlanes`. The existing host `nonzero` mirror still drives today's active-child compaction and claim assembly, but the device buffer is no longer dropped immediately after the flag readback.
- Diff summary: `SplitPlanes` now owns `activity_flags: DeviceBuffer<u64>` and exposes `activity_flags()`; `split_parent` stores the same buffer it passes to `launch_dec_split` after mirroring it to the host for the current compatibility path.
- Gates: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Measurement: `benchmark-results/gpuprof/20260706T103142Z-dec-split-activity-surface.json`; CPU median `2146.6ms`, CUDA median `430.0ms`, speedup `4.99x`, proof byte-identical, residency clean. `gpuprof.py check benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json benchmark-results/gpuprof/20260706T103142Z-dec-split-activity-surface.json` passed (`429.8ms -> 430.0ms`, `23` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-fjigdl2_ --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T103142Z-dec-split-activity-surface.json` reconciled cleanly and still ranked `fold.superneo.pi_dec.commit_children` as the top exclusive lever (`fix_class=graph_capture`).
- Verdict: accepted as structural ownership groundwork only; no performance baseline ratchet. The next DEC code slice should consume `SplitPlanes::activity_flags()` to build a device active-index/active-count surface or a graph-safe child-plane schedule. If that cannot stay dense and avoid host joins, return to the Pi_CCS terminal-output consumer seam instead.

## 2026-07-06T10:40:19Z — DEC device active-index surface consumed

- Hypothesis: make the DEC child-output path consume a device-built active-child index surface instead of using the host `nonzero` list as the authority for active-plane compaction and commitment-word scatter. Expected value: structural ownership progress toward graph-safe DEC child scheduling; no standalone speedup required.
- DAG change: `dec_split -> activity_flags -> active_indices/active_count` now stays on device. `compact_active_planes` reads `active_indices` to pack only active child planes, and `commit_active_planes` scatters active commitment words back to canonical child order with `active_indices`. The host `nonzero` mirror still exists for current `Commitment` vector assembly and compatibility checks, but dense child-plane movement is no longer host-indexed.
- Diff summary: added `dec_build_active_index`, `dec_compact_active_planes`, and `dec_scatter_active_words` kernels plus launch wrappers in `src/kernels/pi_dec.rs`; extended `SplitPlanes` with `active_indices`, `active_count_device`, and `active_count`; rewired `compact_active_planes` and `commit_active_planes` in `src/reduce/dec.rs` to consume device indices.
- Gates: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- Measurement: `benchmark-results/gpuprof/20260706T103921Z-dec-device-active-index.json`; CPU median `2130.0ms`, CUDA median `427.6ms`, speedup `4.98x`, proof byte-identical, residency clean. `gpuprof.py check benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json benchmark-results/gpuprof/20260706T103921Z-dec-device-active-index.json` passed (`429.8ms -> 427.6ms`, `23` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-afc9jcrx --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T103921Z-dec-device-active-index.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`) and ranked `fold.superneo.pi_dec.commit_children` as the top exclusive lever (`recoverable_ms=14.378`, `fix_class=graph_capture`).
- Verdict: accepted as structural GPU-ownership progress. The speed movement is within noise, so do not ratchet solely on performance. The next DEC boundary step should remove the remaining host active-child authority by making child claim/commitment assembly consume device-owned child-output surfaces, or introduce the session-owned graph-safe child-plane arena needed before replaying the DEC child-output path as a captured graph.

## 2026-07-06T10:46:54Z — DEC canonical child outputs scattered on device

- Hypothesis: use the device active-index surface as the authority for canonical child output placement, not only for active plane compaction. Expected value: remove host active-child placement from `y_ring`, `y_zcol`, and commitment-word canonicalization; no standalone speedup required.
- DAG change: dense active child outputs now flow `active output words + active_indices -> canonical child words` on device for all three child-output surfaces. The host still decodes canonical `Commitment` and `CeClaim` Rust objects for the current proof API, but it no longer decides where active child output words land.
- Diff summary: removed `SplitPlanes::nonzero` and the host `scatter_active_words` helper; `eval_and_commit_children` now uses `canonicalize_active_words` backed by `dec_scatter_active_words` for `y_ring` and `y_zcol`; `commit_active_planes` commits active planes on device, scatters active commitment words into a canonical device buffer, then decodes commitments from that canonical buffer.
- Gates: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- Measurement: `benchmark-results/gpuprof/20260706T104601Z-dec-device-canonical-outputs.json`; CPU median `2127.2ms`, CUDA median `430.2ms`, speedup `4.94x`, proof byte-identical, residency clean. `gpuprof.py check benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json benchmark-results/gpuprof/20260706T104601Z-dec-device-canonical-outputs.json` passed (`429.8ms -> 430.2ms`, `23` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-2gpyju_u --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T104601Z-dec-device-canonical-outputs.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`) and still ranked `fold.superneo.pi_dec.commit_children` as the top exclusive lever (`recoverable_ms=14.379`, `fix_class=graph_capture`).
- Verdict: accepted as structural GPU-ownership progress only; no performance ratchet. The cost of the extra tiny scatter launches is visible but within the accepted gate. The remaining DEC frontier is no longer child-output placement; it is the graph-safe execution grain/lifetime model for child planes and commitment/opening outputs.

## 2026-07-06T10:56:30Z — DEC split summary and child-plane slot reuse

- Hypothesis: after ingest copies retained running planes into the current fold buffer, the old retained running-plane buffer is dead and can become the next DEC split output slot. Also, the host should mirror only a compact device-written split summary instead of downloading the full activity flags surface. Expected value: structural progress toward graph-safe DEC child-plane lifetime and less host authority over active-child metadata; no standalone speedup required.
- DAG change: `previous DEC children -> ingest.running D2D -> fold_planes` now leaves the old child-plane buffer available for `pi_dec.split` output when its shape matches. `dec_split -> activity_flags -> active_indices/active_summary` remains device-owned; the host mirrors only `[active_count, split_out_of_range]` for current proof assembly sizing and error reporting.
- Diff summary: `dec_build_active_index` now writes a two-word active summary; `SplitPlanes` stores `active_summary` instead of a count-only device buffer; `split_parent` reads the summary instead of the full flags buffer; `DeviceDec::prove` accepts an optional reusable split-plane buffer; the CUDA adapter reclaims matching retained running planes after resident commitment composition and passes them to DEC.
- Gates: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- Measurement: `benchmark-results/gpuprof/20260706T105517Z-dec-reuse-split-planes.json`; CPU median `2132.0ms`, CUDA median `432.6ms`, speedup `4.93x`, proof byte-identical, residency clean. `gpuprof.py check benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json benchmark-results/gpuprof/20260706T105517Z-dec-reuse-split-planes.json` passed (`429.8ms -> 432.6ms`, `23` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-txvz9sm0 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T105517Z-dec-reuse-split-planes.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`) and ranked `fold.superneo.pi_dec.commit_children` as the top exclusive lever (`recoverable_ms=14.363`, `fix_class=graph_capture`).
- Verdict: accepted as structural GPU-ownership progress only; no performance ratchet. The candidate is inside tolerance but slower than the accepted median, so keep `20260706T102832Z-stage-dag-current.json` as the performance baseline. The remaining DEC frontier is a coarser graph-safe execution grain for child commitments/openings; the reusable split-plane slot removes one allocation/lifetime obstacle but does not by itself solve the `commit_children` graph-capture lever.

## 2026-07-06T11:05:04Z — DEC canonical scatter fully writes inactive child slots

- Hypothesis: canonical DEC child-output surfaces should be kernel-owned, not `DeviceBuffer::zeroed` plus active-child scatter. Expected value: graph-safe canonical commitment/y surfaces whose inactive child slots are written by the same device kernel that places active child words; no standalone speedup required.
- DAG change: `active output words + activity_flags -> canonical child words` now writes every canonical child word on device. Inactive children are zeroed by `dec_scatter_active_words`; active children derive their active-source slot from the device activity flags. Canonical buffers no longer rely on a host/API-side zeroing precondition before scatter.
- Diff summary: `dec_scatter_active_words` now takes `activity_flags`, launches across `k * words_per_child`, writes inactive zeros, and scatters active words by device-side prefix count. `commit_active_planes` and `canonicalize_active_words` now allocate uninitialized canonical buffers and pass `SplitPlanes::activity_flags()` as the placement authority.
- Gates: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- Measurement: `benchmark-results/gpuprof/20260706T-canonical-scatter-fullwrite.json`; CPU median `2140.3ms`, CUDA median `431.4ms`, speedup `4.96x`, proof byte-identical, residency clean. `gpuprof.py check benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json benchmark-results/gpuprof/20260706T-canonical-scatter-fullwrite.json` passed (`429.8ms -> 431.4ms`, `23` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-ftwnzywn --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T-canonical-scatter-fullwrite.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`) and still ranked `fold.superneo.pi_dec.commit_children` as the top exclusive lever (`recoverable_ms=14.353`, `fix_class=graph_capture`).
- Verdict: accepted as graph-safety groundwork only; no performance ratchet. The accepted performance baseline remains `benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json`. This closes the stale-zeroing precondition for canonical child outputs; the next DEC slice must widen execution grain/capture for `commit_children` and `open_children`, or return to the Pi_CCS output-materialization seam.

## 2026-07-06T11:11:21Z — DEC child-output workspace reuse

- Hypothesis: DEC child-output scratch should be owned by the `DeviceDec` session, not allocated per fold inside `commit_children/open_children`. Expected value: make compact child planes, y-ring output, y-zcol scratch/output, and canonical y surfaces graph-safe by giving the child-output path stable reusable buffers; no standalone speedup required.
- DAG change: `split -> compact active planes -> child commitments/openings -> canonical child outputs` now reuses `DecChildWorkspace` buffers across folds. This removes another fold-local allocation/lifetime obstacle before capturing the DEC child-output path as a coarser graph unit. Proof-facing D2H and host Rust object assembly are intentionally unchanged.
- Diff summary: added `DecChildWorkspace` to `DeviceDec`; `compact_active_planes` now takes a workspace slot; y-ring uses `ring_mat_vec_into` with a reused output buffer; y-zcol challenge/chi/output buffers are reused; canonical y/y-zcol scatter outputs are reused through `canonicalize_active_words`.
- Gates: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- Measurement: `benchmark-results/gpuprof/20260706T-dec-child-workspace.json`; CPU median `2129.4ms`, CUDA median `432.1ms`, speedup `4.93x`, proof byte-identical, residency clean. `gpuprof.py check benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json benchmark-results/gpuprof/20260706T-dec-child-workspace.json` passed (`429.8ms -> 432.1ms`, `23` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-3rrcpr08 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T-dec-child-workspace.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`) and still ranked `fold.superneo.pi_dec.commit_children` as the top exclusive lever (`recoverable_ms=14.337`, `fix_class=graph_capture`).
- Verdict: accepted as graph-safety groundwork only; no performance ratchet. The accepted performance baseline remains `benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json`. The next DEC step must actually widen the `commit_children/open_children` execution grain, most likely by capturing the stable-buffer child-output subgraph or by changing the downstream proof/output consumer boundary. More workspace-only churn is not enough.

## 2026-07-06T11:16:05Z — FE round-partial underfill launch shape

- Hypothesis: NCU showed `fe_round_partials` launching only `49` blocks on a `128`-SM GPU in a representative row round, with `128` registers/thread and `16.46%` achieved occupancy. When the default cuda-oxide `256`-thread launch underfills the GPU, use `64`-thread blocks to expose more independent blocks. Expected value: increase legal CUDA parallelism for underfilled FE row rounds; no protocol-boundary or benchmark-contract change.
- DAG change: none. This is purely a launch-grain change inside the existing FE row-round kernel. It does not alter transcript order, proof bytes, device residency, or host authority.
- Diff summary: `launch_fe_round_partials` now routes through `fe_round_launch_config`, which keeps the normal `LaunchConfig::for_num_elems` shape except when the default block count is below `128` SMs; underfilled rounds use `64`-thread blocks to raise the grid block count.
- Gates: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity ccs_prove` (`ERROR SUMMARY: 0 errors`).
- Measurement: `benchmark-results/gpuprof/20260706T-fe-round-adaptive-block.json`; CPU median `2138.6ms`, CUDA median `429.7ms`, speedup `4.98x`, proof byte-identical, residency clean. `gpuprof.py check benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json benchmark-results/gpuprof/20260706T-fe-round-adaptive-block.json` passed (`429.8ms -> 429.7ms`, `23` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-j15inv74 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T-fe-round-adaptive-block.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`) and still ranked `fold.superneo.pi_dec.commit_children` as the top exclusive lever (`recoverable_ms=14.334`, `fix_class=graph_capture`).
- Verdict: accepted as CUDA-utilization cleanup only; no performance ratchet because the median movement is noise-level. The change does reduce the underfilled FE round-partial lint footprint in this run, but it does not solve the real remaining boundaries: `pi_dec.split` attribution/host join, `pi_ccs.sumcheck.fe.row_download`, and host `pi_rlc.combine_claims`. The next substantial work must be a boundary/execution-grain slice, not more launch-size tuning.

## 2026-07-06T11:31:00Z — DEC child commit/opening overlap rejected and restored

- Hypothesis: after the graph-safety groundwork, `commit_children`, `open_children.y_ring`, and `open_children.y_zcol` read the same compact active child planes and write disjoint outputs. Run child commitments on a forked stream while y-ring/y-zcol openings proceed on the existing streams. Expected value: expose legal DEC DAG parallelism without changing proof bytes.
- DAG result: the independence is real, but it is not useful at this execution grain. The branches are all ring mat-vec / child-plane readers, so separate streams contend for the same GPU resources instead of filling idle SMs.
- Diff summary: temporarily added a stream-parametric child-plane commitment path and a pending child-commit object, then enqueued commitments before the child openings and joined them later. The attempt was reverted; no stream-parametric commit helper remains in `DeviceAjtai`, and `DeviceDec` is back on the accepted serial child-output schedule.
- Gates before rejected measurement: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- Rejected result: `benchmark-results/gpuprof/20260706T-dec-commit-overlap.json`; CPU median `2130.2ms`, CUDA median `431.6ms`, speedup `4.95x`, proof byte-identical, residency clean. Strict check against `benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json` failed: `fold.superneo.pi_dec.open_children` regressed `29.4ms -> 44.2ms`, even though `commit_children` itself fell to `14.5ms`. The attempted overlap moved cost into sibling openings instead of reducing the critical path.
- Restore check: after reverting, `timeout 300s python3 scripts/gpuprof/gpuprof.py run e2e_gpu_fast_bench --repeat 3 --assert-residency --json benchmark-results/gpuprof/20260706T-current-after-dec-overlap-revert.json` reported CPU median `2128.1ms`, CUDA median `428.4ms`, speedup `4.97x`, proof byte-identical, residency clean. Strict check against `benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json` passed (`429.8ms -> 428.4ms`, `23` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-64xeottn --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T-current-after-dec-overlap-revert.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`) and ranked `fold.superneo.pi_dec.commit_children` as the top exclusive lever (`recoverable_ms=13.975`, `fix_class=graph_capture`).
- Verdict: rejected and restored. Do not retry naive DEC child commit/opening stream overlap. The next DEC attempt must change execution grain, most likely graph capture or a fused/downstream consumer boundary for the child-output group; otherwise return to the Pi_CCS terminal-output/proof-log seam.

## 2026-07-06T11:36:41Z — DEC commitment decode moved to emit boundary

- Hypothesis: `commit_children` should produce canonical device commitment words only. Host `Commitment` object decoding is proof/export material, not an input to child openings, so it belongs at the existing `pi_dec.emit.download` proof boundary. Expected value: cleaner DEC child-output ownership and graph-capture shape; no standalone performance claim.
- DAG change: `split -> compact active planes -> commit active planes -> canonical commitment words` now remains device-owned through `commit_children`. `emit.download` decodes the canonical commitment words alongside the y/y_zcol proof surfaces.
- Diff summary: `commit_active_planes` now returns only `DeviceBuffer<u64>` commitment words. `eval_and_commit_children` calls `ajtai.download_commitments` inside `fold.superneo.pi_dec.emit.download`, after the child commitment kernel group has finished.
- Gates: prior to this measurement the tree had already passed `timeout 300s cargo fmt --all`, `timeout 300s cargo check --workspace --release`, `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`, `timeout 300s ./target/release/parity quick`, `timeout 300s ./target/release/parity ccs_prove`, and `timeout 300s ./target/release/parity nifs`. This slice also ran `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec`, which reported `ERROR SUMMARY: 0 errors`.
- Measurement: `benchmark-results/gpuprof/20260706T-dec-commit-decode-egress.json`; CPU median `2141.6ms`, CUDA median `429.3ms`, speedup `5.01x`, proof byte-identical, residency clean. Repeat values were `[426.6, 429.4, 429.3]`.
- Baseline check: `python3 scripts/gpuprof/gpuprof.py check benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json benchmark-results/gpuprof/20260706T-dec-commit-decode-egress.json` passed (`429.8ms -> 429.3ms`, `23` comparisons, `0` regressions).
- Gpuscope: `python3 scripts/gpuscope/cli.py analyze /tmp/gpuprof-une7h1d0 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T-dec-commit-decode-egress.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `idle_overlap_clamp_ms=0`, `stage_mismatches=0`) and still ranked `fold.superneo.pi_dec.commit_children` as the top exclusive lever (`recoverable_ms=14.010`, `fix_class=graph_capture`).
- Verdict: accepted as structural boundary progress only; no performance ratchet. The accepted performance baseline remains `benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json`. This does not solve the DEC child-output graph-capture lever; it removes host proof-object decoding from the child-commit stage so the next DEC attempt can focus on coarser graph-safe execution grain rather than host object assembly.

## 2026-07-06T11:51:31Z — FE row async proof-log export rejected and reverted

- Hypothesis: FE row proof-log materialization is not consumed by Pi_RLC or Pi_DEC, so start an async pinned D2H export after Pi_CCS, let RLC/DEC continue, and decode the row rounds at proof egress. Expected value: reduce the `pi_ccs.sumcheck.fe.row_download` boundary without changing proof bytes.
- DAG result: the dependency claim is true, but the standalone implementation only moved the same proof-log boundary. `row_download` disappeared from the FE leaf, but the cost reappeared as egress/proof export and the online median regressed.
- Rejected diff summary: temporarily added a pending FE row-round export handle, a deferred proof state in the CUDA adapter, and finish helpers in the Pi_CCS wrappers. The attempt was reverted; `PendingFeRowRoundExport`, `DeferredFeRows`, `start_export_retained_row_rounds`, and `finish_with_exported_fe_rows` are gone, and the default adapter path is back on `prove_from_parts_with_backends_and_transcript_mode`.
- Gates before rejected measurement: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity ccs_prove` (`ERROR SUMMARY: 0 errors`).
- Rejected measurement: `benchmark-results/gpuprof/20260706T-fe-row-async-proof-export.json`; CPU median `2125.5ms`, CUDA median `442.0ms`, speedup `4.82x`, proof byte-identical, residency clean. `gpuprof.py check` passed by tolerance, but the median regressed by `12.2ms` against the fixed accepted baseline. `gpuscope analyze /tmp/gpuprof-yunn_sn7 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T-fe-row-async-proof-export.json` reconciled cleanly and ranked `fold.superneo.pi_ccs.sumcheck.fe` as the top lever (`recoverable_ms=62.396`, `fix_class=graph_capture`).
- Restore gates: after reverting, `timeout 300s cargo fmt --all`, `timeout 300s cargo check --workspace --release`, and `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers` passed. Symbol search confirmed the rejected FE export surface is gone.
- Restore measurement: `benchmark-results/gpuprof/20260706T-fe-row-async-proof-export-reverted.json`; CPU median `2137.6ms`, CUDA median `430.8ms`, speedup `4.96x`, proof byte-identical, residency clean. Repeat values were `[427.5, 430.8, 434.9]`.
- Restore check: `python3 scripts/gpuprof/gpuprof.py check benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json benchmark-results/gpuprof/20260706T-fe-row-async-proof-export-reverted.json` passed (`429.8ms -> 430.8ms`, `23` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-xn3ihkg9 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T-fe-row-async-proof-export-reverted.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `idle_overlap_clamp_ms=0`, `stage_mismatches=0`) and restored the top lever to `fold.superneo.pi_dec.commit_children` (`recoverable_ms=14.235`, `fix_class=graph_capture`).
- Verdict: rejected and reverted. Do not retry async FE proof-log export as a standalone retiming. A valid Pi_CCS proof-log/output slice must change the execution grain or add a downstream proof-output consumer seam; otherwise it only shifts the D2H/export cost to another profiler bucket. The accepted performance baseline remains `benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json`.

## 2026-07-06T11:59:29Z — DEC active commitment workspace

- Hypothesis: active-child commitment words are temporary graph-capture work, while the canonical commitment words are externally retained as the next-fold identity surface. Move the active temporary output into the reusable DEC child workspace without changing the canonical output contract.
- DAG change: `compact active child planes -> active commitment words -> canonical child commitment words` now writes the active commitment words into caller-owned workspace via `DeviceAjtai::commit_planes_device_into`. The canonical commitment buffer is still returned and decoded at `pi_dec.emit.download`, so cross-fold commitment identity remains unchanged.
- Diff summary: added `DeviceAjtai::commit_planes_device_into` and reused `DecChildWorkspace::active_commit_words` in the active-child path. The all-child path keeps the existing canonical commitment allocation because that buffer may be retained externally by the fold cache.
- NCU context: the current `mat_vec_coeff_partials` report (`benchmark-results/ncu-mat-vec-current-20260706.ncu-rep`) shows no local-memory spills, `54` registers/thread, `47.20%` achieved occupancy, `15.44` average active threads/warp, low DRAM throughput, and a partial-tail-wave warning. This supports changing ownership/execution grain around the child commit path rather than retrying another local stream split.
- Gates: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- Measurement: `benchmark-results/gpuprof/20260706T-dec-active-commit-workspace.json`; CPU median `2127.1ms`, CUDA median `429.6ms`, speedup `4.96x`, proof byte-identical, residency clean. Repeat values were `[427.8, 429.6, 430.5]`.
- Baseline check: `python3 scripts/gpuprof/gpuprof.py check benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json benchmark-results/gpuprof/20260706T-dec-active-commit-workspace.json` passed (`429.8ms -> 429.6ms`, `23` comparisons, `0` regressions).
- Gpuscope: `python3 scripts/gpuscope/cli.py analyze /tmp/gpuprof-1ph350t5 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T-dec-active-commit-workspace.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `idle_overlap_clamp_ms=0`, `stage_mismatches=0`) and still ranked `fold.superneo.pi_dec.commit_children` as the top exclusive lever (`recoverable_ms=14.196`, `fix_class=graph_capture`).
- Verdict: accepted as graph-safety groundwork only; no performance ratchet. The accepted performance baseline remains `benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json`. The next useful DEC attempt must widen the child-output execution grain, likely through a graph-safe stage capture or downstream consumer boundary, not another workspace-only change.

## 2026-07-06T12:05:00Z — Pi_CCS oracle/FE DAG audit

- Fresh evidence: `gpuscope analyze /tmp/gpuprof-1ph350t5 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T-dec-active-commit-workspace.json` reports `fold.superneo.pi_ccs.oracle` at `29.166ms` wall / `6.966ms` GPU busy, `fold.superneo.pi_ccs.sumcheck.fe` at `30.743ms` wall / `25.886ms` GPU busy, and `fold.superneo.pi_ccs.sumcheck.fe.row_download` at `25.471ms` wall / `25.083ms` GPU busy with only `0.010528MB` D2H.
- DAG: Pi_CCS public binding/challenge sampling precedes FE oracle construction; FE row oracle tables precede the row-round ladder; each row round is `coeffs -> Poseidon2 challenge -> fold`, so round `r+1` depends on challenge `r`. The Ajtai `Y_eval` output depends on the finalized row point; Pi_RLC already consumes resident K-surfaces after Pi_CCS output.
- Classification: the default FE row ladder is GPU-owned and currently dependency-limited at the local row-round scope. The large `row_download` window is mostly waiting for queued FE kernels to drain, not moving proof-log bytes. The previous async FE proof-log export attempt proved that retiming the download only shifts the wait to egress and regresses the median.
- Oracle note: `oracle.F` / `oracle.Eval` still have small metadata uploads and host gaps, but the largest remaining local lever in Pi_CCS oracle is under `3ms`; changing those uploads would be a small cleanup, not the structural GPU-ownership move requested by the goal.
- Decision: do not implement another local Pi_CCS row-log/export/metadata retiming. The next valid Pi_CCS attempt must change execution grain at the whole-phase/whole-fold boundary, or widen the downstream proof/output consumer seam so the host does not need terminal proof material as online authority. Until that design is ready, move to the current profiler-ranked structural target: DEC child-output graph capture.

## 2026-07-06T12:09:22Z — DEC active commitment graph capture

- Hypothesis: after moving active-child commitment words into stable workspace, capture the active-child Ajtai commitment body as a CUDA graph. Expected value: widen execution grain for the active child commit sub-DAG and remove one graph-safety blocker for a larger DEC child-output graph; no standalone speedup required.
- DAG change: `compact active child planes -> active commitment words` is now graph-captured when the `(active_count, plane_stride, pp shape, input ptr, output ptr)` key is stable. The all-child path is unchanged because its canonical commitment buffer may be retained externally as the next-fold identity surface. Canonical commitment words still leave `Pi_DEC` as an owned device buffer and are decoded at `pi_dec.emit.download`.
- Diff summary: added `DeviceAjtai::prepare_commit_planes` so graph capture can pre-size mat-vec scratch before capture, and added a keyed `DecActiveCommitGraph` cached in `DecChildWorkspace`. The graph body calls the existing `commit_planes_device_into` into caller-owned active commitment workspace, then the existing scatter kernel materializes canonical child commitment words.
- Gates: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- Measurement: `benchmark-results/gpuprof/20260706T-dec-active-commit-graph.json`; CPU median `2136.5ms`, CUDA median `429.7ms`, speedup `4.97x`, proof byte-identical, residency clean. Repeat values were `[427.1, 429.7, 430.2]`.
- Baseline check: `python3 scripts/gpuprof/gpuprof.py check benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json benchmark-results/gpuprof/20260706T-dec-active-commit-graph.json` passed (`429.8ms -> 429.7ms`, `23` comparisons, `0` regressions).
- Gpuscope: `python3 scripts/gpuscope/cli.py analyze /tmp/gpuprof-e1fbp5ux --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T-dec-active-commit-graph.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `idle_overlap_clamp_ms=0`, `stage_mismatches=0`). The top exclusive lever moved to `fold.superneo.pi_dec.emit.download` (`recoverable_ms=21.611`, `fix_class=graph_capture`), and `gpuprof` now ranks `fold.superneo.pi_dec.commit_children` at `8.3ms` recoverable rather than the previous ~`14ms` child-commit lever.
- Verdict: accepted as structural execution-grain progress only; no performance ratchet. The accepted performance baseline remains `benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json`. The next meaningful DEC step is not more active-commit workspace work; it is a proof/export consumer boundary or a wider DEC child-output graph that can address `pi_dec.emit.download` without breaking the retained canonical commitment/plane ownership contract.

## 2026-07-06T12:15:00Z — DEC emit/download seam audit

- Fresh evidence: after the active-commit graph slice, `gpuprof` ranks `pi_dec.emit.download` as a top recoverable leaf (`21.7ms` wall, `21.6ms` recoverable) and `pi_dec.split` / `pi_ccs.sumcheck.fe.row_download` as sibling waits. Gpuscope ranks `fold.superneo.pi_dec.emit.download` as the top exclusive lever.
- Ownership check: `DeviceDec::prove` still returns ordinary host `Children`, `Proof`, `SplitPlanes`, and canonical child commitment words. The adapter immediately uses `children.claims` to build the returned `RunningInstance`, proof, recomposition checks, cached running commitment host identities, and the next fold's host-visible running state.
- Trait boundary: `NifsProverAdapter::prove` currently returns `(RunningInstance, NifsProof)`. That means the CUDA adapter cannot honestly remove DEC child-claim downloads inside one DEC helper: the public API still demands host `CeClaim`s at the end of every fold. Skipping those downloads would be a benchmark-contract change unless the adapter/lifecycle grows a device-backed running/proof-output seam.
- DAG classification: `pi_dec.emit.download` is not a slow DEC arithmetic kernel. It is the host lifecycle boundary where device child surfaces are converted back into CPU protocol objects. A local pinned-copy, stream, or retiming patch would only move this wait, like the rejected FE proof-log export attempt.
- Next valid structural target: widen the NIFS/CUDA adapter boundary so the repeated online fold can carry a device-backed running surface plus a light host shell, with full host `CeClaim`/proof material materialized only at proof egress or in parity/debug mode. This must preserve CPU byte-identical proof output and verifier-side recomputation. Until that seam exists, do not retry local `pi_dec.emit.download` retiming as a standalone optimization.

## 2026-07-06T12:19:17Z — DEC child-surface and fold-output seam

- Hypothesis: the local DEC code should make the proof-materialization boundary explicit before the adapter grows a device-backed running state. `y_ring`, child commitments, and `y_zcol` are device child surfaces; turning them into host `CeClaim`/`Commitment` proof objects is a lifecycle/export concern. Expected value: clearer boundary for the next adapter seam; no standalone performance claim.
- DAG change: `open_children.y_ring + commit_children + open_children.y_zcol -> materialize host child claim surfaces` is now named as `materialize_child_claim_surfaces`. The helper owns canonical child-surface downloads and workspace restoration. Device arithmetic remains in the existing `open_children` / `commit_children` stages, while host proof-object materialization is isolated inside `fold.superneo.pi_dec.emit.download`.
- Diff summary: added `MaterializedChildSurfaces` and moved the commitment decode, canonical `y` download, optional canonical `y_zcol` download, stream join, and workspace stores out of `eval_and_commit_children` into `materialize_child_claim_surfaces`. Also replaced the positional `DeviceDec::prove` return tuple with `DecFoldOutput { children, proof, split, child_commitment_words }`, making the host protocol objects and resident device surfaces explicit at the adapter call site. The returned proof bytes and retained device commitment buffer are unchanged.
- Gates: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- Measurement: `benchmark-results/gpuprof/20260706T-dec-fold-output-seam.json`; CPU median `2138.2ms`, CUDA median `428.7ms`, speedup `5.00x`, proof byte-identical, residency clean. Repeat values were `[428.7, 425.9, 430.0]`.
- Baseline check: `python3 scripts/gpuprof/gpuprof.py check benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json benchmark-results/gpuprof/20260706T-dec-fold-output-seam.json` passed (`429.8ms -> 428.7ms`, `23` comparisons, `0` regressions).
- Gpuscope: `python3 scripts/gpuscope/cli.py analyze /tmp/gpuprof-ow4c7i8h --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T-dec-fold-output-seam.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `idle_overlap_clamp_ms=0`, `stage_mismatches=0`) and ranked `fold.superneo.pi_dec.emit.download` as the top exclusive lever (`recoverable_ms=21.645`, `fix_class=graph_capture`).
- Verdict: accepted as structural seam progress only; no performance ratchet. The accepted performance baseline remains `benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json`. The next aligned change is the adapter/lifecycle device-backed running/proof-output seam. Local `pi_dec.emit.download` copy retiming remains out of scope because the host `RunningInstance` contract still demands materialized child claims every fold.

## 2026-07-06T12:35:24Z — DEC resident all-child split rejected and reverted

- Hypothesis: `pi_dec.split` was spending ~`29-38ms` on an active-summary D2H/API join for only a few words. In `ResidentOnly` mode, skip the host active-count decision, treat all `k` children as active on device, and rely on later recomposition checks to reject bad splits. Expected value: remove a mid-fold CPU/GPU join from `split`.
- DAG result: the split join disappeared, but the design over-widened the active child set. Running every child lane made downstream child openings and downloads more expensive, so the cost moved from `split` into `open_children`/`emit` instead of leaving the critical path.
- Rejected diff summary: temporarily added `DecOutputMode::compacts_active_children`, passed that mode into `split_parent`, and made `ResidentOnly` return `active_count = shape.k` without launching/downloading `dec_build_active_index`. The attempt was reverted; `split_parent` is back to building compact active indices and checking `SplitOutOfRange` for all modes.
- Gates before rejected measurement: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- Rejected measurement: `benchmark-results/gpuprof/20260706T-dec-resident-all-children.json`; CPU median `2149.4ms`, CUDA median `447.3ms`, speedup `4.82x`, proof byte-identical, residency clean. `pi_dec.split` fell to `0.1ms`, but `pi_dec.open_children` regressed to `66.8ms` and `pi_dec.open_children.y_ring` to `35.5ms`. Strict check against `benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json` failed with `3` regressions.
- Restore gates: after reverting, `timeout 300s cargo fmt --all`, `timeout 300s cargo check --workspace --release`, `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`, `timeout 300s ./target/release/parity quick`, `timeout 300s ./target/release/parity ccs_prove`, and `timeout 300s ./target/release/parity nifs` passed.
- Restore measurement: `benchmark-results/gpuprof/20260706T-dec-resident-all-children-reverted.json`; CPU median `2139.7ms`, CUDA median `427.1ms`, speedup `5.01x`, proof byte-identical, residency clean. Repeat values were `[427.1, 426.6, 429.2]`.
- Restore check: `python3 scripts/gpuprof/gpuprof.py check benchmark-results/gpuprof/20260706T102832Z-stage-dag-current.json benchmark-results/gpuprof/20260706T-dec-resident-all-children-reverted.json` passed (`429.8ms -> 427.1ms`, `23` comparisons, `0` regressions). `python3 scripts/gpuscope/cli.py analyze /tmp/gpuprof-0d1n55tr --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T-dec-resident-all-children-reverted.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`) and ranked `fold.superneo.pi_dec.emit.download` as the top exclusive lever (`recoverable_ms=21.636`, `fix_class=graph_capture`).
- Verdict: rejected and restored. Do not retry "all children active" as a way to remove `pi_dec.split`; it defeats the compact-active child schedule and increases downstream mat-vec/download work. The next aligned DEC move remains the adapter/lifecycle device-backed running/proof-output seam, not local split retiming.

## 2026-07-06T12:52:00Z — FE row proof-log deferral retried against stage baseline and rejected

- Hypothesis: route the default row-trace path through the deferred Pi_CCS proof path so FE row coefficients stay resident until final proof assembly. Expected value: remove `fold.superneo.pi_ccs.sumcheck.fe.row_download` from the Pi_CCS critical path while keeping Pi_RLC/Pi_DEC fed by resident Pi_CCS surfaces.
- DAG result: the row proof log is not needed by Pi_RLC or Pi_DEC, but it is still needed by the current full-audit proof-output contract before egress completes. Without a downstream device proof consumer or wider command-stream boundary, deferring the log only changes where the host waits.
- Diff summary: temporarily added `DeviceFeRowProofLogExporter`, added a deferred FE-row proof-state branch in the CUDA adapter, and changed the default row-trace call site to `defer_from_parts_with_device_backends_and_transcript_mode`. The attempt was reverted after measurement; source search confirms `DeferredFeRows`/`DeviceFeProofLogExporter` are gone from `crates/neo-prover-cuda/src`.
- Gates before rejected measurement: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Baseline: `benchmark-results/gpuprof/20260706T123928Z-gpu-stage-baseline.json`; CPU median `2134.4ms`, CUDA median `455.6ms`, speedup `4.69x`, proof byte-identical, residency clean.
- Rejected result: `benchmark-results/gpuprof/20260706T124342Z-row-summary-deferred-fe.json`; CPU median `2129.7ms`, CUDA median `470.6ms`, speedup `4.51x`, proof byte-identical, residency clean. `row_download` disappeared, but `fold.egress.export` appeared at about `14.1ms` and total online time regressed `+15.1ms`.
- Restore check: after reverting, `timeout 300s ./target/release/parity quick`, `timeout 300s ./target/release/parity ccs_prove`, and `timeout 300s ./target/release/parity nifs` passed. `benchmark-results/gpuprof/20260706T124855Z-row-summary-deferred-fe-reverted.json` reported CPU median `2129.4ms`, CUDA median `459.5ms`, speedup `4.63x`, proof byte-identical, residency clean. `gpuprof.py check benchmark-results/gpuprof/20260706T123928Z-gpu-stage-baseline.json benchmark-results/gpuprof/20260706T124855Z-row-summary-deferred-fe-reverted.json` passed with `25` comparisons and `0` regressions. `gpuscope analyze /tmp/gpuprof-j0hjyxnj --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T124855Z-row-summary-deferred-fe-reverted.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`).
- Verdict: rejected and reverted. Do not retry FE row proof-log deferral, async proof-log export, pinned row download, or row-summary proof-log retiming as standalone slices. A valid next Pi_CCS proof-output attempt must change the downstream proof-output consumer boundary or the whole-fold execution grain; otherwise it only moves the same required host export to another bucket.

## 2026-07-06T13:05:00Z — DEC masked active-child path rejected against stage baseline

- Hypothesis: remove the `pi_dec.split` active-summary D2H by keeping split flags on device and making resident child `y_ring`, `y_zcol`, and commitment outputs canonical masked all-`k` surfaces. Expected value: keep the split flags device-resident without losing inactive-child arithmetic savings.
- DAG result: `split` no longer needed a host `active_count` for resident folds, but the canonical masked child outputs widened the downstream child-open/commit work. Inactive children skipped the ring products, but the first DEC join and masked child-output grain moved the cost into `open_children`.
- Diff summary: temporarily added masked ring mat-vec kernels/wrapper, masked DEC `y_zcol` kernels/wrapper, `commit_planes_masked_device_into`, and a `masked_child_outputs` split path for `ResidentOnly`; reverted after measurement.
- Gates before rejected measurement: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- Baseline: `benchmark-results/gpuprof/20260706T123928Z-gpu-stage-baseline.json`; CPU median `2134.4ms`, CUDA median `455.6ms`, speedup `4.69x`, proof byte-identical, residency clean.
- Rejected result: `benchmark-results/gpuprof/20260706T125701Z-dec-masked-active-children.json`; CPU median `2133.6ms`, CUDA median `455.9ms`, speedup `4.68x`, proof byte-identical, residency clean. `pi_dec.split` improved to about `7.5ms`, but strict check failed on `fold.superneo.pi_dec.open_children` (`28.6ms -> 49.7ms`), with new `mat_vec_coeff_partials_masked` at about `19.1ms`.
- Restore check: after reverting, `timeout 300s ./target/release/parity quick`, `timeout 300s ./target/release/parity ccs_prove`, and `timeout 300s ./target/release/parity nifs` passed. `benchmark-results/gpuprof/20260706T130132Z-dec-masked-active-children-reverted.json` reported CPU median `2130.3ms`, CUDA median `459.3ms`, speedup `4.64x`, proof byte-identical, residency clean. Strict check against the stage baseline passed (`25` comparisons, `0` regressions), and `gpuscope analyze /tmp/gpuprof-o6w8o_9k --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T130132Z-dec-masked-active-children-reverted.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`).
- Verdict: rejected and reverted. Do not retry masked all-`k` canonical child outputs as a standalone split-boundary fix. Future split work must either preserve compact active execution grain fully on device, move/remove the first DEC child-output join as a whole boundary, or be part of a device-backed running/proof-output consumer seam.

## 2026-07-06T13:14:00Z — Pi_CCS F-oracle row-table stream fanout rejected

- Hypothesis: `fold.superneo.pi_ccs.oracle.F` builds independent f-var row tables one matrix at a time on the main stream. Since each table reads the same resident witness plane and writes a disjoint output buffer, forked streams should expose real per-matrix parallelism without changing proof semantics.
- DAG result: the per-matrix row-table kernels are independent, but stream fanout at this narrow boundary is too small a grain. It increased visible `oracle.F` GPU busy and sync/join accounting without reducing the parent oracle wall or online median.
- Diff summary: temporarily added `DeviceRowMatrices::build_row_tables_device_concurrent` and routed resident-plane `device_mcs_row_tables` through it. The attempt was reverted after measurement.
- Gates before rejected measurement: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity ccs_prove` (`ERROR SUMMARY: 0 errors`).
- Baseline: `benchmark-results/gpuprof/20260706T123928Z-gpu-stage-baseline.json`; CPU median `2134.4ms`, CUDA median `455.6ms`, speedup `4.69x`, proof byte-identical, residency clean.
- Rejected result: `benchmark-results/gpuprof/20260706T130914Z-pi-ccs-row-tables-concurrent.json`; CPU median `2132.8ms`, CUDA median `456.1ms`, speedup `4.68x`, proof byte-identical, residency clean. Strict check passed (`25` comparisons, `0` regressions), but the candidate did not improve beyond noise. `oracle.F` changed from `wall 1.33ms / gpu 3.01ms / syncs 0` to `wall 1.77ms / gpu 9.75ms / syncs 56`; the parent oracle stayed flat (`39.8ms -> 39.6ms`).
- Restore check: after reverting, `timeout 300s ./target/release/parity quick` passed. `benchmark-results/gpuprof/20260706T131129Z-pi-ccs-row-tables-concurrent-reverted.json` reported CPU median `2125.7ms`, CUDA median `459.2ms`, speedup `4.63x`, proof byte-identical, residency clean. Strict check against the stage baseline passed (`25` comparisons, `0` regressions), and `gpuscope analyze /tmp/gpuprof-hkl1apce --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T131129Z-pi-ccs-row-tables-concurrent-reverted.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `stage_mismatches=0`).
- Verdict: rejected and reverted. Do not retry simple per-matrix row-table stream fanout as a standalone Pi_CCS-oracle slice. A valid oracle-concurrency design needs a wider graph/stream boundary that amortizes stream joins, or it must fuse/batch row-table work instead of adding per-table streams.

## 2026-07-06T13:30:00Z — FE row proof-log deferral retried and rejected again

- Hypothesis: reuse the reductions-level deferred Pi_CCS proof path directly from the default row-trace CUDA adapter, carrying FE row proof-log export until final proof assembly. Expected value: remove `fold.superneo.pi_ccs.sumcheck.fe.row_download` from the repeated Pi_CCS stage while leaving Pi_RLC/Pi_DEC fed by resident surfaces.
- DAG result: same family as the earlier async FE proof-log export rejection. The row proof log is not needed by Pi_RLC/Pi_DEC, but the current full-audit proof contract still needs it before proof egress completes. The change removed the FE leaf wait and reintroduced it as `fold.egress.export`, so it shifted cost rather than removing a CPU/GPU boundary.
- Rejected diff summary: temporarily added `DeviceFeRowProofLogExporter`, a `PiCcsProofState::DeferredFeRows` branch, and routed the default row-trace call through `defer_from_parts_with_device_backends_and_transcript_mode`. The attempt was reverted; source search confirms `DeviceFeRowProofLogExporter`, `DeferredFeRows`, and the row-trace deferred call are gone from `crates/neo-prover-cuda/src`.
- Gates before rejected measurement: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Baseline: `benchmark-results/gpuprof/20260706T123928Z-gpu-stage-baseline.json`; CPU median `2134.4ms`, CUDA median `455.6ms`, speedup `4.69x`, proof byte-identical, residency clean.
- Rejected result: `benchmark-results/gpuprof/20260706T131935Z-deferred-fe-row-proof-log.json`; CPU median `2129.8ms`, CUDA median `468.3ms`, speedup `4.54x`, proof byte-identical, residency clean. `gpuprof.py check` passed by tolerance, but the median regressed by `12.7ms`. The key stage movement was `pi_ccs.sumcheck.fe.row_download -> 0` and `fold.egress.export -> 14.0ms`.
- Restore gates: after reverting, `cargo fmt --all`, `timeout 300s cargo check --workspace --release`, `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`, `timeout 300s ./target/release/parity quick`, `timeout 300s ./target/release/parity ccs_prove`, and `timeout 300s ./target/release/parity nifs` passed.
- Restore measurement: `benchmark-results/gpuprof/20260706T133000Z-deferred-fe-row-proof-log-reverted.json`; CPU median `2140.0ms`, CUDA median `456.6ms`, speedup `4.70x`, proof byte-identical, residency clean. Strict check against the stage baseline passed (`455.6ms -> 456.6ms`, `25` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-fn5pmd26 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T133000Z-deferred-fe-row-proof-log-reverted.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `idle_overlap_clamp_ms=0`, `stage_mismatches=0`) and ranked `fold.superneo.pi_dec.emit.download` as the top exclusive lever (`recoverable_ms=21.619`, `fix_class=graph_capture`).
- Verdict: rejected and restored. Park standalone FE row proof-log deferral/export/retiming completely. A future Pi_CCS proof-output attempt must introduce a device-backed proof-output consumer or a whole-fold execution boundary that removes host proof-log assembly from the timed path, not just moves it to egress.

## 2026-07-06T13:38:00Z — Pi_CCS FE NVIDIA-counter audit

- Trigger: the stage-by-stage GPU ownership loop requires NVIDIA-tool evidence before another Pi_CCS FE change. Local retimings have repeatedly shifted cost, so the next FE move must be driven by hardware counters or by a wider protocol boundary.
- Tooling: `timeout 300s ncu --set basic --target-processes all --kernel-name regex:fe_round_partials --launch-count 1 --force-overwrite -o benchmark-results/ncu-fe-round-current-20260706 ./target/release/parity ccs_prove` and `timeout 300s ncu --set basic --target-processes all --kernel-name regex:p2_transcript_absorb_device_challe --launch-count 1 --force-overwrite -o benchmark-results/ncu-p2-transcript-current-20260706 ./target/release/parity ccs_prove` both passed; `parity ccs_prove` remained byte-identical under profiler replay.
- FE partials: `fe_round_partials` sampled launch used `196` blocks x `64` threads on `128` SMs, `128` registers/thread, `0.19` waves/SM, `6.33%` active warps, `15.97%` SM throughput, `14.52%` issue active, `2.84%` DRAM throughput. This is underfilled/register-heavy, not a saturated memory-bandwidth kernel.
- Device transcript: `p2_transcript_absorb_device_challenge` sampled launch used `1` block x `256` threads, `62` registers/thread, `0.00` waves/SM, `2.14%` active warps, `0.07%` SM throughput, `0.05%` issue active. This is a serial transcript node; it cannot use the whole GPU as a standalone kernel.
- Dependency audit: the heavy `fold.superneo.pi_ccs.output.y_prime` / Ajtai `Y_eval` work is not independent of FE tail/NC in the current protocol schedule. `Y_eval` is injected at the FE Ajtai-tail boundary, the tail challenges extend the FE transcript, and NC prolog/rounds come after that transcript state. Do not schedule `y_prime` beside NC unless the whole FE-tail transcript dependency is redesigned.
- Verdict: do not spend the next Pi_CCS iteration on standalone FE kernel tuning, row-log retiming, or naive y_prime/NC overlap. The valid FE directions are wider execution grain only: whole-fold graph/stream scheduling that overlaps independent work outside the FS chain, or a protocol-boundary change that removes host proof/output authority from the timed path. For immediate structural work, the restored profile and gpuscope both point back to `pi_dec.emit.download` / device-backed running-output ownership.

## 2026-07-06T13:41:04Z — Pi_CCS oracle upload staging workspace accepted

- Hypothesis: `fold.superneo.pi_ccs.oracle.upload` was still allocating, synchronizing, and freeing small transient upload buffers inside the repeated Pi_CCS oracle path. Reusing a workspace-owned staging buffer should remove CPU/GPU join/API churn without changing proof dataflow. Expected value: structural hygiene for the Pi_CCS oracle boundary, not a headline speedup.
- DAG result: host-origin table/tensor-point uploads still exist, so H2D bytes and copies are unchanged. The change removes transient buffer lifetime joins from the upload leaf: `api_calls 74 -> 54`, `api_memalloc_calls 11 -> 5`, `api_memfree_calls 7 -> 0`, `syncs 7 -> 0`, and upload API wall `0.960ms -> 0.313ms`. Parent `pi_ccs.oracle` dropped `api_calls 273 -> 253` and `syncs 16 -> 9`; online median stayed within noise.
- Diff summary: added `FeOracleWorkspace::upload_staging` and routed mixed host/table and tensor-point uploads through `take_buffer` + `copy_host_to_device` + `store_buffer` instead of `DeviceBuffer::from_host`. The buffer is reused only on the same CUDA stream and returned immediately after the staged copy kernel is enqueued.
- Gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Measurement: `benchmark-results/gpuprof/20260706T134104Z-fe-oracle-staging.json`; CPU median `2136.5ms`, CUDA median `456.2ms`, speedup `4.68x`, proof byte-identical, residency clean. Repeat values were `[456.2, 461.4, 455.7]`.
- Baseline check: `python3 scripts/gpuprof/gpuprof.py check benchmark-results/gpuprof/20260706T123928Z-gpu-stage-baseline.json benchmark-results/gpuprof/20260706T134104Z-fe-oracle-staging.json` passed (`455.6ms -> 456.2ms`, `25` comparisons, `0` regressions).
- Gpuscope: `python3 scripts/gpuscope/cli.py analyze /tmp/gpuprof-xojicvpx --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T134104Z-fe-oracle-staging.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `idle_overlap_clamp_ms=0`, `stage_mismatches=0`) and ranked `fold.superneo.pi_dec.emit.download` as the top exclusive lever (`recoverable_ms=21.629`).
- Verdict: accepted as a measured join/API reduction inside the audited Pi_CCS oracle stage; no performance ratchet. This does not solve the larger GPU-ownership problem because the upload sources are still host-built. Next aligned work should either remove those host-built oracle inputs or move to the current top boundary issue, `pi_dec.emit.download` / device-backed running-output ownership.

## 2026-07-06T14:06:50Z — DEC split workspace / dense child scheduling rejected and restored

- Hypothesis: `pi_dec.split` was still paying a tiny active-summary D2H/API wait. Reusing split metadata buffers, then trying dense child schedules for `ResidentOnly` and `Full`, might remove that join without changing the proof contract.
- DAG result: the metadata workspace only moved the wait from D2H into stream synchronization, and dense child scheduling over-widened the child-work DAG. Treating inactive children as active increased downstream mat-vec / commitment work, so the boundary cost moved into `commit_children`, `open_children`, and `emit` instead of leaving the critical path.
- Rejected measurements:
  - `benchmark-results/gpuprof/20260706T135400Z-dec-split-workspace.json`: CPU median `2138.6ms`, CUDA median `457.3ms`, speedup `4.68x`, proof byte-identical, residency clean. `pi_dec.split` D2H wait disappeared but `api/sync` wall stayed around `29ms`.
  - `benchmark-results/gpuprof/20260706T135721Z-dec-dense-resident-split.json`: CPU median `2143.1ms`, CUDA median `472.4ms`, speedup `4.54x`, proof byte-identical, residency clean. Dense resident scheduling regressed child openings/commit work.
  - `benchmark-results/gpuprof/20260706T135857Z-dec-terminal-dense-split.json`: CPU median `2137.0ms`, CUDA median `461.7ms`, speedup `4.63x`, proof byte-identical, residency clean. Strict check against `benchmark-results/gpuprof/20260706T123928Z-gpu-stage-baseline.json` failed on `fold.superneo.pi_dec` (`237.3ms -> 252.1ms`, `mat_vec_coeff_partials 25.8ms -> 37.7ms`).
- Revert diff summary: removed `DecSplitWorkspace`, `SplitState`, `SplitMetadata`, and `DecOutputMode::uses_dense_child_schedule`; restored `SplitPlanes` as the compact active-child schedule owner; restored `split_parent` to always build active indices and check `SplitOutOfRange` for every mode.
- Restore gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Restore measurement: `benchmark-results/gpuprof/20260706T140650Z-dec-split-reverted.json`; CPU median `2141.1ms`, CUDA median `456.3ms`, speedup `4.68x`, proof byte-identical, residency clean. Repeat values were `[456.3, 460.0, 453.1]`.
- Restore checks: strict check against `benchmark-results/gpuprof/20260706T123928Z-gpu-stage-baseline.json` passed (`455.6ms -> 456.3ms`, `25` comparisons, `0` regressions); strict check against `benchmark-results/gpuprof/20260706T134104Z-fe-oracle-staging.json` also passed (`456.2ms -> 456.3ms`, `25` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-u79kfk4e --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T140650Z-dec-split-reverted.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `idle_overlap_clamp_ms=0`, `stage_mismatches=0`) and ranked `fold.superneo.pi_dec.emit.download` as the top exclusive lever (`recoverable_ms=21.616`).
- Verdict: rejected and restored. Park split-local active-summary retiming, dense all-child scheduling, and masked all-k canonical child surfaces as standalone DEC split fixes. Future DEC work must change the device-backed running/proof-output consumer boundary or preserve compact active execution fully on device; otherwise it only moves the required host materialization wait.

## 2026-07-06T14:11:50Z — Fast-contract baseline refreshed after DEC restore

- Purpose: separate the GPU-resident prover target from full-audit terminal witness export. `e2e_bench` remains the full witness parity guard; `e2e_gpu_fast_bench` is the timed contract for "private terminal planes stay on device."
- Measurement: `benchmark-results/gpuprof/20260706T141150Z-fast-contract-after-dec-restore.json`; CPU median `2137.3ms`, CUDA median `428.1ms`, speedup `4.96x`, claims-only proof/audit byte-identical, residency clean. Repeat values were `[428.1, 428.1, 431.4]`.
- Check: against `benchmark-results/gpuprof/20260706T-dec-resident-all-children-reverted.json`, strict check passed (`427.1ms -> 428.1ms`, `24` comparisons, `0` regressions).
- Gpuscope: `python3 scripts/gpuscope/cli.py analyze /tmp/gpuprof-m_wagsjc --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T141150Z-fast-contract-after-dec-restore.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `idle_overlap_clamp_ms=0`, `stage_mismatches=0`) and ranked `fold.superneo.pi_dec.emit.download` as the top exclusive lever (`recoverable_ms=21.690`).
- Stage audit: full-audit-only terminal `emit.planes` is not the fast-path blocker. The remaining fast-contract blockers are still structural CPU/GPU boundaries: `pi_dec.split` active-summary join, `pi_ccs.sumcheck.fe.row_download`, `pi_dec.emit.download`, and host-side claim/proof assembly shells.
- Verdict: use `e2e_gpu_fast_bench` for GPU-resident prover timing decisions and keep `e2e_bench` as the full terminal-witness parity guard. Next accepted work must reduce a real fast-contract boundary, not merely move terminal witness export between benchmark windows.

## 2026-07-06T14:24:02Z — DEC split/forms stream overlap rejected and restored

- Hypothesis: `pi_dec.split` and `open_children.forms` both depend on the parent claim/witness but not on each other. Forking split to its own stream, issuing the two-word active-summary D2H asynchronously into a pinned buffer, building forms on the main stream, then joining before child openings should expose valid DAG parallelism without widening the child set. Expected value: reduce the split wall if the forms kernels could cover the active-summary wait.
- DAG result: the schedule was valid and byte-identical, but it did not collect time. The `29ms` split wait moved from `fold.superneo.pi_dec.split` to `fold.superneo.pi_dec.split.join`; online median stayed flat. The wait is dominated by earlier stream backlog / host join semantics, not by useful split arithmetic that forms can hide. This is not a profitable execution grain.
- Rejected diff summary: temporarily added a two-word pinned split-summary buffer, `PendingSplit`, forked-stream `begin_split_parent`, async `copy_to_pinned_host_async`, and split `enqueue`/`join` timer leaves. The attempt was reverted; source search confirms `PendingSplit`, `begin_split_parent`, `split.enqueue`, `split.join`, and `split_summary_host` are gone.
- Gates before rejected measurement: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s compute-sanitizer --tool memcheck ./target/release/parity dec` (`ERROR SUMMARY: 0 errors`).
- Rejected measurement: `benchmark-results/gpuprof/20260706T141930Z-dec-split-forms-overlap-fast.json`; CPU median `2151.6ms`, CUDA median `428.2ms`, speedup `5.03x`, proof byte-identical, residency clean. Strict check against `benchmark-results/gpuprof/20260706T141150Z-fast-contract-after-dec-restore.json` passed with `0` regressions, but there was no improvement beyond noise. `gpuprof diff` showed the new `split.join` leaf at `+28.8ms`; `gpuscope analyze /tmp/gpuprof-_pqa_uem --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T141930Z-dec-split-forms-overlap-fast.json` reconciled cleanly and still ranked `fold.superneo.pi_dec.emit.download` as the top exclusive lever.
- Restore checks: after reverting, `cargo fmt --all`, `timeout 300s cargo check --workspace --release`, `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`, and `timeout 300s ./target/release/parity quick` passed.
- Restore measurement: `benchmark-results/gpuprof/20260706T142402Z-dec-split-forms-overlap-reverted-fast.json`; CPU median `2130.6ms`, CUDA median `429.7ms`, speedup `4.95x`, proof byte-identical, residency clean. Strict check against `benchmark-results/gpuprof/20260706T141150Z-fast-contract-after-dec-restore.json` passed with `24` comparisons and `0` regressions. `gpuscope analyze /tmp/gpuprof-1epxqsl6 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T142402Z-dec-split-forms-overlap-reverted-fast.json` reconciled cleanly and again ranked `fold.superneo.pi_dec.emit.download` as the top exclusive lever.
- Verdict: rejected and restored. Park split/forms overlap as a standalone split fix. Along with dense all-child, masked all-k, and split-local workspace attempts, this proves the split leaf is not worth more local retiming. Future DEC work must change the device-backed running/proof-output consumer boundary or preserve compact-active execution fully on device without a host active-count join.

## 2026-07-06T14:31:31Z — DEC proof-output boundary audit

- Trigger: the stage-by-stage GPU ownership goal now requires checking whether `fold.superneo.pi_dec.emit.download` is arithmetic work, a copy-retiming problem, or a lifecycle proof boundary. The latest restored fast run is `benchmark-results/gpuprof/20260706T142402Z-dec-split-forms-overlap-reverted-fast.json`; `gpuscope analyze /tmp/gpuprof-1epxqsl6 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T142402Z-dec-split-forms-overlap-reverted-fast.json` reconciled cleanly and ranked `fold.superneo.pi_dec.emit.download` as the top exclusive lever (`recoverable_ms=21.638`).
- Dependency DAG: `Pi_RLC z_mix on device -> Pi_DEC split planes on device -> child y_ring/y_zcol/commitments on device -> host CeClaim materialization -> host RunningInstance + NifsProof.pi_dec.children -> audit/recursive proof object`.
- Source audit: `DeviceDec::prove` already returns resident split planes and child commitment words, but it also downloads child commitments/y surfaces to assemble ordinary `Children` and `Proof`. The CUDA adapter then uses those host child claims for deferred recomposition checks, cached commitment identity, returned `RunningInstance`, returned `NifsProof`, and post-fold summaries. `NifsProverAdapter::prove` still returns `(RunningInstance, NifsProof)`, so a local DEC helper cannot honestly remove this materialization boundary.
- Compiler note: R1CS F' can skip native recursive NIFS verification with `requires_recursive_compile_reverify = false` and can consume the CUDA-provided `FPrimeFoldPostSummary`; that means recursive compile does not require a full host replay online. The concrete audit/proof object still does: `StepProof` contains `NifsProof`, and `NifsProof.pi_dec.children` is a `Vec<CeClaim>`.
- Verdict: do not retry local `pi_dec.emit.download` pinned-copy, stream, split, or workspace retiming. The next valid implementation is a fold-output ownership seam: `Pi_DEC` produces a device-backed fold-output handle plus light host shell/post summary for recursive compile, and ordinary `CeClaim`/`NifsProof` materialization happens only at final/parity egress. `crates/neo-prover-cuda/PLAN-full-gpu.md` now records this exact DAG.

## 2026-07-06T14:38:42Z — Fast-path DEC recomposition validation moved out of online mode

- Hypothesis: the fast GPU contract should not run prover-side CPU recomposition validation inside the timed CUDA fold. Full audit/parity paths should still validate, but `e2e_gpu_fast_bench` should treat those checks as debug/parity work, not online protocol authority. Expected value: structural cleanup only; no performance ratchet expected.
- DAG result: this does not remove the real `Pi_DEC` proof-output materialization boundary. It only removes the fast-mode CPU validation leaves after DEC child surfaces are already materialized. The remaining hard edge is still `Pi_DEC device child surfaces -> host CeClaim/NifsProof materialization`.
- Diff summary: `CudaNifsProver::prove` now sets `run_online_dec_checks = !self.terminal_claims_only` and wraps the deferred `verify_y_recomposition`, `verify_x_recomposition`, and `verify_commitment_recomposition` calls with that guard. Non-fast/full-audit paths still run the checks.
- Gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`; `timeout 300s ./target/release/parity e2e_bench`.
- Measurement: `benchmark-results/gpuprof/20260706T143842Z-dec-fast-no-online-recompose.json`; CPU median `2132.2ms`, CUDA median `431.1ms`, speedup `4.97x`, claims-only proof/audit byte-identical, residency clean. Repeat values were `[432.0, 431.1, 426.0]`.
- Baseline check: `python3 scripts/gpuprof/gpuprof.py check benchmark-results/gpuprof/20260706T142402Z-dec-split-forms-overlap-reverted-fast.json benchmark-results/gpuprof/20260706T143842Z-dec-fast-no-online-recompose.json` passed (`429.7ms -> 431.1ms`, `24` comparisons, `0` regressions). This is within noise but not an improvement.
- Gpuscope: `python3 scripts/gpuscope/cli.py analyze /tmp/gpuprof-njn8k2_6 --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T143842Z-dec-fast-no-online-recompose.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `idle_overlap_clamp_ms=0`, `stage_mismatches=0`) and still ranked `fold.superneo.pi_dec.emit.download` as the top exclusive lever (`recoverable_ms=21.609`).
- Verdict: accepted as structural-only fast-path cleanup, with no speedup claim and no baseline ratchet. Next aligned work remains the device-backed fold-output/proof-output ownership seam; do not spend the next loop on another 1-20ms local DEC cleanup.

## 2026-07-06T14:49:18Z — DEC post-accumulator handle seam rejected and restored

- Hypothesis: the recursive-compile post-fold summary should be sourced from a compact accumulator handle emitted by `DeviceDec::prove`, not reconstructed from the host `RunningInstance.claims`. This would be a step toward a device-backed fold-output seam where host `CeClaim` materialization is no longer summary authority. Expected value: structural boundary movement only; no speedup claim.
- Rejected diff summary: temporarily added `DecFoldOutput::post_accumulator`, computed `AccumulatorHandle::from_running_parts(&claims, Some(parent))` inside `DeviceDec::prove`, and changed the adapter to cache `FPrimeFoldPostSummary` from that handle plus `parent_authority` instead of from the returned `RunningInstance`.
- Rejection gate: after `cargo fmt --all`, `timeout 300s cargo check --workspace --release`, `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`, `timeout 300s ./target/release/parity quick`, `timeout 300s ./target/release/parity ccs_prove`, and `timeout 300s ./target/release/parity nifs` passed, `timeout 300s ./target/release/parity e2e_bench` failed on `append 2: Pi_CCS FE initial sum mismatch`. That proves the seam was too narrow or placed at the wrong lifecycle boundary.
- Revert diff summary: removed `DecFoldOutput::post_accumulator` and restored `CudaNifsProver::cache_post_fold_summary(&RunningInstance)` so the post summary is again derived from the same host-visible running accumulator as the rest of the lifecycle path.
- Restore gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity e2e_bench` passed (`online prove cpu=2140.48ms gpu=459.09ms`, full terminal-witness contract byte-identical).
- Restore measurement: `benchmark-results/gpuprof/20260706T144854Z-post-accumulator-handle-reverted-fast.json`; CPU median `2141.8ms`, CUDA median `428.5ms`, speedup `5.00x`, claims-only proof/audit byte-identical, residency clean. Repeat values were `[432.0, 425.6, 428.5]`.
- Restore checks: strict check against `benchmark-results/gpuprof/20260706T143842Z-dec-fast-no-online-recompose.json` passed (`431.1ms -> 428.5ms`, `24` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-q6l98sfj --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T144854Z-post-accumulator-handle-reverted-fast.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `idle_overlap_clamp_ms=0`, `stage_mismatches=0`) and ranked `fold.superneo.pi_dec.emit.download` as the top exclusive lever (`recoverable_ms=21.648`).
- Verdict: rejected and restored. Do not retry "DEC emits only a post-accumulator handle" as an isolated local seam. A valid fold-output ownership change must widen the lifecycle/proof-output boundary so the returned `RunningInstance`, `NifsProof`, F' summary, and downstream Pi_CCS transcript authority all agree on the same device-backed fold-output object.

## 2026-07-06T14:55:00Z — Pi_CCS oracle + FE DAG audit

- Trigger: the active GPU-ownership goal says to audit `fold.superneo.pi_ccs.oracle` and `fold.superneo.pi_ccs.sumcheck.fe` from fresh profiler evidence before attempting more CUDA edits. The source of truth is `benchmark-results/gpuprof/20260706T144854Z-post-accumulator-handle-reverted-fast.json` plus `gpuscope analyze /tmp/gpuprof-q6l98sfj`.
- Dependency DAG: `bind/challenge -> oracle row/eval tables -> FE row FS chain -> FE row summary/log download -> y_prime/Ajtai tail surface -> NC prolog/sumcheck -> Pi_CCS output claims`. The FE row transcript chain is serial by Fiat-Shamir, and the current host proof assembly still needs row challenges/log slices before it can complete the existing `PiCcsDeferredProof` object.
- Profiler evidence: exclusive `pi_ccs.oracle` is `28.84ms` wall / `6.48ms` GPU busy / `0.14MB` H2D / `0MB` D2H / `9` syncs / `108` launches. Its leaf levers are small: `oracle.Eval recoverable_ms=2.50`, `oracle.Q recoverable_ms=1.63`, `oracle.F recoverable_ms=0.54`.
- FE evidence: exclusive `pi_ccs.sumcheck.fe` is `30.68ms` wall / `26.36ms` GPU busy / `0.01MB` D2H / `17` syncs / `384` launches. The visible `row_download` leaf is `25.48ms` wall, but its floor is `25.08ms`; it is mostly the join for already-enqueued device FE work, not a removable copy. `row_enqueue_loop` is only `0.79ms` host window.
- NCU evidence: `fe_round_partials` sampled at `196` blocks x `64` threads, `128` registers/thread, `0.19` waves/SM, `33.33%` theoretical occupancy, `6.32%` achieved occupancy, `3.99%` memory throughput, `2.84%` DRAM throughput. `p2_transcript_absorb_device_challenge` sampled at `1` block x `256` threads, `62` registers/thread, `0.00` waves/SM, `66.67%` theoretical occupancy, `2.13%` achieved occupancy, `0.07%` memory throughput. Both gates stayed byte-identical under NCU replay.
- Verdict: park local Pi_CCS oracle/FE retiming for now. The remaining FE cost is either the real serial transcript chain or a proof-output consumer boundary; standalone row-log download deferral, FE row proof-log export, and cooperative row fusion have already been rejected. The next valid Pi_CCS move must widen the whole-phase/whole-fold consumer boundary or overlap independent work outside the FE Fiat-Shamir chain; do not spend the next iteration on another local FE row kernel or download shuffle.

## 2026-07-06T15:00:00Z — Pi_DEC split/commit audit

- Trigger: after parking Pi_CCS oracle/FE local retiming, the stage-order loop moves to `fold.superneo.pi_dec.split` and adjacent child scheduling. The current fast profile reports `pi_dec.split` as `29.47ms` wall and `pi_dec.commit_children` as `8.13ms` wall.
- Split DAG: `z_mix device words -> dec_split writes k digit planes + activity flags -> dec_build_active_index writes active indices/count/error -> host reads 2-word active summary -> compact child openings/commitments use active_count`. The host join is currently required because downstream compact scheduling sizes active buffers and launches from host `active_count`.
- NCU evidence: `timeout 300s ncu --set basic --target-processes all --kernel-name regex:dec_split --launch-count 1 --force-overwrite -o benchmark-results/ncu-dec-split-current-20260706T1500 ./target/release/parity dec` passed and kept DEC parity. The sampled `dec_split` kernel was `7.10us`, `79` blocks x `256` threads, `26` registers/thread, `0.10` waves/SM, `100%` theoretical occupancy, `16.37%` achieved occupancy, `33.66%` memory throughput, `2.47%` DRAM throughput.
- Interpretation: the split kernel is underfilled, but it is not the 29ms wall by itself. The expensive stage is the compact-schedule active-summary join / stream ordering boundary. Earlier split-local attempts already proved that moving this join to another stream or widening to dense all-child scheduling does not improve the fast median.
- Commit audit: the apparent `commit_children` API cost is graph/lifetime churn around active child commitments, not a duplicate allocation bug in the current file. `commit_active_planes` has a single output allocation in the current tree.
- Verdict: park local `dec_split` kernel tuning and split active-summary retiming. A valid DEC improvement must either (1) keep compact-active scheduling fully device-driven without a host active-count join, or (2) widen the fold/proof-output consumer boundary so host child-claim/proof materialization is not required in the timed fast path. Do not retry dense all-child scheduling, split/forms overlap, split workspace, or local pinned-copy retiming as standalone fixes.

## 2026-07-06T15:15:16Z — FE row proof-log egress deferral retried and reverted

- Trigger: active Pi_CCS ownership work resumed after context compaction with a partially applied default-path FE row proof-log deferral. The goal requires current evidence over intent, so the attempt was rebuilt and measured before accepting or reverting it.
- Hypothesis: use `DeferredProof` plus a small FE-row proof-log exporter so Pi_CCS terminal outputs can feed Pi_RLC/Pi_DEC while row proof coefficients stay in the retained `FePhaseWorkspace` until final proof egress. Expected structural value: remove the mid-Pi_CCS `row_download` leaf.
- Rejected diff summary: the temporary state added a `DeviceFeRowProofLogExporter`, a `PiCcsProofState::DeferredFeRows` adapter state, and routed the default row-trace path through `defer_from_parts_with_device_backends_and_transcript_mode`. The attempt was then reverted.
- Rejected measurement: `benchmark-results/gpuprof/20260706T151035Z-row-proof-log-egress-fast.json`; CPU median `2138.4ms`, CUDA median `437.7ms`, speedup `4.88x`, claims-only proof/audit byte-identical, residency clean. Strict check against `benchmark-results/gpuprof/20260706T144854Z-post-accumulator-handle-reverted-fast.json` passed by tolerance (`428.5ms -> 437.7ms`, `23` comparisons, `0` hard regressions), but the median regressed by `+9.2ms`.
- Finding: the boundary moved but did not become GPU-owned. `row_download` disappeared under Pi_CCS, but `fold.egress.export` grew to about `13.9ms`, D2H copies doubled from `4` to `8`, and the fast online wall regressed. This reproduces the earlier negative results: FE row proof-log deferral is not a standalone improvement unless a downstream GPU proof consumer removes the host export from the timed path.
- Restore gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Restore measurement: `benchmark-results/gpuprof/20260706T151452Z-row-proof-log-egress-reverted-fast.json`; CPU median `2129.2ms`, CUDA median `427.1ms`, speedup `4.99x`, claims-only proof/audit byte-identical, residency clean. Repeat values were `[425.4, 427.1, 429.4]`.
- Restore checks: strict check against `benchmark-results/gpuprof/20260706T144854Z-post-accumulator-handle-reverted-fast.json` passed clean (`428.5ms -> 427.1ms`, `24` comparisons, `0` regressions). `gpuscope analyze /tmp/gpuprof-wqgx_ns_ --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T151452Z-row-proof-log-egress-reverted-fast.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `idle_overlap_clamp_ms=0`, `stage_mismatches=0`) and ranked `fold.superneo.pi_dec.emit.download` as the top exclusive lever (`recoverable_ms=21.789`).
- Verdict: rejected and restored. Do not retry FE row proof-log deferral, pinned FE row download, or row-log export relocation as standalone Pi_CCS work. The next aligned move is a real proof-output/fold-output ownership seam or a dependency-correct whole-fold command stream where proof material is consumed on device instead of exported later inside the same timed window.

## 2026-07-06T15:23:49Z — Proof-carrier seam audit for device-backed fold output

- Trigger: the restored fast profile still ranks `fold.superneo.pi_dec.emit.download` as the top exclusive lever, while repeated DEC-local retiming attempts only move the same wait. The active goal requires a structural GPU ownership move rather than another copy/stream tweak.
- Source audit: `NifsProverAdapter::prove` returns `(RunningInstance, NifsProof)`. `construction2::StepProof` stores `FoldProof::Recursive(NifsProof)`, and `pi_dec::Proof` is `children: Vec<CeClaim>`. `lifecycle::UncompressedAudit.steps` and `decider::Witness.steps` clone those ordinary proof objects. The fast e2e gate still compares recursive fold `NifsProof`s after every append; terminal claims-only mode excludes only private terminal witnesses, not per-fold Pi_DEC child claims.
- Dependency DAG: `Pi_DEC device child surfaces -> host CeClaim materialization -> RunningInstance + NifsProof -> StepProof -> audit/decider`. The first node is already CUDA-owned; the materialization node is forced by the proof carrier, not by DEC arithmetic.
- Conclusion: an adapter-local change cannot remove this boundary honestly. A valid implementation must widen the proof carrier: `Pi_DEC device child surfaces -> DeviceFoldOutputHandle -> RunningInstance shell + FPrimeFoldPostSummary for online compile -> deferred StepProof/NifsProof materializer for parity/audit/decider`.
- Guardrail added: `crates/neo-prover-cuda/PLAN-full-gpu.md` now records this proof-carrier constraint and explicitly parks adapter-local variants (`DeviceDec` post-accumulator-only, child-download skip while returning ordinary proof objects, or post-summary authority diverging from stored proof carrier). The known failure mode is the 2026-07-06 `append 2: Pi_CCS FE initial sum mismatch` rejection.
- Measurement: `benchmark-results/gpuprof/20260706T152412Z-proof-carrier-audit-fast.json`; CPU median `2135.3ms`, CUDA median `429.0ms`, speedup `4.98x`, claims-only proof/audit byte-identical, residency clean. Repeat values were `[429.2, 426.5, 429.0]`.
- Baseline check: `python3 scripts/gpuprof/gpuprof.py check benchmark-results/gpuprof/20260706T151452Z-row-proof-log-egress-reverted-fast.json benchmark-results/gpuprof/20260706T152412Z-proof-carrier-audit-fast.json` passed (`427.1ms -> 429.0ms`, `24` comparisons, `0` regressions).
- Gpuscope: `python3 scripts/gpuscope/cli.py analyze /tmp/gpuprof-w3yfc4kb --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T152412Z-proof-carrier-audit-fast.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `idle_overlap_clamp_ms=0`, `stage_mismatches=0`) and ranked `fold.superneo.pi_dec.emit.download` as the top exclusive lever (`recoverable_ms=21.641`, `fix_class=graph_capture`).
- Verdict: no code-path change accepted in this audit. Next implementation must modify the lifecycle/proof carrier or choose a different structurally independent GPU-scheduling stage; do not spend the next loop on DEC-local materialization retiming.

## 2026-07-06T15:33:31Z — NIFS prover output carrier seam

- Hypothesis: the adapter tuple return is the wrong ownership boundary for a device-backed fold output. Before a CUDA path can defer Pi_DEC child-claim materialization, the protocol layer needs a named carrier where materialized and future deferred outputs can live. Expected value: structural seam only, no speedup claim.
- Diff summary: added `NifsProofCarrier::Materialized(NifsProof)` and `NifsProverOutput` in `paper::nifs::backend`, changed `NifsProverAdapter::prove` to return that carrier, kept `prove_with_adapter` returning the ordinary `(RunningInstance, NifsProof)` tuple by calling `into_materialized_parts()`, added an internal output-returning adapter helper, moved the F' adapter branch to consume that carrier before materializing, and updated the CPU/CUDA adapter implementations plus the direct NIFS parity harness call site.
- DAG effect: no CUDA timing edge changed yet. The important boundary moved from an anonymous tuple in the trait to a protocol-owned proof carrier visible at the F' lifecycle boundary, which is the required place for a later `DeviceFoldOutputHandle -> deferred NifsProof materializer` design.
- Gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Verdict: accepted as structural-only groundwork. No gpuprof run and no baseline ratchet, because the materialized proof path is intentionally behavior-equivalent. Next aligned implementation is widening `NifsProverOutput` into a materialized/deferred fold-output carrier, not another DEC-local copy or stream tweak.

## 2026-07-06T15:50:00Z — NIFS deferred materializer contract

- Hypothesis: the previous carrier seam was still only a named materialized tuple. To let a future CUDA fold-output handle own resident Pi_DEC child surfaces until proof egress, the carrier needs a cloneable deferred materialization contract before it can safely move into wider lifecycle proof objects. Expected value: structural seam only, no speedup claim.
- Fresh evidence: `benchmark-results/gpuprof/20260706T154244Z-gpu-stage-baseline.json` reports full-audit CUDA median `455.3ms` / `4.68x`, byte-identical, residency clean. `gpuscope analyze /tmp/gpuprof-wtrnqia3` reconciled with `max_reconciliation_error_ms=0` and ranked `fold.superneo.pi_dec.emit.download` as the top exclusive lever (`recoverable_ms=21.667`). This keeps the active boundary at proof/output materialization rather than local DEC arithmetic.
- Diff summary: added `DeferredNifsProofMaterializer: Send + Sync + 'static` with `materialize(&self) -> Result<NifsProof, Error>`, extended `NifsProofCarrier` with `Deferred(Arc<dyn DeferredNifsProofMaterializer>)`, made carrier materialization fallible, and added `NifsProverOutput::into_parts()` so later lifecycle code can carry the proof payload without immediately exporting ordinary proof bytes. Current CPU/CUDA paths still return `NifsProverOutput::materialized`, and current public wrappers still materialize before returning `(RunningInstance, NifsProof)`.
- DAG effect: no CUDA timing edge changed yet. The protocol-owned carrier can now represent `Pi_DEC device child surfaces -> deferred NifsProof materializer -> parity/audit/decider egress`, but no device handle is stored there yet. This is intentionally not a performance ratchet.
- Gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Verdict: accepted as structural-only groundwork. No post-change gpuprof run and no baseline ratchet because the materialized path is behavior-equivalent. Next aligned implementation is widening the lifecycle proof carrier (`FoldProof` / `StepProof` / audit egress) or making the CUDA adapter return a real deferred carrier, not DEC-local copy/stream retiming.

## 2026-07-06T15:58:12Z — Lifecycle `StepProof` carries the NIFS proof carrier

- Hypothesis: the deferred materializer contract is useful only if the lifecycle proof object can carry it across append/audit boundaries. If `FoldProof::Recursive` stores a concrete `NifsProof`, the CUDA path must still export ordinary Pi_DEC child claims before the step proof is recorded. Expected value: structural seam only, no speedup claim.
- Diff summary: changed `FoldProof::Recursive` from `NifsProof` to `NifsProofCarrier`, added helper constructors/materialization on `FoldProof`, changed the F' adapter branch to keep `NifsProverOutput::into_parts()` instead of immediately materializing, and updated verifier/decider/R1CS-audit/parity/test consumers to materialize at proof-consumption boundaries.
- DAG effect: no CUDA timing edge changed yet. The lifecycle path can now represent `Pi_DEC device child surfaces -> deferred NifsProof materializer -> StepProof -> parity/audit/decider egress`, but current CPU/CUDA paths still store `NifsProofCarrier::Materialized`.
- Gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo check --workspace --release --tests`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Verdict: accepted as structural-only groundwork. No gpuprof run and no baseline ratchet because the materialized path is behavior-equivalent. Next aligned implementation is a real CUDA deferred carrier backed by resident fold-output surfaces, not another DEC-local copy/stream retiming.

## 2026-07-06T16:07:32Z — NIFS post-fold summary carried by prover output

- Hypothesis: the post-fold accumulator digest override should not live in a separate adapter side-channel while the proof payload is moving into `NifsProverOutput` / `NifsProofCarrier`. Carrying the digest and F' summary with the NIFS output keeps the returned `RunningInstance`, stored proof carrier, and state-advance authority on one ownership boundary. Expected value: structural seam only, no speedup claim.
- Diff summary: added `NifsPostFoldSummary` to `paper::nifs::backend`, extended `NifsProverOutput` with `with_post_fold_summary`, `into_parts_with_summary`, and `into_materialized_parts_with_summary`, removed `take_post_acc_digest_override` from `NifsProverAdapter`, and changed the F' native and final-fold adapter paths to consume the post-fold digest from the output. `CudaNifsProver` now attaches the summary to `NifsProverOutput::materialized(...)` instead of caching a separate post-accumulator digest hook.
- DAG effect: no CUDA timing edge changed yet. This removes one more adapter-local authority path before the real device-backed fold-output carrier lands. The R1CS F' compiler still uses the existing `take_f_prime_post_summary` side-channel, so a later cleanup must route that compiler path through the output-owned summary or an equivalent proof-output-owned seam.
- Gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo check --workspace --release --tests`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Verdict: accepted as structural-only groundwork. No gpuprof run and no baseline ratchet because the materialized path is behavior-equivalent. Next aligned implementation is still a real CUDA deferred carrier backed by resident fold-output surfaces; do not fall back to DEC-local copy/stream retiming.

## 2026-07-06T16:13:13Z — R1CS F' summary hook removed

- Hypothesis: after `NifsPostFoldSummary` moved into `NifsProverOutput`, the remaining `take_f_prime_post_summary` adapter hook was stale ownership. The R1CS F' builder should receive the same summary through the lifecycle call that produced the fold, not by asking the adapter for side state after the fact. Expected value: structural seam only, no speedup claim.
- Diff summary: added internal adapter-output variants for F' native step and lifecycle in-place extend, returning `Option<NifsPostFoldSummary>` while keeping existing public `(State, StepProof)` / `UncompressedAudit` APIs stable. `R1csChainBuilder::prepare_next_fold` now consumes `summary.f_prime()` from that lifecycle result. Removed `NifsProverAdapter::take_f_prime_post_summary`, the CUDA adapter cache field, and the now-unused in-place compatibility wrappers.
- DAG effect: no CUDA timing edge changed yet. This closes the post-fold summary side channel completely: F' native, finalization, and R1CS recursive compile now all source post-fold summary authority from the same NIFS output object that carries the proof carrier.
- Gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo check --workspace --release --tests`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Verdict: accepted as structural-only groundwork. No gpuprof run and no baseline ratchet because the materialized path is behavior-equivalent. Next aligned implementation remains a real CUDA deferred carrier backed by resident fold-output surfaces.

## 2026-07-06T16:23:50Z — NIFS running-output carrier seam

- Hypothesis: a proof-only carrier is still too narrow for the CUDA fold-output boundary. The current blocker is not just delayed `NifsProof` bytes; the next fold, F' state advance, and audit path also need a coherent post-fold `RunningInstance`. A real device-backed fold output therefore needs a running carrier beside the proof carrier. Expected value: structural seam only, no speedup claim.
- Fresh evidence: `benchmark-results/gpuprof/20260706T161714Z-post-summary-carrier-baseline.json` reports full-audit CUDA median `458.0ms` / `4.65x`, byte-identical, residency clean. `gpuscope analyze /tmp/gpuprof-vpx_yqo6` reconciled cleanly (`max_reconciliation_error_ms=0`) and ranked `fold.superneo.pi_dec.emit.download` as the top exclusive lever (`recoverable_ms=21.659`). The Pi_CCS-local FE row-log deferral remains parked because prior attempts moved the export to egress and regressed.
- Diff summary: added `DeferredNifsRunningMaterializer` and `NifsRunningCarrier::{Materialized, Deferred}` in `paper::nifs::backend`, changed `NifsProverOutput` to own `NifsRunningCarrier` plus `NifsProofCarrier`, added carrier-returning accessors, and made running materialization fallible at the few lifecycle call sites that keep proof material deferred. Current CPU/CUDA paths still construct `NifsProverOutput::materialized`, so no timed path changes yet.
- DAG effect: no CUDA timing edge changed yet. The protocol layer can now represent `Pi_DEC resident child surfaces -> deferred RunningInstance materializer -> deferred NifsProof materializer -> StepProof/audit/decider egress` as one coherent fold-output boundary instead of deferring proof bytes while forcing host-built running state.
- Gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo check --workspace --release --tests`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Verdict: accepted as structural-only groundwork. No gpuprof ratchet because the materialized path is intentionally behavior-equivalent. Next implementation must make the CUDA adapter return a real deferred running/proof carrier backed by resident Π_DEC child surfaces, or choose a broader whole-fold device consumer; do not retry local DEC copy retiming or FE row-log relocation.

## 2026-07-06T16:37:29Z — F' running-carrier materialization boundary named

- Hypothesis: the new `NifsRunningCarrier` seam is still easy to lose because the F' adapter branch called `NifsProverOutput::into_parts_with_summary()`, hiding the forced conversion back to a concrete `RunningInstance`. Naming that conversion makes the remaining CPU-owned lifecycle edge explicit and gives the next GPU-owned proof-state slice one helper to replace. Expected value: structural clarity only; no speedup claim.
- Fresh evidence: `benchmark-results/gpuprof/20260706T163102Z-gpu-stage-fast-baseline.json` reported claims-only CUDA fast path parity clean and residency clean. `gpuscope analyze /tmp/gpuprof-on2tqgkv --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T163102Z-gpu-stage-fast-baseline.json` reconciled cleanly and ranked the remaining top exclusive lever under the DEC/proof-output boundary. Targeted `ncu --set basic` on `dec_scatter_active_words|dec_pack_public_x` showed microsecond underfilled helper kernels, not a slow arithmetic kernel. The evidence still points at lifecycle materialization, not DEC-local tuning.
- Diff summary: changed the F' adapter branch to call `NifsProverOutput::into_carriers_with_summary()`, keep the `NifsProofCarrier`, and route only the running carrier through `materialize_running_for_proof_state(...)`. The helper documents that `ProofState::Active` still stores concrete `RunningInstance`, which is the next boundary a device-backed running carrier must remove.
- DAG effect: no CUDA timing edge changed. The repeated online path still materializes running state before storing `ProofState::Active`; the proof carrier can stay deferred. This entry prevents the next loop from mistaking carrier existence for GPU-owned lifecycle state.
- Gates: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo check --workspace --release --tests`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Measurement: `benchmark-results/gpuprof/20260706T163954Z-fprime-running-materialization-boundary.json`; CPU median `2138.1ms`, CUDA median `425.6ms`, speedup `5.02x`, claims-only proof/audit byte-identical, residency clean. Repeat values were `[427.2, 425.6, 421.6]`.
- Baseline check: `python3 scripts/gpuprof/gpuprof.py check benchmark-results/gpuprof/20260706T163102Z-gpu-stage-fast-baseline.json benchmark-results/gpuprof/20260706T163954Z-fprime-running-materialization-boundary.json` passed (`423.1ms -> 425.6ms`, `24` comparisons, `0` regressions).
- Gpuscope: `python3 scripts/gpuscope/cli.py analyze /tmp/gpuprof-25szaipn --validate-gpuprof --gpuprof-json benchmark-results/gpuprof/20260706T163954Z-fprime-running-materialization-boundary.json` reconciled cleanly (`max_reconciliation_error_ms=0`, `idle_overlap_clamp_ms=0`, `stage_mismatches=0`) and still ranked `fold.superneo.pi_dec.emit.download` as the top exclusive lever.
- Trend tail: latest fast runs stayed in the same band (`2026-07-06T16:31:25Z 423.1ms`, `2026-07-06T16:40:18Z 425.6ms`).
- Verdict: accepted as structural-only groundwork with no baseline ratchet. Next implementation should widen `ProofState`/lifecycle state so a CUDA fold can carry a deferred running/proof pair backed by resident Π_DEC child surfaces across F' steps.

## 2026-07-06T17:24:48Z — Device Pi_CCS digest for RLC rho bind rejected

- Hypothesis: compute the Π_CCS output digest from resident K surfaces on device, then let Π_RLC bind that device digest and sample rho from the device transcript. Expected value: remove one host-owned digest/bind boundary before RLC while preserving CPU byte parity.
- Diff summary: temporarily added `sample_rhos_from_device_outputs_digest_deferred` in the CUDA RLC path and routed the adapter through `DevicePiCcsOutputsDigest::compute_from_shells(...)` when resident Pi_CCS K surfaces were available. The attempt was reverted after measurement.
- Gates before measurement: `timeout 300s cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Rejected result: `benchmark-results/gpuprof/20260706T171919Z-device-pi-ccs-digest-rlc-bind.json`; CPU median `2196.3ms`, CUDA median `579.4ms`, speedup `3.77x`, proof byte-identical, residency clean. This regressed `+127.9ms` from the fresh full-audit baseline `benchmark-results/gpuprof/20260706T171316Z-gpu-stage-baseline.json`.
- Finding: the boundary moved in the right authority direction but at the wrong execution grain. The candidate introduced underfilled Poseidon hashing (`p2_hash_fields` about `122.8ms` over `8` launches, each `1 block x 256 threads`), so it traded a small host bind for a large serial-ish GPU hash cost. Do not retry naive device Pi_CCS output digest binding as a standalone slice; if revisited, it must be fused/batched with a wider proof-output consumer so it does not add standalone underfilled digest kernels.
- Restore check: after reverting, `benchmark-results/gpuprof/20260706T172425Z-revert-device-pi-ccs-digest-rlc-bind.json` reported CPU median `2188.7ms`, CUDA median `451.9ms`, speedup `4.81x`, proof byte-identical, residency clean. Strict check against `benchmark-results/gpuprof/20260706T171316Z-gpu-stage-baseline.json` passed with `26` comparisons and `0` regressions (`451.5ms -> 451.9ms`, limit `476.0ms`).
- Trend tail: the attempted slice appears as a clean spike and recovery: `451.5ms -> 579.4ms -> 451.9ms`.
- Verdict: rejected and reverted. Accepted full-audit baseline remains `benchmark-results/gpuprof/20260706T171316Z-gpu-stage-baseline.json`. Next aligned work should avoid standalone digest kernels and focus on a wider device-owned fold-output/proof-consumer boundary or dependency-correct Pi_CCS/DEC scheduling.

## 2026-07-06T17:26:45Z — Whole-FE full-audit recheck still parked

- Hypothesis: re-measure the existing whole-FE device transcript path against the fresh full-audit baseline before doing more Pi_CCS work. If the current tree has fixed enough churn, whole-FE should remove the FE row boundary without violating residency.
- Measurement only: `benchmark-results/gpuprof/20260706T172645Z-whole-fe-current-full-audit.json`; CPU median `2183.5ms`, CUDA median `478.1ms`, speedup `4.57x`, proof byte-identical. The run exited nonzero because the residency gate failed: `fold.superneo.pi_ccs.sumcheck d2h_mb 0.36MB > budget 0.25MB`.
- Finding: whole-FE remains structurally interesting but not acceptable. It reduces `pi_ccs.sumcheck.fe.row_download` from about `25.4ms` to `6.4ms`, but moves cost into extra FE device work, `fold.egress.export` (`14.0ms`), more launches (`pi_ccs` `1088 -> 1310`), and a residency violation. The default row-trace full-audit path remains faster (`451.9ms`) and budget-clean.
- NCU evidence: targeted `ncu --set basic --target-processes all --kernel-name regex:fe_round_partials ./target/release/parity e2e_bench` is now usable on this machine. It confirmed `fe_round_partials` launches as `1 block x 256 threads` on a `128` SM GPU, uses `128` registers/thread, has theoretical occupancy about `33%`, and achieved occupancy around `2.2%`. This is a grid-underfill/round-shape problem, not a place for blind scalar micro-tuning.
- Verdict: parked. Do not promote whole-FE until its proof-log/residency churn is eliminated or it is paired with a wider proof-output consumer. Do not spend the next slice on FE kernel internals unless it changes the round scheduling shape; the single-block FE kernels are dependency/launch-shape limited.

## 2026-07-06T18:xxZ — Shared-context multichain throughput gate

- Hypothesis: a single SuperNeo chain has a serial Fiat-Shamir spine with many underfilled kernels, so legal parallelism must also include independent chains/proofs. Measure two independent SHA lifecycle chains concurrently before designing a larger scheduler. Expected value: structural throughput signal, not a single-chain latency win.
- Diff summary: added `e2e_multichain_bench`, refactored the SHA e2e fixture builder for reuse, added `Device::open_context` / `Device::from_context`, `DeviceSession::new_on_context`, and `CudaNifsProver::new_on_context`. The multichain gate uses one shared CUDA context with separate streams and separate prover sessions; each chain remains full-audit byte-identical against the CPU reference.
- Gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity e2e_multichain_bench`.
- Result: direct gate run reported CPU aggregate `4400.8ms`, sequential CUDA `996.1ms` (`4.42x`), parallel CUDA `811.7ms` (`5.42x`), overlap `1.23x`, proof byte-identical.
- Gpuprof: `benchmark-results/gpuprof/20260706T-multichain-shared-context.json` reported CPU aggregate `4363.9ms`, sequential CUDA `986.9ms` (`4.42x`), parallel CUDA `812.4ms` (`5.37x`), overlap `1.21x`, proof byte-identical.
- Finding: shared context removes context duplication (`session.device` is no longer the issue), but concurrency amplifies the same protocol-boundary problems: `Pi_CCS.sumcheck.fe.row_download`, `Pi_DEC.emit.*`, `Pi_RLC.combine_claims`, and thousands of tiny underfilled launches. The multichain probe also violates the current sumcheck D2H residency budget (`0.40MB > 0.25MB`) because two chains expose the still-present proof-log boundary.
- Verdict: accepted as a structural measurement/architecture seam. It proves independent-chain scheduling is a real lever (`~1.2x` overlap for two chains), but it is not enough for the 20x goal until the fold-output/proof boundary is more device-owned. Next aligned work remains removing the Pi_CCS proof-log/output boundary or carrying a device-backed fold-output through F' before scaling chain concurrency.

## 2026-07-06T21:03:21Z — Standalone FE row proof-log deferral remeasured and rejected

- Hypothesis: retrying FE row proof-log deferral with an async pinned D2H exporter might remove `Pi_CCS sumcheck.fe.row_download` from the critical path without waiting for a wider proof carrier. Expected value: structural boundary reduction, but only acceptable if the online median did not regress.
- Diff summary: temporarily added a narrow `FeRowProofLogExporter` trait, a `DeviceFeRowProofLogExporter`, and a `PiCcsProofState::DeferredRows` adapter path. The default row-trace path used `defer_from_parts_with_device_backends_and_transcript_mode`, started the FE coefficient-log D2H on a forked stream, and materialized the proof at `fold.egress.export`.
- Rejected measurement: `benchmark-results/gpuprof/20260706T-deferred-fe-row-proof-export-async-repeat3.json`; CPU median `2182.4ms`, CUDA median `465.0ms`, speedup `4.69x`, proof byte-identical, residency clean. `Pi_CCS sumcheck.fe.row_download` disappeared and `fold.superneo.pi_ccs.sumcheck` D2H stayed clean, but the same work moved into `fold.egress.export` (`~14ms`) and the online median regressed relative to the accepted `~450ms` band.
- Restore action: removed the exporter trait, CUDA row-log exporter, and `DeferredRows` adapter state; restored the default path to `prove_from_parts_with_backends_and_transcript_mode`.
- Restore gates: `cargo fmt --all`; `timeout 300s cargo check --workspace --release`; `timeout 300s cargo +nightly-2026-04-03 oxide build --features cuda,perf-timers`; `timeout 300s ./target/release/parity quick`; `timeout 300s ./target/release/parity ccs_prove`; `timeout 300s ./target/release/parity nifs`.
- Restore measurement: `benchmark-results/gpuprof/20260706T-revert-standalone-fe-row-export-repeat3.json`; CPU median `2195.5ms`, CUDA median `453.6ms`, speedup `4.84x`, proof byte-identical, residency clean. Repeat values were `[456.9, 453.6, 450.6]`.
- Verdict: rejected and restored. Do not retry FE row proof-log relocation as a standalone slice. The valid next move is a device-backed fold-output/proof carrier that consumes resident Pi_CCS logs and Pi_DEC child surfaces without requiring per-fold proof materialization.
