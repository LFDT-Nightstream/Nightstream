# SuperNeo packed-row helpers live here.
#
# v1 plan:
# - row decode helpers over Rust-provided flattened tables
# - `superneo_bar_block` parity with the Rust implementation


fn scaffold_ready() -> Bool:
    return True
