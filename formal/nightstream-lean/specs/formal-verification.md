# Nightstream Lean formal-verification specification

- Status: normative active specification
- Version: 0.5
- Date: 2026-07-14

This file governs the structure, claims, evidence, and completion criteria of
the active formalization in `formal/nightstream-lean`. The README is only an
overview. Per-property specs may add detail, but they may not weaken this file.

## 1. Objective

The project exists to establish that the production verifier accepts only valid
Nightstream executions, except with probability bounded by explicit
cryptographic failure events.

Lean code is useful only when it contributes to this assurance chain:

```text
paper relation
    ↓ faithful formal statement
Lean protocol semantics
    ↓ proved preservation and soundness
generated R1CS semantics
    ↓ checked circuit correspondence
Rust prover/verifier behavior
    ↓ state, encoding, transition, and failure-path refinement
terminal acceptance
    ↓ reduction to validity or named bad event
valid recursive execution
```

The project is not measured by theorem count, lines of Lean, or absence of
`sorry`. It is measured by closed assurance properties with explicit source
mapping and conformance evidence.

## 2. Governing principles

1. Authority comes from checked witnesses, relations, transcripts, and
   verifier-owned configuration. A digest is only compression.
2. Claim truth and verifier acceptance are separate predicates.
3. One independently defined semantic relation is refined by the native model,
   circuit, and verifier; it is not defined by any of them.
4. Completeness, deterministic correctness, and cryptographic soundness are
   separate theorem families.
5. A Lean-model theorem is not a Rust-refinement theorem.
6. Every assumption names an irreducible mathematical or cryptographic
   boundary. Assumptions may not contain local protocol conclusions.
7. Deprecated modules are evidence to mine, never dependencies to import.
8. The current implementation is the concrete `pc = 1` direct-F' specialization.
   General multi-program NIVC is deferred until the concrete path is assured.
9. No existing Rust verifier, R1CS relation, row count, or decoded-checklist
   theorem is a semantic oracle. Reproducing implementation behavior in Lean
   establishes correspondence only; it does not establish that the behavior is
   sufficient, necessary, or faithful to the paper relation.
10. Constraint retention and removal require independent semantic authority:
    first prove a reduced obligation set sound and complete for the paper-level
    F'/NIFS transition, then prove concrete Rust/R1CS refinement. Necessity is a
    separate per-obligation claim, witnessed by a counterexample when a check is
    removed or by a proof that the check follows from the remaining obligations.

## 3. Sources of truth

The following sources must be reconciled rather than silently prioritized:

| Source | Location | Owns |
|---|---|---|
| SuperNeo paper | `docs/superneo-paper/07-7-neo-s-folding-scheme-for-ccs.md:3-147` | CCS, CE, PiCCS, PiRLC, PiDEC |
| HyperNova multi-folding | `docs/hypernova-paper/08_3_Multi_folding_schemes.md:3-30` | Abstract multi-fold interface and soundness shape |
| HyperNova compatibility | `docs/hypernova-paper/13_6_2_NIVC_Compatible_multi_folding_schemes.md:3-20` | Encoder and default-instance obligations |
| HyperNova Construction 2 | `docs/hypernova-paper/14_6_3_A_compiler_from_NIVC_compatible_folding_schemes_to_NIVC.md:3-65` | F' state, recursive link, compiler, verifier |
| Rust carrier state | `crates/neo-fold-clean/src/paper/construction2/state.rs` (`State`) | Active state representation |
| Rust transition | `crates/neo-fold-clean/src/paper/construction2/transition.rs` (`state_base_case_check`, `advance_state`) | Branch checks, state advance, `x_out` |
| Rust native F' | `crates/neo-fold-clean/src/paper/f_prime/native.rs` (`prove_with_semantic_state`, `verify`) | Prover/verifier control flow and failures |
| Rust recursive circuit | `crates/neo-fold-clean/src/paper/f_prime/r1cs.rs` (`enforce_f_prime_recursive_step_circuit`) | Enforced recursive-step constraints |

If the paper, Lean model, circuit, and Rust disagree, the work is classified as
one of: `code-first`, `model-first`, `harness-first`, or `theorem-first`. The
disagreement must be resolved or recorded as an explicit implementation
specialization. It may not be hidden by a bridge assumption.

## 4. Threat and authority model

The adversary controls:

- proof bytes and prover messages;
- claimed fresh CCS instances and witnesses;
- serialized public inputs not fixed by verifier configuration;
- ordering, duplication, omission, and replay of instances;
- self-consistent mutation followed by recomputation of non-authoritative
  digests;
- malformed base/active tags, counters, encodings, and field elements.

The verifier owns:

- verifier keys and protocol parameters;
- the CCS/R1CS structure and semantic-state mode;
- transcript domain separators and challenge derivation;
- canonical decoding rules;
- the terminal statement and expected public inputs.

The following are never authority by themselves:

- a digest or accumulator handle;
- a prover-supplied mode, flag, counter, or structure description;
- a self-consistent chain of hashes;
- a record field named `sound`, `valid`, `accepted`, or `authorized`.

Denial of service, timing leakage, allocator safety, and general Rust memory
safety are outside the initial Lean scope unless they change protocol logic.

## 5. End-to-end claim

The primary deterministic reduction target is represented by
`Nightstream.Assurance.VerifierReductionTarget`:

```lean
verify statement proof = true →
  ValidExecution step terminalValid initial final steps ∨
  BadEvent statement proof
```

`ValidExecution` must eventually mean all of the following:

- the trace starts from the advertised `z0` and initial semantic state;
- every application transition is valid;
- every F' base or recursive transition is valid;
- every fresh instance satisfies the concrete CCS relation;
- every running instance is exactly the output of the checked fold verifier;
- the recursive instance binds the prior `x_out` preimage;
- all counters, modes, structures, and program counters are verifier-authorized;
- the terminal accumulator satisfies the concrete CE relation;
- the terminal decider checks that CE relation;
- every public digest is recomputed from authoritative preimage data;
- the advertised final public state equals the trace endpoint.

The cryptographic closure theorem must separately bound `BadEvent`:

```text
Pr[BadEvent] ≤
  epsilon_sumcheck +
  epsilon_ajtai_binding +
  epsilon_poseidon2 +
  epsilon_fiat_shamir +
  epsilon_decider
```

No deterministic theorem may replace this probabilistic statement by defining
acceptance to contain claim truth.

## 6. Project structure and ownership

The project remains one Lake package with deep modules and a small root import.

```text
formal/nightstream-lean/
├── Nightstream/
│   ├── SuperNeo/          paper relations and folding reductions
│   ├── HyperNova/         generic multi-fold and Construction-2 semantics
│   ├── Protocol/          concrete SuperNeo + HyperNova specialization
│   ├── Implementation/    Rust, encoding, transcript, and R1CS models
│   └── Assurance/         reachability, terminal, and security theorems
├── tests/                 counterexamples, regressions, axiom checks
├── specs/                 this file and per-property proof contracts
└── assurance/             append-only proof and conformance evidence
```

| Layer | Owns | Must not own |
|---|---|---|
| `SuperNeo` | CCS/CE, SumCheck claim, PiCCS, PiRLC, PiDEC | Recursive state, Rust layout |
| `HyperNova` | Relation interface, multi-fold contract, compatibility, F' compiler semantics | Ajtai or SuperNeo-specific algebra |
| `Protocol` | `pc = 1` Nightstream instantiation and cross-paper composition | Rust serialization or R1CS builder internals |
| `Implementation` | Exact Rust-shaped state, encodings, transcripts, native and R1CS semantics | Cryptographic security conclusions |
| `Assurance` | Trace validity, terminal closure, reduction games, final theorem | New protocol algorithms |

`SuperNeo` and `HyperNova` are sibling foundations. `Protocol` may import both.
`Implementation` may import the paper model and concrete protocol. `Assurance`
may import all active layers. Imports in the opposite direction are forbidden.
Tests may import any active module. Nothing may import `formal/deprecated`.

An interface module is justified only by at least one real consumer. File paths
and namespaces should name the concept they own; there is no mirrored
`Spec/Impl/Security` hierarchy.

## 7. Required formal properties

Evidence states are defined in Section 8.

| ID | Property | Target | Source surface | State |
|---|---|---|---|---|
| `REL-CCS` | Commitment, public projection, norm, and CCS satisfaction define membership | `SuperNeo.CCS.Holds`, `Concrete.ccsMembership_iff` | SuperNeo Definition 12 | `model-proved` |
| `REL-CE` | Commitment, projection, norm, point-domain shape, and all matrix evaluations define membership | `SuperNeo.CE.Holds`, `Concrete.ceMembership_iff` | SuperNeo Definition 13 | `model-proved` |
| `REL-CONCRETE` | Goldilocks, quadratic/cyclotomic rings, centered norm, MLE, caller-parameterized prefix projection, and Ajtai operations form an executable relation model whose membership predicates expand exactly relative to those definitions. This does not claim that arbitrary `publicWidth` instantiates SuperNeo's production `L_in` | `SuperNeo.Concrete.relationSemantics`, `Concrete.ccsMembership_iff`, `Concrete.ceMembership_iff` | Executable algebra/model definitions | `model-proved` |
| `REL-CONCRETE-PRODUCTION` | The fixed-active production relation is the exact paper-aligned `L_in` profile: one fresh source, fourteen running sources, `n_F,in = d * n_R,in = 54 * 5 = 270`, fresh `257 + 13` projection, and complete 270-coordinate running ownership. The instantiated relation shape is definitionally the independent five-ring relation; concrete CCS satisfaction is preserved, PiRLC remains inside the complete `L_in`, and PiDEC split/recomposition is exact | `ProductionRelation.{relationShape_eq,publicWidth_eq,publicInput_eq_lIn,sourceArity_exact,freshPublicInput_exact,runningAssignment_exact,liftedCcsSatisfied_iff,piDecPublicInput_roundTrip,exactProfile}` together with the generic exact `Concrete.{ccsMembership_iff,ceMembership_iff}` expansions. This is model-level relation instantiation only: Rust decoding, Ajtai key bytes, R1CS, generated rows, and physical ownership remain obligation-11/M4 work | SuperNeo Definitions 12–14 and Sections 7.4–7.5; fixed-active Construction-2 arity | `model-proved` |
| `REL-LIN-RUNNING-CLOSURE` | The fixed-active public carrier is exactly `270 = 54*5` coordinates, owned as 257 external plus 13 completion coordinates by an explicit two-sided equivalence. A fresh input fixes the 13 to zero and decodes fail-closed. That zero tail is **not** closed under the profile's Pi_RLC ring action: the sampler-valid challenge `X` maps external coordinate 256 (block 4, lane 40) to coordinate 257 (block 4, lane 41). A running `L_in` is therefore authoritative on all 270 coordinates, and a 257-coordinate view of a 270-coordinate running carrier admits no total left inverse at the paper NIFS running type, the frozen fixed-one F' input, or the generic Construction 2 input. This does **not** claim the repository currently instantiates that interface at 257; those interfaces are polymorphic in their public-input type and are pinned nowhere, so the no-decoder theorems are conditional and do not by themselves exhibit a live defect. Successor `REL-LIN-RUNNING-PIN` must pin the interface before concrete phase refinement begins. `exactPaperVerifier_soundAndCompleteModulo` is the first concrete discharge of `Obligations.NifsSoundAndCompleteModulo` in this repository; every other occurrence is an assumed hypothesis | `LogicalCarrier.{coordinateEquiv_bijective,decodeFresh_sound,decodeFresh_complete,encodeFresh_injective,decodeFresh_rejects_nonzero_padding,piDecSplit_recompose,shiftChallenge_valid,shift_enters_first_padding,freshImage_not_piRlcClosed,projectExternal_not_injective}`, `FixedActiveCarrierObstruction.{exactPaperVerifier_soundAndCompleteModulo,eraseRunning_not_injective,no_exact_paperNifs_running_decoder,no_exact_fixedOne_fprime_decoder,no_exact_construction2_fprime_decoder}` | SuperNeo Definitions 13-14 and Sections 7.4-7.5; HyperNova Construction 2 | `model-proved` |
| `ARITH-GOLDILOCKS-FIELD` | The concrete Goldilocks base field and its quadratic extension are discharged rather than assumed: `EuclidPrime goldilocksP` (Euclid's lemma for `q = 2^64 - 2^32 + 1`, currently a typed hypothesis on 295 occurrences across 95 files), `SevenNonresidue` (7 a non-residue, defining `K = F[u]/(u^2 - 7) = F_{q^2}` per Appendix B.2, 29 occurrences across 8 files), and the `BaseFieldNoZeroDivisors` premise consumed by the norm-residual theorems. No Mathlib is available, so both certificates are built from Lean core; `q - 1 = 2^32 * 3 * 5 * 17 * 257 * 65537` admits a clean Pratt certificate. This property exists so that concrete-field discharge is owned in one place instead of blocking every consumer: `FOLD-PICCS-TARGET-CONVENTION`, the norm-residual theorems, and all 44 `artifact-checked` `CIR-*` properties currently carry these as typed hypotheses | `EuclidPrime`, `SevenNonresidue`, `BaseFieldNoZeroDivisors` discharge for `Concrete.productionGlobalParams` | SuperNeo Appendix B.2 | `planned` |
| `PARAM-GLOBAL` | Verifier-owned global parameters own `q`, `b`, `k`, `B = b^k`, `K_max`, `T`, the Definition-14 inequality `(K+k)·T·(b−1) < B`, the norm stages (`b` fresh / `B` combined / `q/2` extraction-ambient), and the binding regime (`(2B,C)`-relaxed binding ← `MSIS` at `8TB`, Appendix B) | `SuperNeo.GlobalParams`, `Concrete.productionGlobalParams` | SuperNeo Definition 14, Theorem 2, Appendix B; Rust `Params::production`, `Params::max_fresh_count` | `model-proved` |
| `SUM-CLAIM` | SumCheck truth is the actual `T = sum Q`; acceptance contains only verifier checks, and the executable checker is equivalent to that logical predicate | `SumCheck.Claim.True`, `SumCheck.Accepted`, `SumCheck.check_eq_true_iff_accepted` | PiCCS SumCheck | `model-proved` |
| `SUM-SOUND` | False symbolic SumCheck acceptance reduces to a round where the claimed and semantic functions differ but agree at the sampled challenge. This theorem still uses declared `Round.degree` metadata. The separate finite bridge derives degree from coefficients, but the bad-challenge theorem has not yet been transported through that bridge to root counting | `SumCheck.false_acceptance_implies_bad_challenge` | Symbolic SumCheck chain | `model-proved` |
| `SUM-FINITE-CERT` | A prover round is a raw finite constant-first coefficient list whose canonical shape is checked by acceptance; evaluation is executable by Horner's rule, degree is length-derived, and claimed-chain checking is exact. Semantic ghosts and verifier challenges are separate from the certificate; accepted finite chains project conditionally into the symbolic truth-path model | `SumCheck.Finite.Message`, `SumCheck.Finite.check_eq_true_iff_accepted`, `SumCheck.Finite.accepted_implies_symbolicAccepted_and_truthPath`; rejection witnesses `tests/SumCheckFiniteRejection.{honest_accepted,honest_chain,emptyMessage_rejected,trailingZero_rejected,degreeTwo_accepted_at_cap_two,degreeAboveCap_rejected,brokenInitialClaim_rejected,brokenTerminal_rejected,missingChallenge_rejected,extraChallenge_rejected,honest_lockstep}` cover every `checkChain` rejection branch against a proved-accepting control | Generic finite SumCheck certificate | `model-proved` |
| `SUM-DEGREE-WIDTH` | The paper-family and NIFS-key width is exactly the verifier-computed syntax ceiling, that ceiling is at most Appendix D.4's `max(u, 2b+1, 2)` for frozen `b = 2`, and relaxing equality to `syntaxDegree <= width` admits a concrete invalid bad-challenge experiment | `ConstraintPolynomial.canonicalEqualityGatedDegreeBound_le_degreeBound`, `VerifierInput.sumcheckDegreeBound_le_paperRoundDegreeCeiling_of_b_eq_two`, `AsymptoticPaperStrong.Point.sumcheckWidth_le_paperRoundDegreeCeiling`, `InteractiveCompositionBridge.strongExecutionContext_paperDegreeWidthExact`; necessity witness `SumCheckSoundnessContract.{context_not_paperDegreeWidthExact,not_sumCheckSoundnessContract_at_paper_budget}` | SuperNeo Definition 6 and Appendix D.4 degree premise | `model-proved` |
| `SUM-POLY-ENC` | The raw fixed-width certificate used by the causal paper PiCCS verifier is transported at the exact submitted round into a claimed-versus-semantic polynomial collision; the repaired exact degree-width invariant, finite root counting, recursive successive-coordinate sampling, and an explicit multi-round union bound construct the existing `SumCheckSoundnessContract` at `cubeVariables * sumcheckWidth / alphabet.cardinality` without retaining that contract as a premise | `FiniteRootCounting.{roots_count_le_degree,collisions_count_le_degree}`, `CausalSumCheckBound.{detectsFrom_count_le,probability_detects_le_ratio}`, `SumCheckSoundness.{sumCheckFailure_implies_detects,verifierDetects_probability_le,sumCheckBadChallenge_probability_le,sumCheckSoundnessContract_of_rootCounting,extraction_after_first_success_of_rootCounting}`; exact Boolean transport `SecurityContracts.sumCheckBadChallengeEvent_eq_true_iff`; the loose-width probability-one countermodel remains ineligible by `SUM-DEGREE-WIDTH` | SuperNeo Definition 6, Section 7.3, and Appendix D.4 SumCheck analysis | `model-proved` |
| `FOLD-PICCS` | Valid `CCS^K x CE^k` inputs produce `CE(b)^(K+k)` outputs. Conditional on an independently correct `PiCCS.Arithmetization` witness for the accepted attempt and extracted assignments, false acceptance reduces to the named SumCheck or mixing-root events; this conditional reduction must not be read as the concrete verifier having constructed that witness | `PiCCS.product_complete`, `PiCCS.strong_extract_or_bad_event`, `PiCCS.repeated_outputs_same_phi` | SuperNeo PiCCS | `model-proved` |
| `FOLD-PICCS-TARGET-CONVENTION` | **Resolved 2026-07-24: `Q` and the Section-7.3 verifier equation are canonical.** The paper's displayed `T(C)` is the local carried-evaluation polynomial whose absolute form is `T_abs(gamma) = gamma^(2K+k) * T_local(gamma)`; the printed display omits that offset, recorded as a paper erratum in `fprime-paper-deviations.md` under `FPR-DEV-PICCS-TARGET-OFFSET`. `Q` is canonical because the three mixing blocks are exponent-disjoint by construction — `F` on `0..K-1`, `NC` on `K..2K+k-1` via its `gamma^K` scaling, so `Eval` must begin at `2K+k` — and that disjointness is exactly what D.4's linear-independence step consumes when splitting Equation (6) into (7), (8), (9). The `(2K+k)` contribution to `epsilon_SZ = (2K+k) * max(log m, ktd) / |K|` is therefore retained, equal to `16` at the Appendix B.2 profile. Lean proves the local-to-absolute identity and exhibits exponent zero as a genuine support-set mismatch, so the displays are provably not interchangeable. The `b=2` norm residual `(z+1)z(z-1) = 0` iff strict centered `|z| < 2` is proved with distinct canonical residues `q-1, 0, 1`, conditional on the typed `BaseFieldNoZeroDivisors` premise. Two obligations are deliberately **not** owned here: concrete discharge of that premise and of `EuclidPrime`/`SevenNonresidue` belongs to `ARITH-GOLDILOCKS-FIELD`, and concrete production residual placement belongs to `FOLD-PICCS-JOINT` and `FOLD-PICCS-SPLIT`. An author query on the erratum may remain open and does not block this property | `PaperJoint.TargetPolynomial.evaluateShifted_eq_shift_mul_evaluateLocal`, `PaperJoint.TargetPolynomial.literalLocal_shifted_support_mismatch_witness`, `PaperJoint.SignedJointIdentity.paperDifference_eq_signedResidualBlocks`, `PaperJoint.NormRange.cubicResidual_eq_zero_iff_strictNormTwo`, `PaperJoint.NormRange.representedRoots_nodup`; all guarded in `tests/Axioms/PiCCSPaperJoint.lean` | SuperNeo Section 7.3 and Appendix D.4 displays | `model-proved` |
| `FOLD-PICCS-JOINT` | The candidate one-joint coefficient skeleton has distinct CCS, norm, and carried-evaluation blocks. A verifier-derived squarefree Boolean-table transform proves coefficient-zero iff every explicit table leaf is zero, and evaluating its canonical flat polynomial equals both an independently recursive MLE and the explicit finite `sum_x eq(x,r) * table[x]`. The CCS, strict-norm, and carried branches independently derive their residual tables and exact zero obligations. The carried traversal is proved to induce gamma exponents exactly `0..ktd-1`. Lean defines pointwise `F`, `NC`, `Eval`, and `Q`, proves `T_abs - sum_x Q = -CCS - norm + carried`, derives the exact signed constant-first three-block coefficient list, and proves executable Horner evaluation equals that identity. A separate finite unsampled object owns alpha-polynomial CCS/norm coefficients and carried scalars; its coefficient truth is equivalent to the explicit table obligations and its alpha specialization is exactly the signed Horner list. One explicit arbitrary-point joint polynomial owns the Boolean initial sum, every canonical expected round, and the full-point terminal. Typed challenge arity enforces one round per Boolean variable. `ConcreteJointData` constructs the sole joint object from independently defined CCS matrices/assignments, typed norm sources, and carried matrix-image data, then proves its coefficient truth is exactly their semantic conjunction. Under the explicit candidate target convention, executable finite acceptance therefore yields that conjunction, the exact signed mixing-root event, or a named SumCheck round collision without a caller-supplied joint object, expected callback, or honesty proof. **Scope fixed 2026-07-24: this property owns the paper-owned one-joint polynomial and its deterministic `SemanticTruth ∨ MixingRoot ∨ BadChallenge` reduction, nothing more.** Bounding the paper mixing root is `FOLD-PICCS-MIXING-BOUND`; concrete primitive/event union accounting remains `BAD-BOUND`. The typed `NormRange.BaseFieldNoZeroDivisors` premise is discharged by `ARITH-GOLDILOCKS-FIELD`. The expected syntactic degree bound is closed: the SumCheck ceiling `VerifierInput.sumcheckDegreeBound` is *derived* as `Nat.max canonicalEqualityGatedDegreeBound 4` from the constraint polynomial rather than supplied by a caller (`sumcheckDegreeBound_eq_of_terms_eq`), every canonical expected round is representable at exactly that ceiling (`expectedRoundsRepresentable`), and the gamma-degree of the signed coefficient polynomial is exactly `jointCoefficientCount - 1` (`polynomial_degreeUpperBound`). Concrete necessity fixtures pin the derived value at `4` (`Necessity/SumCheckSoundnessContract`, `Necessity/SumCheckFixedWidthPadding`). The derived `4` is tighter than D.4's conservative `max(u, 2b+1, 2) = 5` at `b = 2` and does not contradict it. Three former entries are reassigned by ownership and are **not** claimed closed here: external Boolean-leaf/production-bit order moves to `FOLD-PICCS-ARITH`; semantic residual alignment is closed by `FOLD-PICCS-SPLIT-RESIDUAL-ALIGNMENT` and terminal/output authority by `FOLD-PICCS-SPLIT-OUTPUT-AUTHORITY`; both are model-proved; neither carries a physical-row obligation, which would belong to `CIR-*` under M4 `BoundOutputs` binds every output point to the joint challenge vector. This is paper-input semantic closure, not production Lemma 7 refinement, target approval, or implementation authority | `PaperJoint.BooleanVertex.all_nodup`, `PaperJoint.BooleanTable.evaluate_eq_equalityWeightedSum`, `PaperJoint.CCSResidualTable.residualTable_allEntriesZero_iff_constraintSatisfied`, `PaperJoint.NormResidualTable.SourceBatch.allResidualTablesZero_iff_allStrictNormBounded`, `PaperJoint.CarriedEvaluationResidual.allResidualsZero_iff_allClaimsHold`, `PaperJoint.canonicalCarriedCoordinates_localGammaExponents`, `PaperJoint.SignedJointIdentity.paperDifference_eq_signedResidualBlocks`, `PaperJoint.SignedCoefficientPolynomial.paperDifference_eq_evaluate`, `PaperJoint.SignedCoefficientObject.specializedCoefficients_eq`, `PaperJoint.SignedCoefficientObject.coefficientTruth_iff_tableObligations`, `PaperJoint.SignedCoefficientObject.evaluate_eq_zero_iff_coefficientTruth_or_mixingRoot`, `PaperJoint.SumCheckTruthPath.sumCompletions_jointPolynomial_eq_summedQ`, `PaperJoint.SumCheckTruthPath.canonicalGhosts_honest`, `PaperJoint.SumCheckInitial.checkJoint_implies_tableObligations_or_mixingRoot_or_badChallenge`, `PaperJoint.ConcreteJointData.coefficientTruth_iff_semanticTruth`, `PaperJoint.ConcreteJointData.checkJoint_implies_semanticTruth_or_badEvent`, `PaperJoint.BoundOutputs.outputPoint_eq_roundChallenges`, `ProtocolPolynomial.VerifierInput.sumcheckDegreeBound_eq_of_terms_eq`, `ProtocolPolynomialDegree.expectedRoundsRepresentable`, `SignedCoefficientPolynomial.polynomial_degreeUpperBound` | SuperNeo Section 7.3 and Appendix D.4 algebraic skeleton plus explicit target-exponent erratum boundary | `model-proved` |
| `FOLD-PICCS-MIXING-BOUND` | The paper causal verifier owns the exact finite product support `S^ell × S × S^ell` in transcript order: alpha word, one shared gamma, then the later SumCheck word. Coefficient nontruth selects one verifier-independent false table or scalar obligation. A direct finite Cartesian MLE theorem bounds bad alpha words by `ell * |S|^(ell-1)`; constant-first synthetic division plus no-zero-divisors bounds bad gamma values by `jointCoefficientCount-1`. Exact fiber counting, the alpha/gamma marginal, two outer product marginals, and Boolean transport to the literal `MixingFailure` construct `MixingRootProbabilityContract` at `(ell + jointCoefficientCount - 1)/|S|`. The combined theorem constructs both the mixing and SumCheck contracts and preserves `(mixing + SumCheck) + rawMismatch/successFloor`; neither contract is a premise. This is paper/model-level: the separately proved production `deriveCore`/support obstruction, concrete Goldilocks certificates, bounded production sampler, Fiat-Shamir/Poseidon2, Rust, and R1CS remain outside this property | `MultilinearRootCounting.zeros_count_le`, `CoefficientRootCounting.roots_count_le_degree`, `SecurityContracts.mixingRootEvent_eq_true_iff`, `MixingSoundness.{alphaGammaZero_probability_le,verifierAlphaGamma_marginal,mixingRoot_probability_le,mixingRootProbabilityContract_of_rootCounting}`, `RootCountingSecurity.{fixedFirstBadBound_of_rootCounting,extraction_after_first_success_of_rootCounting}`; guarded in `tests/Axioms/PiCcsPaperJointMixingSoundness.lean` | SuperNeo Section 7.3 and Appendix D.4 alpha/gamma Schwartz-Zippel analysis | `model-proved` |
| `FOLD-PICCS-SPLIT` | The production-shaped two-SumCheck FE/NC protocol reconstructs public input and source order from canonical openings, derives both challenge points, starts NC from FE's exact final transcript state, and materializes `y_ring` and packed `y_zcol` only after replay. Accepted verification implies the independent Section 7.3 paper relation or an exact FE/NC algebraic event; block/lane residuals are equivalent to paper NC truth, all carrier coordinates have exact owners, delayed packed projection is raw-running recomposition, honest paper transitions construct accepted certificates, and accepted outputs are the exact PiRLC product. The selected ideal-interactive carrier samples `alpha`, `betaA`, `betaR`, one `gamma` shared by FE and NC, `betaBlock`, `producerBeta`, `batchWeight`, then the FE and NC words in exact transcript order. NC may adapt to the completed FE word while each current challenge remains hidden. Finite multilinear/univariate root counting bounds every literal FE/NC mixing constructor; the unchanged causal theorem bounds the two physical round-collision families by `(feRoundCount * Drow)/|C| + (25 * 4)/|C|`. Two-way event transport identifies the monitor with the actual dependent `FeFailure ∨ NcFailure` family and preserves `(feMixingBudget + ncMixingBudget) + splitCollisionBudget`. Neither `SumCheckSoundnessContract` nor a generic mixing contract is a premise. The arbitrary opaque-schedule/support obstruction remains valid; the positive theorem applies only to the selected constructor. Fiat-Shamir, closed Goldilocks/nonresidue certificates, bounded production-sampler binding, and Rust/R1CS refinement remain separate open boundaries | `ProductionRefinement.{accepted_implies_paper_or_algebraic_failure,not_transcriptFailure,not_bindingFailure,blockLaneCombinedNc_refines_paperNc,everyCoordinate_has_exact_owner,delayedProjection_refines_rawRecomposition,honest_complete_with_output,accepted_output_suitable_for_piRlc}`, `DelayedPackedYZcol.Lifecycle.Trace.{base_owns_no_predecessor,edge_owns_production_and_consumption,terminal_owns_discharge,terminalCount_eq_one,closedTrace_reduces_to_paper_transitions_or_named_failure}`, `CausalFixedPhase.{detects_probability_le,badChallenge_implies_detects}`, `BlockLane.CausalSoundness.{fe_uniformRounds_eq_generated,fe_roundCollision_implies_detects,nc_roundCollision_implies_detects,split_detects_probability_le}`, `BlockLaneCombinedNc.CausalSoundness.{rawRoundRepresentable,splitCollision_implies_detects,splitCollision_probability_le}`, `IdealInteractiveCarrier.{input_supportAligned,derivePreSumcheck_shared_gamma,derivePreSumcheck_delayed}`, `IdealInteractiveExecution.{certificate_fe_coordinates,certificate_nc_coordinates}`, `IdealInteractiveFeSoundness.mixingRoot_probability_le`, `IdealInteractiveNcMixing.ncMixingRoot_probability_le`, `IdealInteractiveSoundness.{algebraicFailureEvent_eq_namedFailureEvent,namedFailure_probability_le,namedFailure_probability_le_of_productionField}` | SuperNeo Section 7.3 and Appendix D.4 versus the registered production FE/NC split | `model-proved` |
| `FOLD-PICCS-SPLIT-RESIDUAL-ALIGNMENT` | **Resolved by a kernel-checked specification obstruction and the correct semantic refinement.** The earlier literal slot-identity target is false: Section 7.3 uses one row-domain polynomial and a distinct gamma exponent for every carried `(running,matrix,coefficient)` coordinate, while production moves the coefficient coordinate onto a separate lane SumCheck axis and uses relative NC gamma weights. In the checked `K=1,k=1,t=2` witness, the production carried group has total exponent `5`, while the corresponding paper coefficient-zero slot has exponent `4` and coefficient-one slot has exponent `6`; both coefficients share that one production gamma group. Production FE has `1+6=7` Boolean coordinates versus the paper row cube's `1`, and the first relative NC exponent is `0` versus absolute joint-Q exponent `1`. `not_literalResidualSlotAlignment` therefore proves that the former target cannot be retained merely to fit a theorem name. The surviving exact correspondences are proved: relative NC exponent is the paper local exponent, applying the joint-Q convention restores the paper norm offset, uncompressed split residual truth is equivalent to the independently stated Section-7.3 relation, and production acceptance yields that relation or the existing exact FE/NC event families. This is a model-level registered-deviation refinement; it makes no message, polynomial, physical-row, or column identity claim | `ProductionRefinement.ResidualAlignment.{ncRelativeExponent_eq_paperLocal,ncJointExponent_eq_paperNormSlot,carriedCoefficientAxis_is_not_gammaAxis,not_literalResidualSlotAlignment,semanticResidualsZero_iff_paperHolds,accepted_implies_paper_or_residual_failure}`; exact witness equations in `ResidualAlignment.Witness`; frozen fail-closed guards in `tests/Axioms/FPrimeFrozenProductionDeviations.lean` | SuperNeo Section 7.3 and Appendix D.4 versus the registered product-domain FE/NC split; specification correction justified by `not_literalResidualSlotAlignment` | `model-proved` |
| `FOLD-PICCS-SPLIT-OUTPUT-AUTHORITY` | Terminal and output authority for the production Split-NC verifier is proved at model level in the required disjunctive form. `OutputAuthority.Holds` binds the verifier-materialized `yRing` to the authoritative FE evaluation, packed `yZcol` to the exact transcript-derived NC block, the FE terminal message to the FE semantic polynomial at the replayed `row ++ lane` point, the ordinary-or-delayed NC terminal message to the selected NC semantic polynomial at the replayed `block ++ lane` point, and the complete downstream PiRLC output product to `PiCCS.honestOutputs` in authoritative source order. Paper truth plus canonical materialization proves this predicate. Consequently, accepted production verification yields `(paper relation ∧ terminal/output authority) ∨ FeFailure ∨ NcFailure ∨ registered deviation`; callers do not supply `¬FeFailure`, `¬NcFailure`, a terminal equality, or an output sidecar. Genuine algebraic failures remain live, so no unconditional authority theorem is claimed | `ProductionRefinement.OutputAuthority.{Holds,of_paper,accepted_implies_paper_and_authority_or_named_failure}`; focused and frozen fail-closed guards in `tests/Axioms/PiCcsSplitNcOutputAuthority.lean` and `tests/Axioms/FPrimeFrozenProductionDeviations.lean` | SuperNeo Section 7.3; registered deviations `delayed packed y_zcol` and `block/lane combined NC` | `model-proved` |
| `FOLD-PICCS-ARITH` | The verifier-visible fixed-width FE/NC certificates are projected to a ghost-free wire view, then interpreted by two independent assignment-indexed semantic polynomials. FE uses the exact transcript-derived `row ++ lane` coordinates; ordinary and delayed NC use the exact `block ++ lane` coordinates and the transcript-owned pending-state tag. Claimed-chain acceptance and verifier-materialized terminal identities transport to independent truth paths; payloads imply FE truth while preserving adversarial public carried claims verbatim, and norms/paper NC truth imply the corresponding NC initial claim. A kernel countermodel proves this construction cannot be automatic for an arbitrary ghost-bearing `PiCCS.Attempt`. At the fixed-active production boundary, accepted verification yields paper truth or the exact FE/NC event families, constructs the source-aligned reference arithmetization, proves that the same reference assignments open the PiRLC inputs, and uses commitment uniqueness to transport arithmetization to extraction or return the existing relaxed-binding collision. Delayed pending-state failure remains the registered delayed-packed-`y_zcol` obligation. The exported theorem has no `rewindArithmetization`, `SumCheckSoundnessContract`, generic mixing contract, semantic ghost, paper-truth, or delayed-bound premise; `MixingRoot` remains live. Concrete no-zero-divisor certification remains separately owned by `ARITH-GOLDILOCKS-FIELD` | `ArithmetizationObstruction.accepted_payloads_norms_ambient_without_arithmetization`, `FixedPhase.SemanticView.{accepted_implies_symbolicAccepted,accepted_implies_truthPath}`, `SumCheck.SemanticAdapter.{feAccepted_implies_truthPath,feClaimTrue_of_truth,ncAccepted_implies_truthPath,ncClaimTrue_of_truth}`, `ProductTruth.{freshTruth_of_payloads,carriedTruth_of_payloads,ncTruth_of_norms,ambientOpenings_of_productHolds}`, `BlockLane.SemanticAttempt.{accepted_of_replay,arithmetization_of_replay}`, `Composition.ReferenceArithmetization.fold_extraction_or_bad_event`, `ProductionRefinement.SemanticAttempt.{accepted,arithmetization}`, `ProductionRefinement.SemanticComposition.fold_extraction_or_named_failure`; guarded directly and through `Frozen.ProductionDeviations.BlockLaneCombinedNc` | SuperNeo Section 7.3 and Appendix D.4 FE/NC truth and composition equations | `model-proved` |
| `FOLD-PIRLC` | Commitment, input, witness, and evaluations are the same random linear combination into `CE(B)`; the reduction is **weak** (Definition 9) with respect to the commitment projection `φ` shared with PiCCS's strength — standalone knowledge soundness is deliberately NOT the target, and extraction lands in the ambient `CE(q/2)` stage (D.5) | `PiRLC.complete`, `PiRLC.ExtractionOutcome`, `PiRLC.same_phi_extractions_unique_or_collision` | SuperNeo PiRLC, Lemma 4, Appendix D.5 | `model-proved` |
| `FOLD-PIDEC` | The Section-7.5 verifier consumes only child commitment/evaluation messages, computes every child public input as `split_b(parent.x)`, copies structure/point, fixes the fresh stage, enforces the structure-owned `t`-evaluation tuple shape, and checks commitment/evaluation recomposition. The concrete Phi81 public split commutes with authoritative projection and recomposes exactly, while its evaluation arity is exactly the matrix count; explicit public-input substitution and trailing-evaluation regressions are rejected. Exact output acceptance is honestly complete, implies the older recomposition predicate, and inherits its **reduction of knowledge** (Theorem 7); the post-decomposition relation returns to the `b` stage, not the ambient bound | `PiDEC.PaperVerifier.{children,EvaluationArity,OutputAccepted,output_complete,reduce_knowledge}`, `PiDEC.PaperVerifier.OutputAccepted.{toRecompositionAccepted,childEvaluations_size}`, `PiDECAlgebra.PublicInput.{splitPublicInput_project,splitPublicInput_recompose}`, `ProductionChildSubstitution.Fixture.{rightNotPaperAccepted,trailingEvaluationChildren_notPaperAccepted}` | SuperNeo PiDEC, Theorem 7 | `model-proved` |
| `FOLD-COMPOSE` | The generic strong–weak law composes PiCCS with PiRLC over the same-`φ` paired experiment (Theorem 6), then composes the resulting knowledge game with zero-loss PiDEC. The frozen final game is definitionally computed from those two operational couplings; it is not a caller-selected summary. Its loss is computed in composition order from fork sampling, SumCheck, Schwartz–Zippel, one conditioning-adjusted relaxed-binding term, and PiDEC's explicit zero. In the concrete finite coupling, PiRLC's ordered commitment vector is definitionally PiCCS's ordered output projection, and two prefixes of one statement therefore have the same `φ`. A separate finite operational instantiation proves the complete `Pi_DEC ∘ Pi_RLC ∘ Pi_CCS` theorem. `FinitePaperStrongBoundary` proves why that older game's runtime slot means exactly all finite cutoffs, and `PiCcsAsymptoticObstruction` proves why no retry result follows from an arbitrary unlinked runtime proposition. The operational replacement is security-parameter indexed: `FirstSuccessTrace` proves finite-prefix conservation, rational-epsilon tail vanishing, normalized terminating mass, the unique retry fixed point, the exact success-conditioned-first × fresh-unconditioned-second law, and expected retry count `1/p`; `FirstSuccessRuntime` derives almost-sure termination and EPT from explicit per-seed work, a polynomial one-run bound, and an inverse-polynomial positive success floor. `AsymptoticPaperStrong.paperStrong` preserves the exact frozen intrinsic order `SumCheck + Schwartz–Zippel` and charges raw mismatch as `delta / mu` once. `Frozen.SuperNeo.piCcsStrong_of_unboundedFirstSuccess` then proves the exact frozen `PiCcsStrong` target for the definitionally linked game. Fiat–Shamir and concrete primitive instantiation remain open | `Frozen.Obligations.{strongWeakKnowledgeGame,superNeoCompositionGame,superNeoCompositionReductionOfKnowledge,superNeoPaperObligations_of_components}`, `Frozen.SuperNeo.{finitePaperStrong,extractorRuntime_iff_all_finite_cutoffs,piCcsStrong_iff_runtime,frozenTarget_without_samplerLink_countermodel,piCcsStrong_of_unboundedFirstSuccess,paperWeak,batchPhi_eq_piCcsOutputPhi,repeatedBatch_samePhi,piDec_reductionOfKnowledge,finiteReductionOfKnowledge}`, `FirstSuccessTrace.{partialTerminationMass_add_failureTail,failureTail_vanishes,jointProbability_unique,jointProbability_eq_conditionedFreshProduct,expectedRetryExecutions_firstStep}`, `FirstSuccessRuntime.{traceWork_le_executionCount_mul_bound,extractorExpectedPolynomialTime}`, `Frozen.CompositionLinkageObstruction.unlinked_fields_countermodel` | SuperNeo Theorem 6 and Appendix D.3–D.4, followed by Theorem 7 and Definition 5 sequential composition | `model-proved` |
| `FOLD-NIFS` | One independent typed NIFS attempt composes PiCCS, PiRLC, and PiDEC with exact statement identity at both phase boundaries. The production fixed-active paper profile fixes `K = 1`, `k = 14`, and `b = 2`: one fresh CCS plus fourteen running CE sources produce an internal combined parent and an operational PiDEC output whose public inputs/structure/point/stage and evaluation-vector arity are verifier-owned and whose commitment/evaluation messages satisfy the two paper equations. `Transition` is public verifier acceptance; target-child openings remain explicit premises of the reduction-of-knowledge theorem rather than checks silently added to PiDEC. Deterministic splitting of the complete private parent opening is only the conditional honest constructor; the existing substitution counterexample applies solely to the weaker recomposition predicate and is explicitly not paper acceptance. The concrete Phi81 realization refines the parameterized profile. The richer `ResultTransition` is the paper realization plus polynomial-input binding, incoming-parent authority, outgoing-parent materialization, and an extra strengthening that fixes prover messages to one selected private split. The knowledge theorem remains conditional on the weak extractor, uniqueness bridge, rewind-time PiCCS arithmetization, target-child openings, and named bad-event exclusions; no transcript, implementation callback, digest, R1CS artifact, or historical count defines this relation | `Nifs.PaperNifsTransition`, `Nifs.complete`, `Nifs.paperNifsTransition_complete`, `Nifs.accepted_inputsValid_or_badEvent`, `Nifs.PaperProfile.{complete,Transition}`, `Nifs.PaperProfile.Realization.{parentOpening,outputAccepted,outputRecompositionAccepted,parentOpening_eq_recompose_or_bindingCollision}`, `ConcretePhi81.FixedActive.PaperProfile.Realization.toGeneric`, `ConcretePhi81.FixedActive.resultTransition_iff_exists_paperDecomposition`, `ConcretePhi81.FixedActive.ResultTransition.toPaperProfile` | `SuperNeo.Folding.{PiCCS,PiRLC,PiDEC,Composition,Nifs,Nifs.PaperProfile}` and `ConcretePhi81.FixedActive.PaperProfile` | `model-proved` |
| `FPR-PAPER` | The independent Construction-2 augmented-relation family is indexed by fixed `j`, has distinct `ell` program slots and `k` SuperNeo children, requires control dispatch to `j`, evaluates fixed `F_j`, and owns an actual no-fold/default base branch plus a recursive prior-link/range/selected-NIFS/copy branch. The same abstract `vk_fs` value in the hash selects the per-slot NIFS semantics and expected structure. Every active accepted step retains and exposes its exact `Nifs.Accepted` attempt and `PaperNifsTransition`; constructive theorems produce canonical base and recursive outputs. Concrete key generation/parsing, Fiat-Shamir, Poseidon2 binding, implementation callbacks, and rows are outside this property | `Paper.Holds`, `Paper.PaperFPrimeStep`, `Paper.selected_nifs_edge`, `Paper.selected_nifs_transition`, `Paper.base_exists_holds`, `Paper.recursive_exists_holds` | HyperNova Construction 2 plus `SuperNeo.Folding.Nifs` | `model-proved` |
| `SAMPLER-FIRST-ACCEPTED` | For an abstract verifier-owned acceptance/symbol map and candidate stream, bounded sampling succeeds exactly when a least terminating reference execution lies within the bound; success returns exactly the requested first accepted symbols, every selected symbol has an accepted source-candidate preimage, and shortfall is explicit. The PiRLC specialization has the production's nested shape: one verifier-owned source/successor state is threaded across all `K+k` scalars, every scalar has its own bounded first-accepted coefficient execution, and the complete selected coefficient vector is assembled into that scalar. Strong-set assurance is split into accepted-coefficient validity and an assembly theorem for the scalar; shortfall at any coordinate rejects the batch. For the current partial carrier, canonical replay plus explicit sampler refinement binds every carried PiRLC scalar to its assembled vector. Independently, the production-alphabet model proves that accepting 16-bit chunks below `65535` gives the exact product domain `Fin 13107 × Fin 5`, centered symbols lie in `[-2,2]`, 54-of-64 success is conditionally equivalent to a least reference cursor inside the bound, and success implies `48 < consumed ≤ 64`. The production schedule further makes each 16-candidate block and its successor state projections of one deterministic machine call, and proves that every successful scalar and batch-state transition uses exactly four complete blocks. The coefficient strong-set layer proves sampled vectors lie in `[-2,2]^54`, pairwise differences lie in `[-4,4]^54`, distinct vectors have a nonzero difference coordinate, threshold `5` suffices for the coefficient-level infinity-norm premise, and the expansion arithmetic is `216`. A pure production-shaped transcript machine now fixes the eight-lane Goldilocks state, overwrite absorption, scalar/digest domain pairs, extracted Poseidon2 permutation, canonical lane conversion, and lane-major little-endian chunk order, and proves successful fixed executions reach the same four-block successor. This still does not prove the carrier complete, the exact reached post-PiCCS state, native/gadget/generated-row conformance, centered Goldilocks/quotient-ring embedding, Theorem-8 invertibility, rotation-matrix refinement, distribution/bias, or full R1CS refinement | `FirstAccepted.boundedSample_eq_some_iff_referenceExecution_within`, `FirstAccepted.mem_firstAccepted`, `PiRlcSampler.coefficient_has_accepted_preimage`, `PiRlcSampler.responseRefinesAt_implies_reference_within`, `PiRlcSampler.responseRefinesAt_valid`, `PiRlcSampler.shortfall_excludes_responseRefinesAt`, `PiRlcSampler.ProductionAlphabet.acceptedFactorization`, `PiRlcSampler.ProductionAlphabet.sample54of64_eq_some_iff_reference_within`, `PiRlcSampler.ProductionAlphabet.successful_cursor_in_fourth_digest_window`, `PiRlcSampler.ProductionSchedule.successful_execution_uses_four_blocks`, `PiRlcSampler.ProductionSchedule.stateAt_succ_eq_referenceBlockState`, `PiRlcSampler.ProductionStrongSet.coefficientStrongPrecondition`, `PiRlcSampler.ProductionStrongSet.sampledChallenge_valid`, `PiRlcSampler.ProductionStrongSet.expansionFactor_value`, `PiRlcSampler.ReplayBridge.acceptsCanonical_challenges_eq_sampled`, `PiRlcSampler.ReplayBridge.acceptsCanonical_challenges_valid`, `PiRlcSampler.ReplayBridge.shortfall_excludes_replayResponseRefines`, `PiRlcChallenge.TranscriptMachine.digestChunks_lane_part`, `PiRlcChallenge.TranscriptMachine.successfulExecution_successorState` | Generic rejection-selection semantics, production alphabet and block schedule, pure production-shaped transcript semantics, coefficient strong-set mathematics, transcript-chained PiRLC coefficient-batch contract, and current-carrier replay bridge | `specified` |
| `FOLD-NIFS-FS-GAP` | The current typed replay deterministically reconstructs its selected FE/NC and PiRLC response skeleton, but is formally blind to FE/NC envelope fields and PiCCS output points. These witnesses prevent replay exactness from being mistaken for transcript sufficiency | `NonInteractive.Replay.acceptsCanonical_iff_carrierAgreement`, `NonInteractive.canonicalEvents_replaceFeEnvelope`, `NonInteractive.canonicalEvents_replaceNcEnvelope`, `NonInteractive.canonicalEvents_replacePiCcsOutputPoints` | Partial noninteractive NIFS carrier | `model-proved` |
| `FOLD-NIFS-FS-FROZEN` | The frozen quantitative obligation-5 proposition is instantiated by the complete finite correlated prefix/post-prefix oracle experiment. Its accepted and transition predicates are the actual typed paper NIFS predicates, its loss is exactly `nonInteractiveTotal`, ideal challenge-sampling failure is zero on the selected support, and the multi-fork programming term is proved internally as `(ell + 1) / |C|`. Premises are only one accepted target-witness extraction bound, four exact interactive-event bounds, and four exact typed transcript-collision bounds on that same experiment. Deterministic NIFS soundness and honest completeness are separately exact. This is the charter-permitted explicit random-oracle boundary; it is not Poseidon2, Rust, or R1CS conformance | `Frozen.SuperNeo.fullOracleMixtureNifsNonInteractiveSound`, `Frozen.SuperNeo.nifsSoundAndCompleteModulo`, `PaperNonInteractive.fullOracleMixtureExplicitRandomOracleContract` | SuperNeo D.3--D.6 and HyperNova Section 3 Fiat--Shamir boundary | `model-proved` |
| `FOLD-NIFS-FS` | Under the charter-permitted explicit random-oracle contract, the typed paper NIFS is deterministically sound modulo its five named events, perfectly complete, and quantitatively non-interactive over the complete finite correlated prefix/post-prefix experiment. The transcript absorbs the complete running/fresh public pair before PiCCS, derives alpha coordinates then shared gamma, absorbs each finite SumCheck message before its indexed challenge, absorbs the complete PiCCS output before PiRLC, and supplies each PiRLC query its literal `Fin (K+k)` coordinate. The final subtractive loss retains one accepted target-witness extraction event, four interactive events, and four typed transcript-collision events; ideal sampling failure is zero and the D.5 programming loss `(ell + 1) / |C|` is derived internally. Neither frozen headline proposition is a premise. Concrete Poseidon2 encoding/collision reduction, the bounded production sampler, Goldilocks/quotient-ring instantiation, Rust, and R1CS remain separate refinement obligations | `Frozen.SuperNeo.paperNifsSoundCompleteAndNonInteractive`, `Frozen.SuperNeo.{piCcsExecution_coins_eq_replayInput,piCcsExecution_outgoingState_eq_postOutput,piRlcChallenge_eq_response_after_piCcsOutput}`, `PaperNonInteractive.{fullOracleAccepted_implies_transition_or_failure,fullOracleMixtureExplicitRandomOracleContract,fullOracleMixtureAccepted_probability_sub_total_le_transition}` | HyperNova Section 3 and Appendix B; SuperNeo public-coin reductions and Appendices D.3--D.6 | `model-proved` |
| `FPR-ENVELOPE` | Base/active tag, counters, `pc`, immutable coordinates, and trace copy are coherent | `FPrime.Envelope.check_sound` | Rust state/transition helpers | `model-proved` |
| `FPR-BASE` | The base branch uses the default running instance and enforces the verifier-derived boundary/trace seeds, semantic mode, initial Nebula lane, empty accumulator, exact NoFold variant, nonempty output batch, and recomputed output. The installed batch's public link is a one-step-delayed consumer/terminal obligation, not a producer-step check | `Step.fPrimeBaseLocal_sound`, `Step.holds_iff_local_and_outgoing`, `Step.closeLocal` | HyperNova Construction 2, Rust base branch and terminal latest link | `model-proved` |
| `FPR-BASE-SPEC` | HyperNova's universal default pair can be replicated into an exact pointwise-satisfying vector of any arity for every `(pp, structure)` context. Rust's empty `RunningInstance` is separately a valid zero-arity product; it does not realize or replace the omitted `u_⊥` entry. Refinement of Rust's nonempty `canonical_zero` accumulator to the replicated paper vector, and native-empty/base-circuit lifecycle alignment, remain open | `Default.replicatedDefault_allPairs`, `Default.emptyRunning_zeroArity` | HyperNova Construction 2 step 3, Rust `RunningInstance::{default,canonical_zero}` | `model-proved` |
| `FPR-COUNTER-REFINE` | The paper's single step index `i` refines to Rust's `(chunk_count, step_count)` pair under an explicit refinement relation (`chunk_count` counts F' invocations, `step_count` sums nonempty fresh-batch cardinalities); native overflow is rejected rather than wrapped | `CounterRefinement.counter_refinement` | HyperNova Construction 2, Rust `advance_state` | `model-proved` |
| `FPR-RECURSIVE` | The recursive branch pins prior authority, recomputes its running handle, checks the prior batch's recursive link, obtains the next running value from executable NIFS.V, checks semantic/Nebula advance, installs a nonempty new batch, advances state, and recomputes x_out. The newly installed batch is linked by its consumer or the terminal fold | `Step.fPrimeRecursiveLocal_sound`, `Step.holds_iff_local_and_outgoing`, `Step.closeLocal`, `Step.holds_advance_facts` | HyperNova Construction 2, Rust recursive branch and terminal latest link | `model-proved` |
| `FPR-CANONICAL-SHARED-DIFFERENTIAL` | Two exact bounded production profiles now share inputs with the frozen Lean checkers. The step corpus contains two honest `verify_step` inputs and seven independent mutations covering the initial endpoint, fold tag in both directions, one-based prior program counter, prior public link, NIFS proof, and claimed `x_out`; Lean kernel-reduces all nine acceptance comparisons. The terminal corpus drives production `verify_uncompressed` on honest base and recursive terminals plus five independent base-endpoint, recursive program-counter, prior-link, running-relation, and fresh-relation mutations. Its recursive payload records the exact Rust link, running-CE, and fresh-CCS checks independently of the final Rust result; Lean kernel-reduces all seven terminal comparisons. Both honest cases accept in each profile and every mutation rejects. The generated maps preserve the verifier-visible payload and exact profile metadata, while opaque values remain typed quotient atoms only where the frozen checker itself treats them opaquely. NIFS, hash, and terminal relation outcomes remain explicit Rust receipts. This is bounded differential evidence under the generated carrier preconditions, not primitive correctness, general refinement over malformed raw `Uncompressed` carriers, R1CS refinement, or row-removal authority. The boundary is now kernel-checked rather than documentary: after erasing the recorded Rust Boolean, explicit Step and Terminal acceptance oracles agree with every generated input but flip the canonical result at one fresh input, so finite corpus agreement alone cannot entail the universal obligation-11 refinement theorem | `FPrimeCanonicalSharedInputDifferential.generated_all_agree`, `FPrimeCanonicalSharedTerminalDifferential.generated_all_agree`, `OneSlot.{stepAgrees_eq_true_iff,terminalAgrees_eq_true_iff}`, `OneSlot.FiniteCorpusBoundary.{Step,Terminal}.not_attemptedUniversalBridge` | `paper::construction2::verify_step_with_execution_receipt`, production `verify_uncompressed`, `canonical_{step,terminal}_export.rs`, generated JSON corpora and `OneSlot/Generated/{Step,Terminal}Cases.lean` | `rust-conformant` |
| `FPR-NATIVE-PAPER-FRESH-LINK-BOUNDARY` | HyperNova's recursive branch compares the unary values `freshPublic(fresh)` and `encodeInstance(priorDigest)`, whereas the current lifecycle model exposes only an arbitrary binary Boolean `freshLink(priorDigest, fresh)`. Lean constructs a complete `Step.Semantics` whose four-entry link relation has overlapping unequal true fibers and proves that no encoded carrier and no pair of unary maps can factor it through equality. This is an exact current-interface obstruction, not a claim about production's actual link. Positively, the typed logical 257-coordinate profile `[1 | enc_inst(digest)]` has an executable coordinate check that accepts exactly the canonical public-input encoder. The typed plain SuperNeo carrier then represents every field coordinate before validation, checks `m_in = 270`, affine one, all 256 lane-major little-endian link coordinates, and all thirteen zero-padding coordinates, and proves both exact equality factorization through `[1 | enc_inst(digest) | 0^13]` and a reduction to the logical paper link. The untrusted-list `sourceCheck` owns `x.length = 270`, the exact affine/body/padding split and flattening order, and pointwise and batch reduction to the logical relation. Production lifecycle and paper-decider acceptance now call one shared pure predicate, and a Rust regression mutates every one of the 256 body and thirteen padding coordinates plus all shape fields. Still missing is a formal translation theorem proving that the concrete Rust predicate and its call arguments implement `sourceCheck`, plus the separately owned optional Nebula suffix | `PaperFreshLinkBoundary.{EqualityFactorization,overlappingLink_not_equalityFactorized,currentInterface_admits_nonFactorizingFreshLink}`, `CanonicalPublicInputLink.{check_eq_true_iff,equalityFactorization}`, `CanonicalPlainCarrierLink.{check_eq_true_iff,equalityFactorization,rawCheck_eq_true_iff,rawCheck_reduces_to_typedCarrier,check_reduces_to_logicalPaperLink}`, `CanonicalPlainCarrierSource.{sourceCheck_eq_true_iff,sourceCheck_reduces_to_logicalPaperLink,sourceBatchCheck_reduces_to_logicalPaperLink}`, `f_prime_public_input_link_matches` and `canonical_public_link` | HyperNova Construction 2 recursive prior-public equality, canonical F-prime public encoding, and the current `Step.Semantics.freshLink` boundary | `model-proved` |
| `FPR-NIFS-BRIDGE` | The typed fixed-one recursive checker consumes the actual raw production-shaped NIFS certificate, enforces the complete existing physical checker plus all five verifier-computed paper PiDEC output equations, computes the canonical F-prime output, and refines successful execution to HyperNova Construction 2 over the actual public child vector or exactly `yRing`, packed-`yZcol`, or PiCCS algebraic failure. Source binding is no longer a generic named escape: the theorem consumes a typed `SourceAuthority`, and `SourceAuthority.ofCanonicalOpening` constructs it from the authoritative opening-derived carrier after one exact selected-context identity. Honest paper sources construct an accepted execution satisfying Construction 2 or expose the exact bounded-sampler shortfall. No child commitment/evaluation is required to equal a deterministic private split. The same fixed-active production prefix reaches the composition knowledge theorem without a `rewindArithmetization` premise | `PaperBoundary.{SourceAuthority,check_eq_true_iff_accepted,run_eq_some_iff,run_refinesConstruction2_or_namedFailure,exists_run_and_construction2_or_samplerShortfall}`, `PaperBoundary.SourceAuthority.ofCanonicalOpening`, `FixedActive.PaperProfile.CertificateRefinement.paperOutputEquations`, `ActiveEvaluator.physicalChecks_refineConstruction2_of_paperTransition`, and `ProductionRefinement.SemanticComposition.fold_extraction_or_named_failure`. The bridge is model-level and follows the charter’s semantics-before-lowering sequence: the shipping decoder still must establish the exact context identity used to construct `SourceAuthority`; Rust/R1CS decoding and generated-row equality are not part of this property and remain obligation-11/M4 refinements | SuperNeo Sections 7.3–7.5; HyperNova Construction 2; typed fixed-active production NIFS | `model-proved` |
| `FPR-OBLIGATION-EXACT` | A fixed-candidate reduced recursive-verifier obligation set is sound and complete for the paper-level NIFS relation, with source data and coins fixed outside the checked propositions so existential witness substitution cannot rescue a forgery. The six macro obligations are fresh CCS truth, all source norms, carried evaluations, complete source binding, strong-set challenges, and exact operational PiDEC acceptance. The NIFS tail uses verifier-computed child public/copy fields plus commitment/evaluation recomposition, not relaxed public recomposition or canonical private splitting. Executable SumCheck/transcript/security and HyperNova lifecycle closure remain separate properties | `FixedActive.PaperProfile.ObligationPlan.{accepts_iff_target,exact}` | SuperNeo PiCCS/PiRLC/PiDEC fixed-active paper profile | `model-proved` |
| `FPR-OBLIGATION-NECESSITY` | After the bounded shared-input differential gate, the payload-minimal fixed-one executable is classified exhaustively. The step ledger retains one base endpoint equality, one recursive prior-public link, and the selected paper-NIFS profile's six macro obligations; removing any one admits a kernel-checked invalid candidate while the other seven remain true. Dispatch and raw prior-counter range are derived from `Fin 1`, and all four public-output equations are definitional constructions rather than checks. The terminal ledger retains exactly the base endpoint, recursive prior link, sole running relation, and selected fresh relation; each has an executable-linked removal counterexample, while the counter and all-slot fold are derived. The bundled step and terminal `obligation8_classification` theorems have fail-closed axiom guards. This is exhaustive inclusion-minimality for those typed normal forms, not necessity of every internal PiDEC equation, source-binding coordinate, physical row, or a global minimum across arithmetizations | `CanonicalVerifier.FixedOne.Minimality.{obligation8_classification,inclusionMinimalSound}`, `CanonicalTerminalVerifier.FixedOne.Minimality.{obligation8_classification,inclusionMinimalSound}`, `SelectedNifs.PaperSemanticMinimality.{necessityWitnesses,provedNecessaryLeaves_eq_checks,inclusionMinimalSound}` | Fixed-one canonical executable step/terminal normal forms and the fixed-active selected paper-NIFS macro plan | `model-proved` |
| `FPR-TYPED-LOWERING` | A typed, artifact-independent lowering IR owns primitive, block, branch, and program execution; every primitive has exact soundness and completeness; schemas own inputs and outputs; every source occurrence contributes a nonoptional structural emission receipt; and the four-way cost `(recurring rows, committed columns, public columns, auxiliary columns)` is definitionally folded from those receipts. The exact fixed-one step and terminal programs execute iff their frozen canonical transition predicates hold. This property does not select physical Goldilocks rows or claim Rust/R1CS conformance | `Typed.Primitive.{exec_eq_some_iff_holds,sound,complete}`, `Typed.Block.{exec_eq_some_iff_holds,sound,complete}`, `Typed.Program.{exec_eq_some_iff_holds,sound,complete,flattened_conservation,cost_eq_receipt_event_cost}`, `FPrimeFixedOne.Step.accepts_iff_transition`, `FPrimeFixedOne.Terminal.accepts_iff_transition` | Typed lowering IR and exact fixed-one executable programs | `model-proved` |
| `FPR-CANONICAL-ENCODING` | The selected fixed-one Goldilocks encoding is constructively realized, parametrically over a typed profile and exact call recipes (plus the Step default-running codec premise). Its ordered source-aligned receipts cover the public prelude, every input, every primitive, both branch activations, the nonempty Step join, the mandatory empty Terminal join, and every continuation. Physical row and column identities are globally unique, every physical occurrence has exactly one typed source owner, nothing exists outside receipts, and cost is the exact receipt fold in lexicographic order `(rows, committed, public, auxiliary)`. The finite local rewrite class holds all opaque-call, literal, input, output, activation, and fixed resources constant; it compares one selected mux row with two gated equalities for each running coordinate, and a direct retained assertion with a materialized residual. Exact selected-site receipts and source coordinates are proved equal to the actual whole-program receipts. Partitioning those actual receipt-owned rows derives—rather than assumes—the selected costs `fixed + (runningWidth + 2)` and `fixed + 4`, and proves both costs minimum in the stated finite class. For every supplied admissible profile and certified recipe family, physical satisfaction is sound for the exact Step/Terminal checker and every admissible accepted execution constructs an assignment satisfying the selected receipts; Step soundness also binds the decoded result columns to the accepted output. This establishes obligations 9–10 and the artifact-independent soundness/completeness slice of obligation 11. A concrete production profile/recipe instantiation, Rust-emitted-program equality, generated-row equality, and any global minimum across arithmetizations remain open | `CanonicalEncodingRealization.{step,terminal,stepObligation10,terminalObligation10}`, `CanonicalEncoding.{Step,Terminal}.obligation10_of_certificate`, `SourceAlignment.AlignedReceiptProgram.{column_identity_has_exactly_one_source_owner,row_identity_has_exactly_one_source_owner,cost_eq_receipt_cost}`, `CanonicalEncoding.Step.costConserved`, `CanonicalEncoding.Terminal.costConserved`, `FPrimeFixedOne.Encoding.NormalForm.{stepCanonicalMinimumWithFixedCost,terminalCanonicalMinimumWithFixedCost}`, `CanonicalStepSoundness.physicalSoundAligned`, `CanonicalStepCompleteness.physicalComplete`, `CanonicalTerminalSoundness.physicalSound`, `CanonicalTerminalCompleteness.physicalComplete` | Exact typed fixed-one programs, selected Goldilocks receipt programs, source alignment, certified call recipes, and the explicit finite local rewrite class | `model-proved` |
| `FPR-DIRECT-CALL-RECIPES` | Five calls in the selected fixed-one vocabulary now have concrete artifact-independent Goldilocks recipes: bounded-iteration zero testing; full-coordinate state equality; fresh-public affine encoding; digest-to-instance affine encoding; and full-coordinate encoded-instance equality. Each recipe proves its exact program-derived footprint, physical ownership and row support, active semantic soundness, honest completion of every declared temporary, and inactive satisfiability. `certifiedSubset` packages exactly those five recipes. `RemainingRecipes` names only the application step, prior hash, next hash, NIFS verification, running terminal check, and fresh terminal check; `allRecipes` can construct the complete family only from certified recipes for all six. No generic acceptance proposition, generated artifact, or compiled Rust semantics is admitted by this result | `DirectCalls.{iterationZeroRecipe,stateEqualRecipe,freshPublicRecipe,encodeInstanceRecipe,encodedEqualRecipe,certifiedSubset,RemainingRecipes,allRecipes,remainingCalls_exact}` | Selected direct-call Goldilocks programs and the typed fixed-one call vocabulary | `model-level` |
| `FPR-APPLICATION-CERTIFICATION` | For every explicitly selected HyperNova application profile whose codecs, fixed-23 hash serialization/alignment equations, and independent running/fresh terminal equations are proved, Lean constructs complete artifact-independent `CallRecipe`s for `hashPrior`, `hashNext`, `runningCheck`, and `freshCheck`. The hash recipes include the rejecting presence/alignment wrapper around the exact width-8 Poseidon2 sponge, preserve distinct auxiliary/public digest ownership, and provide active soundness, honest active completeness, inactive satisfiability, row ownership/support, and nonoptional receipts through the existing `CallRecipe` contract. The terminal recipes compare authoritative running and running-witness encodings and authoritative fresh and fresh-witness encodings independently; neither NIFS nor one terminal relation can substitute for the other. Successful codec decoding has exact width. The four-call list and its one-each multiplicities are derived, and `phase34Cost_exact` kernel-computes rows and committed/public/auxiliary columns from the selected widths. This is a profile-indexed application-certification theorem, as permitted by HyperNova Construction 2; it does not select a unique deployment application, fill `step` or `nifsVerify`, construct `allRecipes`, prove Rust/generated-row equality, or claim collision/Fiat–Shamir security | `ApplicationCertification.{poseidon23,calls_exact,call_multiplicities,hash_outputs_distinct,terminal_calls_distinct,hashPriorCost_exact,hashNextCost_exact,runningCheckCost_exact,freshCheckCost_exact,phase34Cost_exact}`, `Poseidon23HashPriorRecipe.recipe`, `Poseidon23HashNextRecipe.recipe`, `RunningCheckRecipe.recipe`, `FreshCheckRecipe.recipe`, and `Codec.length_eq_width_of_decode` | HyperNova Construction 2 application-selected circuits/encoders; frozen fixed-one `hashPrior`, `hashNext`, `runningCheck`, and `freshCheck` calls | `model-proved` |
| `FPR-COMPLETE-APPLICATION-CERTIFICATION` | For every explicitly selected Poseidon23 application profile and setup that supply complete proof-carrying physical `CallRecipe` programs for the HyperNova application step and the selected NIFS verifier, Lean supplies exactly one certified recipe at each of the eleven typed call positions. These two setup-owned inputs are executable row programs with exact footprints, receipts, active soundness, honest active completeness, and inactive satisfiability; they are not acceptance propositions or caller-supplied semantic conclusions. Call multiplicities are separately derived from the actual typed ASTs: Step contains nine call occurrences and omits the two terminal checks, while Terminal contains eight and omits application step, next hash, and NIFS verification. The resulting canonical Step and Terminal programs are sound and honestly complete, and physical acceptance is extensionally equal to the unchanged frozen checker for admissible codec values. Every emitted row and allocated column has exactly one source-aligned receipt owner, no row or column exists outside the receipt fold, and the exact four-component costs are computed from the assembled programs and attain the already selected finite rewrite-class minima. The result is profile-indexed, as required by HyperNova Construction 2: it does not select one shipping application/setup, derive a unique numerical deployment cost without that profile, prove Rust-emitted-program or generated-row equality, suppress named algebraic events, or claim cryptographic security | `Phase5CallCertification`, `CompleteApplicationCertification.{allRecipes,recipe_family_multiplicities,stepProgramCalls_exact,terminalProgramCalls_exact,step_call_multiplicities,terminal_call_multiplicities,step_sound,step_complete,stepPhysicalAccepts_iff,terminal_sound,terminal_complete,terminalPhysicalAccepts_iff,step_obligation10,terminal_obligation10,stepCost_exact,terminalCost_exact,stepCost_eq_receiptFold,terminalCost_eq_receiptFold,step_everyRow_has_exact_owner,step_everyColumn_has_exact_owner,terminal_everyRow_has_exact_owner,terminal_everyColumn_has_exact_owner,step_rows_conserved,step_columns_conserved,terminal_rows_conserved,terminal_columns_conserved}` | HyperNova Construction 2 setup-selected application circuit and NIFS verifier; complete artifact-independent fixed-one Step/Terminal encoding | `model-proved` |
| `FPR-NATIVE-LOWERING-ALIGNMENT` | The universal native fixed-one adapter now instantiates the typed lowering vocabulary with the same setup and paper machine. Lean proves exact call semantics for all six calls whose physical recipes remain open: totalized application, prior hash at iteration `i`, next hash at `i + 1`, ordered NIFS verification, and the supplied exact running/fresh terminal checks at the unique selected slot and key. The instantiated typed Step program accepts exactly `Step.Holds`, and the instantiated typed Terminal program accepts exactly the supplied paper terminal transition. Widths and footprints remain explicit shape placeholders: this result supplies no codec, physical recipe, row or column placement, generated artifact equality, or compiled-Rust semantics | `FixedOneLoweringAdapter.CallAlignment.{step,hashPrior,hashNext,nifsVerify,runningCheck,freshCheck}`, `FixedOneLoweringAdapter.{stepAccepts_iff_directHolds,terminalAccepts_iff_transition}` | Universal native fixed-one adapter and artifact-independent typed lowering vocabulary | `model-level` |
| `FPR-PRODUCTION-ITERATION-ZERO-RECIPE` | The canonical bounded-natural zero test is selected independently of every protocol-data codec and unrelated call map. Given a supplied full lowering profile, the permitted Goldilocks no-zero-divisor/inversion contracts, and exact footprint alignment, it forms a complete typed `CallRecipe`: exactly three rows, exactly two auxiliary one-coordinate temporary bundles, active soundness, honest completion, inactive satisfiability, physical ownership/support, and a nonoptional exact receipt. This does not instantiate any other production call or identify rows with a Rust artifact | `ProductionIterationZeroCallRecipe.{selected_footprint_exact,recipe_row_count,receipt_exact}` | Canonical bounded-natural/Boolean codecs, paper field laws, and the selected zero-test compiler | `model-level` |
| `FPR-PRODUCTION-DIGEST-CODECS` | The first concrete production-codec slice fixes the native digest as four identical Goldilocks coordinates in Rust `[F; 4]` lane order, gives it an exact round-trip decoder, represents a rejecting optional digest as one presence coordinate plus four lanes with a unique all-zero absent payload, and represents the compact fixed-one adapter output as that optional digest plus one canonical linked bit. The native adapter's `encodeInstance` is proved to be exactly the affine map that copies the five optional-digest coordinates and appends verifier-fixed one. This result does not instantiate the complete lowering codec family or widths, and it supplies no nonlinear fresh-public recipe, state/fresh/witness codec, hash/NIFS/terminal recipe, row placement, generated artifact equality, or compiled-Rust semantics | `ProductionDigestCodecs.{digestCodec_encode_exact,digestCodec_roundtrip,optionalDigestCodec_roundtrip,adapterEncodedCodec_roundtrip,encodeInstance_coordinates_exact}` | Production four-lane Goldilocks digest, compact fixed-one adapter, and the artifact-independent affine direct-call interface | `model-level` |
| `FPR-PRODUCTION-HASH-WRAPPER-BOUNDARY` | The frozen production `paperHash` returns the absent digest exactly when its current state is absent or its duplicated iteration/initial-state/running/program-counter carriers fail alignment. Its compact encoding is then exactly five zeros; an aligned present state is encoded as presence one followed by the four-lane XOut result. A canonical absent-current preimage proves that no always-present four-lane core can extensionally implement `paperHash` over its complete typed domain. Thus the presence/alignment wrapper is a retained semantic obligation, not optional serialization. This result emits no rows and does not supply state/running codecs, alignment gadgets, XOut serialization, typed Poseidon2 rows, either hash `CallRecipe`, physical placement, native parity, or collision resistance | `ProductionHashCallBoundary.{paperHash_eq_none_iff,paperHash_encoding_eq_absent_iff,alignedCurrent_encoding_exact,no_nonoptionalCoreRefines}` | Frozen fixed-one adapter hash and production optional-digest codec | `model-level` |
| `FPR-NUMERIC-TYPED-ROW-BRIDGE` | The repository's sparse numeric Goldilocks R1CS semantics now has one semantics-preserving translation into stable typed rows. Coefficients and assignment coordinates are reduced into the paper Goldilocks carrier; each source column is routed through an explicit `Nat -> ColumnId` map; one translated equation holds iff the numeric equation holds on canonical representatives; and this equivalence lifts to the complete ordered row list. `ownedRowsFrom` preserves every source-row occurrence, assigns caller-selected owner-local ordinals, proves duplicate-free row identities, and exposes the exact mapped column support. The bridge allocates no columns, selects no primitive, and supplies no activation gating, call footprint, emission receipt, generated-row equality, or Poseidon2/Rust correctness claim | `NumericRowBridge.{terms_eval_eq_residue_lcEval,row_columnIds,row_holds_iff,ownedRowsFrom_length,ownedRowsFrom_rows,ownedRowsFrom_ids_nodup,ownedRowsFrom_satisfies_iff}` | Numeric R1CS semantics and independent typed Goldilocks row semantics | `model-level` |
| `FPR-PRODUCTION-DIGEST-R1CS-REFINEMENT` | Satisfaction of the exact recursive-output owner, under canonical Goldilocks representatives and the constant-one wire, constructs one typed four-lane production digest. Its selected codec coordinates equal the physical `xOutColumns` in lane order and decode back to that digest. When the exact terminal delayed-link owner also holds, the same codec coordinates equal its `terminalFreshDigest`; producer and consumer therefore agree on one typed value rather than only on an untyped list. Independently, that selected digest is proved equal to the digest used by the captured terminal logical-link theorem, so the exact 257-coordinate terminal logical public input equals the paper-owned canonical `encodePublicInput` of the same value. The full-row theorem lifts both facts through the exact owner partition and identifies the final Construction-2 state's singleton fresh-public payload and logical public input with their corresponding canonical encodings of one digest. A constructive honest-completion theorem copies those 257 authoritative coordinates into the current isolated 270-row owner, assigns zero to exactly its thirteen new padding columns, proves all current rows satisfied, and identifies the current typed claim, final-state payload, captured logical public input, and codec round trip with that same digest. A separate bounded certificate from one live current two-step synthesis records rows `[9673389,9673659)`, the exact 527-column relabel, producer bits `16766..17021`, and fresh carrier `4090877..4091146`; Lean proves mapped-row equality, exact producer alignment, equivalence with the frozen logical paper link, the singleton source program, compact fixed-one `freshPublic = encodeInstance`, and the exact `Terminal.priorLinkAccepted` Boolean in the selected typed lowering, plus a composed current physical digest theorem. The digest in these equivalences is reconstructed from the output wires. This does not provide a generated aggregate for the whole current program, universal profile/batch placement, a production codec for the complete state, physical equality with the selected lowering recipes for `freshPublic`, `encodeInstance`, and `encodedEqual`, compiled-Rust semantics, or Poseidon2 collision resistance; this prior-link result is not the distinct terminal `freshCheck` call | `FPrimeFullHistoryProductionDigestCodec.{codec_values_eq_outputDigest,codec_roundtrip,decodedDigest_eq_logicalLinkDigest,terminalLogicalPublic_eq_encodePublicInput,rows_decode_exact_xOut,output_and_terminal_rows_decode_same_digest,output_and_terminal_rows_decode_linked_digest}`, `FPrimeFullHistoryCurrentTerminalLinkCompletion.{currentRows_of_snapshotLinkRows,completedAssignment_producerAligned,output_and_snapshot_rows_construct_currentPlainOwner}`, `FPrimeFullHistoryCurrentTerminalLinkPlacementSound.{mapped_rows_eq_generated,producerColumnsAligned,generatedRows_iff_sourceProgram,generatedRows_iff_freshPublic_eq_encodeInstance,generatedRows_iff_loweringPriorLinkAccepted,generatedRows_iff_logicalPaperLink,output_and_generated_rows_construct_currentPlainOwner}`, `FPrimeFullHistoryProductionDigest.{fullRows_finalState_latest_digest,fullRows_finalState_latest_digest_and_logical_public,fullRows_construct_currentPlainOwner,fullRows_and_currentTerminalPlacement_construct_plainOwner}` | Exact recursive-output, captured terminal logical-link, current isolated terminal-link, bounded current full-history terminal-link placement, and captured full-artifact row owners plus the selected production digest codec | `artifact-checked` |
| `FPR-PRODUCTION-ENCODE-INSTANCE-RECIPE` | The concrete compact `encodeInstance` coordinate map has a physical affine realization with caller-owned row/column identities: exactly six recurring rows, no temporary columns, one row per output coordinate, unique row identities, exact row support and ownership, active soundness, honest active completeness, and inactive satisfiability. A minimal production profile packages that same map as a complete typed `CallRecipe` with an exact nonoptional output/temporary/row receipt. Selection requires a supplied full lowering profile and proves alignment only for the optional-digest codec, compact-encoded codec, and `encodeInstance` footprint; it does not claim unrelated codecs or call implementations. No nonlinear fresh-public recipe, generated-row equality, or compiled-Rust theorem is supplied | `ProductionEncodeInstanceRecipe.{row_count,footprint_exact,rows_owned,row_ids_nodup,rows_supported,active_sound,active_complete,inactive_complete}`, `ProductionEncodeInstanceCallRecipe.{selected_footprint_exact,recipe_row_count,receipt_exact}` | Concrete digest codecs and the artifact-independent affine row/call compiler | `model-level` |
| `FPR-PRODUCTION-ENCODED-EQUALITY-RECIPE` | Equality of the compact production adapter values is selected independently of every unrelated codec and call implementation. Given a supplied full lowering profile whose encoded codec is the audited six-coordinate adapter codec, the permitted Goldilocks no-zero-divisor/inversion contracts, and exact footprint alignment, it forms a complete typed `CallRecipe`: exactly eighteen rows; auxiliary temporary bundles of widths six, six, and five; active soundness; honest completion; inactive satisfiability; physical ownership/support; and a nonoptional exact receipt. This does not instantiate state or fresh codecs, identify rows with a generated artifact, or prove compiled-Rust conformance | `ProductionEncodedEqualCallRecipe.{selected_footprint_exact,recipe_row_count,receipt_exact}` | Compact fixed-one adapter codec, paper field laws, and the selected equality compiler | `model-level` |
| `FPR-PRODUCTION-FRESH-PUBLIC-SINGLETON` | Specializing the native adapter to the paper's one fresh instance and the plain production carrier proves that compact `freshPublic = encodeInstance` is exactly the source-shaped 270-coordinate public-link predicate, exactly the selected six-phase program with definitional cost 273, and exactly the typed/logical HyperNova public-input equality. The theorem requires the adapter's fresh-link semantics to be the audited source checker and the ordered batch to be the singleton raw claim. It does not cover the current multi-fresh list deviation, physical rows for the 273 obligations, Rust-source or compiled-Rust semantics, NIFS, or a complete lowering profile | `ProductionFreshPublicSingletonBridge.{freshPublic_eq_encodeInstance_iff_sourceCheck,freshPublic_eq_encodeInstance_iff_program,freshPublic_eq_encodeInstance_reduces_to_logicalPaperLink}` | HyperNova Construction 2 one-fresh transition, compact fixed-one adapter, and the production plain-carrier source program | `model-level` |
| `FPR-PRODUCTION-FRESH-PUBLIC-SINGLETON-ROWS` | The complete typed plain-carrier serialization is injective: exact reads recover affine one, all lane-major body coordinates, and every padding coordinate, so its raw source check is extensionally equal to the typed checker. For one typed fresh claim, the existing receipt-owned 270-row terminal-link block is then equivalent in both directions to the raw source check, the six-phase source program, and compact `freshPublic = encodeInstance`; the current isolated physical artifact has the same theorem by kernel-checked row-list equality. The source program's cost is exactly `270 + 3 = 273`: expected length, `m_in`, and vector length remain host/source shape checks because the physical theorem starts from an already typed `Fin 1` claim. Producer alignment, Goldilocks canonicality, and constant one remain explicit premises. This does not cover multi-fresh batching, host or compiled-Rust semantics, surrounding full-history placement, or the producer output-encoding rows | `CanonicalPlainCarrierSerialization.{Carrier.coordinates_injective,serializeClaim_injective,sourceCheck_serializeClaim_iff_check}`, `FPrimeProductionFreshPublicSingletonRows.{sourceProgram_cost_eq_rows_plus_shape,selectedRows_iff_sourceCheck,selectedRows_iff_sourceProgram,selectedRows_iff_freshPublic_eq_encodeInstance,artifactRows_iff_freshPublic_eq_encodeInstance}` | HyperNova Construction 2 singleton fresh-public equality, selected plain terminal-link rows, and the isolated current artifact | `artifact-checked` |
| `CIR-FPR-PIDEC-SOURCE` | For the bounded active binary strict-PiDEC fixture at `kappa = 4`, Rust exports one contiguous 11,845-row source-R1CS interval and its exact decoded layout in 48 bounded shards. Lean proves coefficient-level equality, modulo sparse-term permutation only, with the independent reduced production compiler. Hence any canonical satisfying assignment with verifier-fixed constant one implies the typed active carrier and exact operational Section-7.5 paper acceptance. The reduced schedule replaces independent child-alphabet checks with the sound and honestly complete shared-sign binary split and omits only padded evaluation recomposition already implied by retained padding-zero rows, saving exactly 3,240 + 260 = 3,500 source rows. The sparse-data equality is compiler-trusted artifact evidence; the generic compiler soundness/completeness and paper bridge are separate model-level theorems. This does not cover production `kappa = 18`, final selective rows, certificate/FoldResult decoder identity, delayed `y_zcol`, or a global constraint lower bound | `PiDecStrictProductionCompiler.{sound_noAdv,complete_noAdv,combined_source_saving}`, `PiDecStrictProductionCompiler.PaperBridge.{accepted_refines_paper,active_source_rows_saved_3500}`, `SourceRefinement.SourceArtifact.{sourceRows_exact,compilerRows_exact}`, `SourceRefinement.sourceRows_imply_paperAccepted` | `enforce_dec_v_strict`, `R1csIvcRelation::audit_fixed_point_pi_dec_source_rows`, and `f_prime_pi_dec_source_lean_artifact` | `artifact-checked` |
| `CIR-FPR-PIDEC-ACTIVE-SEAM` | A typed strict-PiDEC carrier accepted by the independent paper bridge is exactly the active verifier's `FixedActive.resultOf` when two explicit physical decoder facts hold: the decoded parent point is the same certificate's derived PiRLC point, and every ordered decoded child commitment/public-input/evaluation payload is the certificate payload at that index. Claims-level active acceptance supplies the retained PiDEC tail, and the outgoing-state theorem rewrites the existing state check onto this exact decoded result while retaining delayed block-by-lane `y_zcol` as a separate sidecar computed from the same certificate. The theorem is kernel-only and introduces no digest authority. The current generated source artifact proves neither decoder fact, so this is a precise refinement seam rather than end-to-end Rust/R1CS closure or row-removal authority | `ActiveResultBridge.{familyPayloadBound_of_decoderFacts,decodedFoldResult_eq_resultOf,claimsAccepted_decodedFoldResult_eq_resultOf,claimsAccepted_outgoingState_rewrite}` | Typed strict-PiDEC decoder, active claims checker, and delayed-production state boundary | `model-proved` |
| `FPR-HASH` | Equal canonical `x_out` outputs imply equality of every direct, verifier-derived, or equality-pinned authority coordinate (including the source Nebula lane), or an explicit outer-hash/inner-lane-digest collision | `XOut.xOut_binding_or_collision` | Rust `compute_x_out`, `state_x_out_digest_with_mode` | `model-proved` |
| `TRACE-VALID` | Retained accepted invocations yield exact-step rich-edge reachability, nonzero batch schedules, exact split-counter refinement, and a pinned final state | `FPrimeTrace.accepted_trace_sound`, `FPrimeTrace.accepted_trace_valid_execution` | HyperNova compiler | `model-proved` |
| `CIR-SOUND` | For the captured 4,193,134-row `FPrimeFullHistoryRows.fullRows` artifact, every canonical satisfying assignment yields a two-edge `ValidExecution` relative to the row-decoded NIFS callback, with direct terminal validity, or the recursive/terminal PiRLC projection exposes its named `BadRoot`. This is R1CS-to-checklist correspondence for that snapshot, not current Rust conformance or paper-level NIFS soundness: the decoded PiCCS/PiRLC/PiDEC data must still be connected to `Composition.fold_knowledge_or_bad_event`. The current plain terminal-link owner has 270 rows while this snapshot contains 257, so `FPrimeFullHistoryTerminalLinkDrift.generatedSnapshotRows_ne_currentPlainOwnerRows` blocks promotion of the composite artifact to current production evidence. Scope is exactly the captured plain/stateless `[1,1]` profile, one recursive invocation, terminal fold, direct terminal CE, and `minimal-supported-bit-carrier` | `Nightstream.Assurance.FPrimeFullHistoryCircuit.fPrimeCircuit_sound_or_bad`; current-drift obstruction `FPrimeFullHistoryTerminalLinkDrift.generatedSnapshotRows_ne_currentPlainOwnerRows` | Captured full-history audit rows, base/recursive circuits, terminal fold, and direct terminal CE | `artifact-checked` |
| `CIR-FPR-FULL-HISTORY-FROZEN-STEPS` | For the same captured 4,193,134-row plain/stateless `[1,1]` artifact, exact `fullRows` satisfaction with canonical Goldilocks coordinates and verifier-fixed one yields acceptance of both concrete base and recursive transitions by the frozen fixed-one executable checker, or the existing full-history `BadEvent` (the reachable branch here is the named recursive PiRLC projection-root event). The adapter instantiates the frozen `Setup`, `Machine`, inputs, outputs, prior/fresh ordering, and exact row-decoded hash/NIFS services; no existential trace packaging remains between the rows and these two checker acceptances. This is bounded R1CS-to-frozen-step soundness for the checked-in snapshot. It does not identify those rows with the obligation-10 selected canonical IR, prove current Rust or compiled-Rust semantics, construct honest assignments, refine the terminal checker, or bound the named event | `FPrimeFullHistoryCircuit.{exactSteps_of_ownerRows_or_bad,exactSteps_of_fullRows_or_bad}`, `FPrimeFullHistoryCanonicalSteps.fullRows_imply_frozenSteps_or_bad` | Captured full-history rows, exact full-history step witnesses, and the universal fixed-one native/paper adapter | `artifact-checked` |
| `CIR-OBLIGATION-TREE` | The captured full-history generated hierarchy expands to one root, eight nested parent partitions, and 61 deepest leaves. Its 59 materialized half-open ranges are duplicate-free and cover exactly all 4,193,134 rows; the remaining two leaves are exact zero-cost organizational nodes. Every parent interval is exactly covered by its immediate children and its cost is their exact sum. Every physical row has exactly one deepest owner, no owner contains a row outside the program, and every leaf has typed mathematical-obligation, Rust-emitter-module/stage, and checked Lean soundness/completeness/necessity routes. Formula-only estimates have a separate range-free type. The theorem explicitly retains the captured-257/current-270 terminal-link drift, so this is fixed-snapshot ownership rather than current-production row equality | `FPrimeFullHistoryObligationTree.{every_parent_cost_exact,exact_hierarchy_shape,materialized_leaf_ranges_cover,materialized_leaf_cost,zero_cost_nodes_exact,exact_leaf_census,materialized_leaf_ranges_nodup,every_materialized_row_has_exactly_one_leaf,no_row_outside_materialized_leaves,every_lean_evidence_checked,every_leaf_cross_layer_mapped,obligation_tree_retains_terminal_drift}` | Captured schema-2 full-history manifest (`cccfb8ab…a481ca`), existing CIR-SOUND/CIR-COMPLETE theorems, Rust stage paths, and terminal-link drift obstruction | `artifact-checked` |
| `CIR-FPR-RECURSIVE-MANIFEST` | The exact 2,640,071-row plain/stateless steady-recursive profile is partitioned into eight contiguous top-level owners; its 2,572,208-row NIFS block is exactly partitioned into PiCCS, PiRLC, PiDEC, and point binding. The PiRLC projection census pins one 1,892-row shared block and 31 equal-shape 1,916-row identities with 15 pairs each. This ownership-only profile is distinct from the one-recursive-invocation `[1,1]` artifact closed by `CIR-SOUND` | `FPrimeRecursiveManifest.topLevel_covers_program`, `FPrimeRecursiveManifest.nifs_covers_block`, `FPrimeRecursiveManifest.projection_census_shape`, `FPrimeRecursiveCircuit.decodedChecks_sound` | `enforce_f_prime_recursive_step_circuit`, `enforce_nifs_v_circuit_with_transcript_inner` | `artifact-checked` |
| `CIR-FPR-SOURCE-CENSUS` | The committed fixed-F-prime base and recursive source traces are exact positive partitions with unique fixed constant-one ownership and exhaustive role totals. Their ordinary-private counts are 3,050 and 154,747. Per-field-41 candidates therefore have conditional floors 125,050 and 6,344,627; the recursive branch alone cannot fit in 1,000,000 coordinates under that premise. Selector sharing can remove base/recursive additivity, but this source-only theorem does not describe encoded/CE coordinates or rule out a different intra-recursive encoding | `FPrimeRecursiveSourceRoleCensus.base_data_check`, `recursive_data_check`, `combined_perField41_width_floor`, `recursive_one_million_perField41_budget_is_no_go` | `gadget_native::{source_schedule,source_manifest}` and the full-relation source-role generator | `artifact-checked` |
| `CIR-FPR-CARRIER270-PADDING` | For the exact three-arm F-prime-width selective-compiler fixture, Rust exports the prepared layout consumed by emission and materializes all thirteen final public-padding matrix rows. Lean proves the generated `257/270` widths, padding range `257..270`, selector range `270..273`, private-alignment range `273..311`, and branch start `311` equal the independent `FPrimeCarrier270`/`SelectiveLayout` formulas. Each raw row decodes fail-closed, is classified from coefficients (not its family tag), and its complete 27-term residual vanishes iff the corresponding typed carrier zero pin holds under an explicit public-prefix connectivity premise. This is a public-carrier slice, not the complete fixed-point F′ relation, private-column refinement, CCS/CE membership proof, Ajtai alignment, or NIFS soundness | `ProductionCarrier.generated_layout_refines_model`, `generated_padding_rows_shape`, `generated_padding_row_iff_zeroPin`, `generated_padding_row_canonical_complete` | `prepare_selective_layout`, final selective matrices and exclusive row ledger; `f_prime_selective_snapshot::selective_carrier_270_lean_artifact_matches_compiler` | `artifact-checked` |
| `CIR-FPR-FIXED-POINT-CARRIER270-PUBLIC-DECODER` | For the bounded stabilized fixed-point profile used by the active projected-emitter artifact, Rust exports the complete public-coordinate owner schedule from the same prepared layout: encoded column zero is verifier-written one, columns `1..257` directly own source fields with identical indices, and columns `257..270` are compiler-inserted zeros. The 270 proof-free records are partitioned into disjoint 256- and 14-record certificates. Lean proves exact generated data, total coverage, ordered identity, and equality of the interpreted vector with both the independent `expectedPublicInput` and `projectPublicInput (assignment ...)` under the explicit source constant-one condition. This closes the prior public-prefix connectivity premise for this bounded fixed-point profile only; private assignment/matrix decoding, CCS/CE membership, Ajtai key alignment, and `y_zcol` authority remain open | `PublicDecoder.generatedCoordinate_exact`, `PublicAssignment.artifactPublicInput_eq_expectedPublicInput`, `PublicAssignment.artifactPublicInput_eq_projectPublicInput` | `project_rows_with_source_provenance_with_alignment`, the prepared selective layout, and `active_selective_fixed_point_projection_artifact_matches_retained_certificate` | `artifact-checked` |
| `CIR-FPR-CARRIER270-PUBLIC-WRITE-TRACE` | The active steady-recursive post-PiDEC audit checks each of the 270 public coordinates against the live builder source, normalized public assignment, and packed committed witness, while pinning the emitted `257 + 13` owner profile. Its exporter carries normalized source, width, centeredness, and alias policy in two disjoint 135-record proof-free shards. Lean fail-closed decodes both shards, proves their exact canonical schedules, constructs the one-arm exporter certificate and fixed execution assignment, derives constant one and `ActivePublicWritesBound` from the generated runtime values, and reconstructs the typed Carrier270 public projection without a caller-provided trace, `PublicSourceDataflow`, or live-write premise. This closes the fixed-profile active public prefix, not the private assignment, complete matrices, CCS/CE membership, Ajtai-key alignment, or permission to remove rows | `ProductionPublicWriteTrace.productionProfile_exact`, `productionTrace_certificate`, `production_projectPhysical270_execute_eq_projectPublicInput`, `ActivePublicWriteExecution.execution_activePublicWritesBound`, `execution_normalizedPublicInput_eq_projectPublicInput` | `capture_post_pi_dec_execution`, `R1csIvcPublicWriteAudit`, generated `Execution/PublicWrites` shards, and the focused mutation/drift test | `artifact-checked` |
| `CIR-FPR-FIXED-POINT-CARRIER270-PUBLIC-PADDING` | For the same bounded stabilized fixed-point profile, Rust projects all 13 public-padding rows directly from the final thirteen-port emitter. Lean checks exactly 13 proof-free raw records, proves unique physical ownership of emitted rows `1423540..1423553`, decodes every row through the shared fail-closed decoder, and derives from coefficients—not the family label—that row `i` realizes `-(z[0] * z[257+i])`. Under the separately owned constant-one fact, satisfaction is exactly zero for columns `257..270`; an explicit zero extension is honestly complete. With an explicit assignment-coordinate agreement, these generated rows are equivalent to the independent typed `FixedPublicPadding` obligation, and honest `FPrimeCarrier270.assignment` values satisfy them. This closes only these 13 public zero pins in the bounded profile, not the remaining private decoder, complete matrices, CCS/CE membership, Ajtai alignment, or `y_zcol` authority | `PublicPadding.generated_row_has_unique_offset`, `PublicPaddingRefinement.expectedRow_decode_exact`, `expectedRow_residual_eq`, `generatedRowsSatisfied_iff_padding_zero`, `withPublicPaddingZero_satisfies`, `generatedRowsSatisfied_iff_fixedPublicPadding`, `generatedRowsSatisfied_of_typedAssignment`, `generated_raw_row_refines` | `project_rows_with_source_provenance_with_alignment`, the final selective matrix-term stream, and `active_selective_fixed_point_projection_artifact_matches_retained_certificate` | `artifact-checked` |
| `CIR-FPR-FIXED-POINT-CARRIER270-PRIVATE-PADDING` | For the same bounded stabilized fixed-point profile, Rust projects all 38 private-alignment rows directly from the final thirteen-port emitter. Lean checks exactly 38 proof-free raw records, proves unique physical ownership of emitted rows `1423553..1423591`, decodes each row through the shared fail-closed decoder, and derives from coefficients—not the family label—that row `i` realizes `-(z[0] * z[273+i])`. Under the separately owned constant-one fact, all rows are satisfied exactly when columns `273..311` are zero; an explicit zero-extension constructs a satisfying assignment. This closes only the compiler-owned private-alignment interval, not later private-coordinate ownership, the complete final matrices, CCS/CE membership, Ajtai alignment, or `y_zcol` authority | `PrivatePadding.generated_row_has_unique_offset`, `PrivatePaddingRefinement.expectedRow_decode_exact`, `expectedRow_residual_eq`, `generatedRowsSatisfied_iff_padding_zero`, `withPrivatePaddingZero_satisfies`, `generated_raw_row_refines` | `project_rows_with_source_provenance_with_alignment`, the final selective matrix-term stream, and `active_selective_fixed_point_projection_artifact_matches_retained_certificate` | `artifact-checked` |
| `CIR-FPR-FIXED-POINT-CARRIER270-RING-PADDING` | The current stabilized relation's final width alignment is owned separately from the 38 private-prefix pins. Rust exports exactly 28 final `RingPadding` rows from the actual selective emitter. Lean checks the proof-free rows, their unique emitted interval `14944191..14944219`, their exact column interval `11437010..11437038`, and their source run owner; coefficient decoding proves row `i` realizes `-(z[0] * z[11437010+i])`. Satisfaction under the separately owned constant-one cell is equivalent to zero in every final padding column, and explicit zero extension is honestly complete. These physical columns finish the last 54-lane packed-witness block; they are distinct from the ten verifier-computed virtual lanes used to extend a 54-value lane polynomial to a 64-point Boolean cube | `RingPadding.generated_rows_exact`, `generated_row_has_unique_offset`, `RingPaddingRefinement.expectedRow_decode_exact`, `expectedRow_residual_eq`, `generatedRowsSatisfied_iff_padding_zero`, `withRingPaddingZero_satisfies`, `generated_raw_row_refines` | `SelectiveEmittedRowFamily::RingPadding`, `carrier_ring_padding.rs`, generated `RingPaddingRows.lean`, and the fail-closed drift gate | `artifact-checked` |
| `CIR-FPR-FIXED-POINT-CARRIER270-SELECTORS` | For the same bounded stabilized fixed-point profile, Rust projects the three selector-domain rows and the selector-total row directly from the final thirteen-port emitter. Lean checks exactly four proof-free raw records, proves unique physical ownership of emitted rows `0`, `1`, `2`, and `1423539`, decodes every row through the shared fail-closed decoder, and derives from coefficients—not family labels—that the first three rows are the Boolean residuals for columns `270..273` and the fourth is the sum-to-one equation. Under separately owned constant-one and Goldilocks primality premises, satisfaction is exactly selector Booleanity plus sum-to-one; an explicit unit-selector extension is honestly complete. This does not cover the selector-gated retained rows and does not authorize retaining or removing the Boolean rows | `Selectors.generated_row_has_unique_owner`, `SelectorRefinement.expectedSelectorRow_decode_exact`, `expectedTotalRow_decode_exact`, `expectedSelectorResidual_eq`, `expectedTotalResidual_eq`, `generatedRowsSatisfied_iff`, `withUnitSelectors_satisfies`, `generated_raw_row_decodes` | `project_rows_with_source_provenance_with_alignment`, the final selective matrix-term stream, and `active_selective_fixed_point_projection_artifact_matches_retained_certificate` | `artifact-checked` |
| `CIR-FPR-PICCS-DELAYED-RAW-CHILD-CONTRACT` | The fixed 270-coordinate delayed-projection input is materialized in canonical child-major/column-major order directly from `SplitNc.Sources.Data.runningAssignments`. Lean proves exact two-dimensional lookup, equality with the existing canonical running-source injection, and coverage by the derived 512-column/64-lane diagnostic domain. Specializing `OldPointBinding` to this table derives `OldPointSumcheckRelation` or the named degree-53 projection `BadRoot`. This remains a useful model leaf, not the production authority theorem: the active production verifier now reads ordered raw `CcsWitness.Z`/running `Mat` tables through the 19-block/6-lane path, while the older `running_output_evaluation` sidecar helper remains an unused diagnostic export. Production therefore refines through `PackedYZcolBoundAtBlock`, not by coercing this fixed-270 relation across domains | `ProductionRawChildren.Fixed270.authoritativeRunningChildren_getD`, `authoritativeRunningChild_eq_orderedAssignment`, `assignmentsFitColumnDomain`, `acceptedProjectionIdentity_implies_oldPointRelation_or_badRoot` | Active owner: raw witness tables and `BlockLaneNcOracle`; `CeClaim.y_zcol` is transport only and is not an authority source | `model-proved` |
| `CIR-FPR-PICCSNC-FLAT-DELAYED-RESIDUAL` | Diagnostic only: over the bounded 270-coordinate public-prefix profile, Lean defines a nine-column-plus-six-lane delayed residual from typed assignments and proves its internal cube, terminal, quartic-degree, residual-weight, projection-root, and fixed-phase SumCheck decomposition. It is not a production authority route. The independent kernel theorem `flatColumnProjection_not_actionHom` proves that flat column evaluation does not commute with the RingF action required by production `y_zcol`, so no Rust verifier or active theorem may refine through this model | `FlatCombinedNc.rawValueAt_eq_radixCombinedRawZ`, `combinedHypercubeSum_eq_ordinary_add_weightedProjection`, `combinedAtPoint_eq_terminalRhs`, `accepted_implies_truth_and_oldPointRelation_or_badEvent`; exclusion theorem `FlatColumnAction.flatColumnProjection_not_actionHom` | No production owner; retained only as a diagnostic counterexample/model | `model-proved` |
| `CIR-FPR-PICCSNC-PRODUCTION-DOMAIN` | The current stabilized fixed-point artifact has exactly 14,944,219 rows, 11,437,010 unpadded coordinates, and 11,437,038 aligned physical coordinates, hence 211,797 complete 54-lane blocks. The relation row cube uses 24 variables. The versioned production NC transcript deliberately uses 19 block rounds plus six lane rounds; 18 block bits already cover this artifact, so 19 is a protocol-format choice with spare capacity rather than a minimality claim. Each block has 54 physical values and ten verifier-computed virtual zero lanes. The 28 final physical alignment coordinates are separately enforced and exactly owned by generated rows. A bounded release regression materializes the complete relation and checks the dimensions against the compiler audit | `ProductionDomain.artifact_relationRows`, `artifact_relationColumns`, `artifact_unpaddedCoordinates`, `semanticShape_blockCount`, `blockLaneDomain_covers`, `blockLaneRoundCount`, `liveLaneCount_exact`, `virtualLaneCount_exact`, `rowVariables_minimal`, `eighteenBlockVariables_cover`, `laneVariables_minimal` | Fixed-point width census, generated ring-padding rows, `active_fixed_point_shape_fits_guard_after_accumulator_ce_compression`, and explicit complete materialization | `artifact-checked` |
| `CIR-FPR-PICCSNC-FULL-Z-DECODER` | For the current stabilized bounded profile, Rust exports one compact full-`Z` layout certificate: exact width 11,437,038, fourteen children, matrix shape `54 × 211,797`, and 64 proof-free Boolean-lane records. The generator exercises `decode_superneo_coeffs_from_witness_mat` and all 108 one-hot cells of two complete blocks through `CcsInstance::from_low_norm_assignment`, Ajtai commitment recomputation, and `Commitment.data[row*54+lane]`; mutation tests reject dimension, lane, and commitment-length changes. Lean proves the logical-coordinate, fourteen-child, and commitment-data bijections and connects every generated live cell plus ten computed-zero virtual lanes to the typed production source table. The bounded fixture uses κ=4 while production uses κ=18. This checks exact coordinate geometry and commitment-data ordering; it does not export the fourteen runtime witness projections at `pending.old_block`. Native PP coefficient equality, accepted opening/extraction, and binding failure remain explicit external cryptographic boundaries | `PackedWitnessDecoder.logicalColumnAt_bijective`, `childLogicalColumnAt_bijective`, `commitmentProbeColumnAt_exact`, `productionCommitmentDataIndex_bijective`, `PackedWitnessCommitment.productionFlattened_matrixCommit_at`, `unpack_at_generatedAddress`, `production_live_eq_generatedWitnessCell`, `production_padding_eq_zero`, `generated_full_decoder_and_lane_partition` | Full-`Z` generated layout, actual constructor/commitment probes, raw block projector, and the generated-artifact drift gate | `artifact-checked` |
| `CIR-FPR-PICCSNC-BLOCK-DELAYED-ACTIVE` | Lean states the versioned raw block×lane combined-NC path, its 25 quartic rounds, the explicit `batchWeight = 0` residual-root branch, the degree-53 producer-beta root, fixed-degree SumCheck collisions, and the one-fold base/recursive/terminal transfer. The model-level trace reaches predecessor `PackedYZcolBoundAtBlock` and Construction-2 semantics without a generic `outputUnbound`, while retaining `yRingUnbound` and named algebraic, state, opening, and commitment failures. The fixed production terminal exporter emits the exact indexed old-block program and physical placement. Its current factored form has 24,185,169 physical rows and, separately, 24,185,061 allocated/committed columns over `[22,834,865, 47,020,034)`; the explicitly retained pre-factorization baseline was 25,243,884 rows and 25,243,776 columns. Lean proves that factoring the common final block term is semantically exact, saves exactly 1,058,715 recurring rows and columns, and has an honest satisfying assignment. The artifact binds the pending old point and parent, fourteen ordered full-`WitnessMat` bases shared with terminal Ajtai openings, the recursive selector, and the profile identity. Lean also proves exact compiler/generated row equality, unique physical ownership, the generated column-map inverse, and a transparent assignment decoder. `productionRows_projectionOpeningAccepted` therefore derives terminal projection-opening authority from actual generated-row satisfaction plus terminal CE, without a caller-provided execution audit, desired projection, child `CeClaim.y_zcol`, digest authority, semantic acceptance, generic output-unbound premise, or raw-old-block execution-refinement failure. The fixed terminal placement/dataflow is artifact-checked and Rust-conformant; `ActiveTrace.Trace.terminalRawProjectionRowsChecked_implies_terminalChecked` lifts the exact rows to the complete terminal checker. The authority headline is the packed-only theorem `ActiveTrace.Trace.terminalRawProjectionRows_imply_baseAndAllPacked_or_parentOpeningFailure`: it yields `BaseNc ∧ AllPacked` or the typed parent-opening failure tree, with explicit base, recursive-predecessor, and terminal handling. The stronger `AllPaper` wrapper separately retains independent public-input, source-product, child-opening, key-refinement, and `y_ring` failures and is not used to claim those gaps closed. Exact row replacement is authorized only for this terminal raw-old-block family; recursive-circuit parent authority and broader end-to-end paper/NIFS refinement remain open | `CombinedNc.Acceptance.expectedRoundsRepresentable`, `residualWeightIdentity_exact_iff`, `TerminalRawOldBlockProjectionArtifact.productionWeightedPrefixProjection_factorFinalRound`, `productionFactoredFinalRoundRowSavings_exact`, `productionFactoredFinalRoundAllocatedColumnSavings_exact`, `honestArtifactRowsSatisfied`, `productionArtifactContract`, `productionPhysicalRow_unique`, `productionRows_projectionOpeningAccepted`, `ActiveTrace.Trace.terminalRawProjectionRowsChecked_implies_terminalChecked`, `ActiveTrace.Trace.terminalRawProjectionRows_imply_baseAndAllPacked_or_parentOpeningFailure` | `RawOldBlockProjectionProgram`, the active post-PiDEC execution audit, terminal raw openings, generated `RawOldBlockProjectionRowAt`/`ProductionEmitterLayout`, and the still-open recursive-circuit parent refinement | `model-proved` |
| `CIR-FPR-PICCSNC-RUNNING-X-PREFIX-DECODER` | The generated 14-by-270 artifact uniquely maps each stabilized incoming `running[child].x` public coordinate through the source arm to one final selective column. This is `CeClaim.X`/public-prefix provenance only. It does not decode `CcsWitness.Z`, `CeWitness.Z`, a private suffix, or the complete production assignment. Earlier descriptions of this artifact as raw-child assignment authority are retracted; its bounded artifact facts remain valid | `RawRunningDecoder.Exact.generatedChunkExact`, `sourceArmColumn_injective`, `finalColumn_injective`; the bounded `ArtifactRefinement.decodedData` is a fixture, not production authority | `R1csIvcRawRunningAssignmentAudit` explicitly owns an incoming running-X coordinate | `artifact-checked` |
| `CIR-FPR-PICCSNC-FRESH-X-PREFIX-DECODER` | For the same bounded stabilized profile, Rust exports exactly one 270-coordinate `prior_link.fresh_public_inputs[0]` source in two proof-free shards of 256 and 14 records. Lean proves complete ordered coverage, the consecutive normalized source-column formula, unique logical ownership, and fail-closed selective-disposition range validity. A separate conditional theorem transports explicit coordinate-value bindings plus direct field-value dataflow to exactly the fresh source's `publicInput` field. Neither the dispositions nor the column map establish field values: the constant-one, 256 bit-link, and thirteen padding rows remain open, as do the rest of `FreshSourceBound`, full witness `Z`, CCS/CE membership, and commitment authority | `FreshSourceDecoder.Exact.chunk_lengths_and_coverage`, `record_formula`, `sourceColumn_has_uniqueLogicalOwner`, `sourceColumn_injective`, `records_all_wellFormed`; `FreshSourceDecoder.Refinement.coordinateValueBindings_and_dataflow_imply_freshPublicInput` | `R1csIvcFreshSourceAssignmentAudit` owns the bounded source-column/disposition export; exact prior-link binding rows remain to be exported | `artifact-checked` |
| `CIR-FPR-PICCSNC-PACKED-WITNESS-LAYOUT` | Lean states the full-witness layout `Z[lane,block] = assignment[block*54+lane]`, proves packing/unpacking inverses and fresh-tail zeros, and constructs the combined-NC running source table from complete packed witnesses. For the current bounded domain, the compact generated decoder proves coordinate ownership for all fourteen `54 × 211,797` matrices; every live combined-NC leaf is its full packed-`Z` cell, the ten virtual Boolean lanes are verifier-computed zero, and the partition is exactly 54+10. Generated coefficient rows separately force the 28 final physical alignment coordinates to zero. Constructor/commitment probes check 108 one-hot cells and `row*54+lane` order; Lean characterizes raw-child binding failure as an exact packed-matrix commitment mismatch rather than digest or sidecar disagreement. The generated terminal execution handoff reads those same ordered raw `WitnessMat` tables at the pending old point, maps them through the exact physical emitter columns, and proves that satisfaction of all 24,185,169 factored raw-old-block rows yields `ProjectionOpeningAccepted`; no child sidecar or digest supplies authority. Semantic equivalence and honest completeness authorize the exact 1,058,715-row/column replacement relative to the pre-factorization baseline only. The fixed terminal layout/dataflow is artifact-checked/Rust-conformant. Its packed-only model composition yields `BaseNc ∧ AllPacked` or the typed parent-opening failure tree. Recursive-circuit authority, native PP coefficient equality, accepted Ajtai opening/extraction, `y_ring` closure, the stronger paper wrapper, and other combined-NC/state/terminal row families remain open | `PackedWitness.unpack_pack`, `pack_unpack`, `coordinatesAligned_iff_unpack_eq`, `PackedWitnessDecoder.childLogicalColumnAt_bijective`, `productionCommitmentDataIndex_bijective`, `PackedWitnessCommitment.productionFlattened_matrixCommit_at`, `TerminalRawOldBlockProjectionArtifact.productionWeightedPrefixProjection_factorFinalRound`, `honestArtifactRowsSatisfied`, `productionRows_projectionOpeningAccepted`, `ActiveTrace.Trace.terminalRawProjectionRowsChecked_implies_terminalChecked`, `ActiveTrace.Trace.terminalRawProjectionRows_imply_baseAndAllPacked_or_parentOpeningFailure` | `CcsWitness.Z`, running `Mat`, raw block projector, native terminal openings, generated full-`Z`/ring-padding/raw-old-block artifacts | `artifact-checked` |
| `CIR-FPR-SELECTOR-COMPOSITION-SLICE` | For the same exact three-arm compiler fixture, Rust materializes the three selector-domain rows, selector-total row, and one representative retained row from final matrices. Lean classifies them solely from decoded coefficients: selector rows realize the Boolean residual; under an explicit constant-one premise, the total row vanishes iff `s₀+s₁+s₂=1`; and the retained row is exactly `s₀` times its decoded source residual. The independent selector theorem proves sum-to-one plus complete branch gating is sound without Boolean selectors, but this artifact covers only one retained row and therefore does not yet authorize removal of the three selector-domain rows | `Artifact.Row.SelectorComposition.selectorGap_eq_zero_iff_total`, `residual_eq_gatedSource`; `SelectorComposition.ArtifactRefinement.generated_selector_rows_shape`, `generated_total_row_iff_selectorTotal`, `generated_gated_row_residual`, `generated_gated_source_residual` | final selective matrices and exclusive row ledger; `f_prime_selective_snapshot::selective_carrier_270_lean_artifact_matches_compiler` | `artifact-checked-slice` |
| `CIR-FPR-PIRLC-YZCOL-SELECTIVE-SLICE` | For the bounded fixed-point PiRLC `y_zcol` fixture, Rust exports the exact source program, compact A/B/C rows, source/derived column provenance, ownership for every emitted polynomial-evaluation, product-sum, and retained-check row, and the separately eliminated linear-definition program from the same structure-term emission path used by final matrices. The generator rejects any derived-product recurrence whose base is nonzero or whose predecessor or ordered factors differ from the actual witness encoder. Lean decodes the artifact fail-closed, proves that the normalized decoded derived-output stream exactly equals the exported witness registry, proves unique physical-row ownership, proves exact coefficient refinement for rewrite and retained rows, and proves deterministic column provenance and satisfaction for the eliminated definitions. Artifact computation is limited to proof-free coefficient records or Boolean summaries in exact ordered partitions whose members are kernel-proved to stay within the certificate bound; generic kernel theorems lift those compact checks to the typed artifact. Lean then proves two directions: satisfying every compact row reconstructs the source projection obligations when the externally owned steady selector and constant-one cells are fixed; and deterministic source execution from a constant-one seed plus the two direct sampled-wire equations constructs a canonical satisfying compact assignment. The reverse theorem assumes neither source-row satisfaction nor decoded acceptance, but it is still a focused projection-execution completeness result rather than a full paper-honest PiCCS/NIFS transition. With explicit upstream producer-column and `y_zcol` message/source bindings, the soundness direction composes with the typed projection bridge to yield the intended message aggregate or the named projection bad-root event. This is only the fixed fixture: selector enforcement, upstream source authority, the complete production carrier, CCS/CE membership, and global row-removal authority remain outside the claim | `Selective.Materialized.Checked.uniqueOwner`, `Selective.RewriteBridge.decodedDerivedRecurrenceRegistryExact`, `Selective.RewriteBridge.Coefficients.rewriteCoefficientsMatch_of_shape_check_true`, `Selective.RewriteBridge.Coefficients.rewriteCoefficientChunksExact`, `Selective.RewriteBridge.rewriteCoefficientsExact`, `Selective.RewriteBridge.retainedCoefficientsExact`, `Selective.SourceProgram.compilerSourceOutputDefinitions_exact`, `Selective.SourceProgram.sourceAssignmentCompilerDefinitionsHold`, `Selective.Soundness.selectedRows_imply_rowsSatisfied`, `Selective.Soundness.selectedRows_decodedOutput_eq_messageAggregate_or_badRoot`, `Selective.HonestAssignment.materializedDerivedWitnessRecurrencesHold`, `Selective.HonestAssignment.TerminalSemantics.exists_selectedRows_of_honestSource` | `R1csIvcRelation::audit_fixed_point_y_zcol_rows`, selective structure-term lowering, and the generated source/compact-row artifacts | `artifact-checked` |
| `CIR-PIRLC-PROJECTION` | Every canonical assignment satisfying the exact 714-row production helper implies the complete 107-coefficient `BatchAccepted` predicate. Every accepted bounded projection batch is then coefficient-wise exact or exposes a nonzero error polynomial vanishing at beta. Honest, bad-root, and row-forgery regressions exercise the universal theorem boundary; a reusable theorem lifts the semantics across a complete shared-definition census | `PiRLCProjection.exactRows_imply_batchAccepted`, `ProjectionTrace.census_batchAccepted`, `ProjectionCheck.batchAccepted_implies_exact_or_badRoot`, `FPrimeRecursiveCircuit.projectedChecks_local_sound_or_badRoot` | `enforce_ring_action_projection_batch` and the PiRLC beta schedule | `artifact-checked` |
| `CIR-SIS-SHARED-SLOTS` | For the exact isolated one-field gadget-native fixture, the generated schema-3 artifact binds exact LEFT/RIGHT roles and polynomial terms for 20 residual-pair rows, one ordinary centered tail, and 82 retained product rows: 103 physical rows encoding 123 logical retained obligations. Conditional on `ProjectiveSevenNonresidue`, verifier-fixed one, and the structural field/digit alias, those rows accept exactly when the decoded assignment satisfies all 124 canonical shifted-ternary rows. The 123 omitted obligations are exactly 41 negative-bitness, 40 internal-borrow-bitness, 41 negative-support, and one reconstruction consequence; no digest carries authority. Open qualification: the SuperNeo nonresidue theorem is not yet an imported refinement bridge for Nightstream's residue carrier | `ShiftedTernarySharedSlots.artifactGateAccepts_iff_productionAccepts`, `ShiftedTernarySharedSlots.artifactGateAccepts_iff_canonicalRows`, `ShiftedTernaryCenteredZero.centered_zero_unique` | `gadget_native::{balanced_ternary,shared_slots}`, isolated shifted-ternary generator and runtime row reader | `artifact-checked` |
| `CIR-SIS-REDUCED-CORE` | Under canonical Goldilocks values, verifier-fixed one, and an explicit lowering-owned field/digit alias, the 123-obligation model core (41 centered-unit, 41 negative-definition, 41 borrow-transition) is equivalent to all 124 canonical opening rows. Negative bitness/support, internal borrow bitness, and reconstruction are proved consequences; the schema-3 production bridge separately and conditionally justifies their exact physical realization | `ShiftedTernaryReducedCore.reduced_iff_canonicalRows`, `ShiftedTernaryReducedCore.CanonicalWitness.reducedCore_complete` | model-level shifted-ternary opening semantics | `model-proved` |
| `CIR-U64CANON` | Satisfying the exact exported canonical-u64 gadget rows forces boolean bits that recompose over the integers to the decomposed element's canonical value | `canonicalU64_sound` | `decompose_var_to_u64_bits` generated rows | `artifact-checked` |
| `CIR-U64INC` | Satisfying the exact exported u64-increment rows forces the output word to equal the input word plus one over the integers and rejects wraparound | `u64Increment_sound` | `enforce_u64_increment` generated rows | `artifact-checked` |
| `CIR-U64ADD` | Satisfying the exact exported u64-add rows forces the output word to equal both input words' integer sum and rejects wraparound | `u64Add_sound` | `enforce_u64_add` generated rows | `artifact-checked` |
| `CIR-FPR-COUNTER` | Satisfying the exact production-used recursive F' counter block binds source words, fixes the batch cardinality, advances both counters over the integers, and rejects wraparound | `FPrimeCounterSound.fPrimeCounter_sound` | F' input-binding and recursive-counter generated rows | `artifact-checked` |
| `CIR-FPR-TERMINAL-LINK` | For one isolated plain-profile trailing fresh claim, the exact production block has 270 rows: one affine-one equality, 256 ordered equalities to the last producer step's `x_out` bits, and thirteen carrier-padding zero rows. Lean proves both directions between those rows and the complete semantic contract. Under the explicit coordinate proposition `ProducerAligned`, row satisfaction is equivalent to the typed 270-coordinate check and hence to zero completion of the paper-owned 257-coordinate equality. The separate exact 532-row output-encoding owner reconstructs one physical `BitVec 64` per lane, proves its integer value is the canonical digest column, and therefore proves all 256 producer bits equal the independent typed `enc_inst` encoder. Composing the owners eliminates `ProducerAligned` and leaves only `ProducerColumnsAligned`. A bounded certificate exported from one live two-step current full-history synthesis now closes that map for its exact `terminal.latest_link` range: rows `[9673389,9673659)`, producer columns `16766..17021`, fresh columns `4090877..4091146`, and all 527 local-column mappings. Lean proves the 270 mapped isolated rows equal the generated range, current-row satisfaction is equivalent to the frozen logical paper equality, the singleton source program, and compact fixed-one prior-public equality, and the physical current claim shares the selected typed output digest and codec. The digest is reconstructed from the recursive output owner rather than carried as authority. `FPrimeTerminalLinkBatch` lifts the selected block artifact-independently to every batch size with exact `270 * batchSize` row/public-column ownership, soundness, completeness, and paper reduction. Production's generated three-instruction schedule equals the typed schedule and is accepted by a fail-closed Lean compiler only when it expands to the complete selected receipt order. For every batch size that compiler returns exactly `FPrimeTerminalLinkBatch.rows`; for the generated singleton schedule its acceptance is exactly the selected typed Terminal `priorLinkAccepted` Boolean under explicit digest, claim-order, source-link, and producer alignment. Relabeling that checked singleton compiler output is now proved exactly equal to the generated current full-history range, and source-program acceptance on the pulled assignment is equivalent both to satisfaction of that range and to the output-derived typed `priorLinkAccepted` Boolean. A separate two-claim drift gate proves the literal 540-row/797-column capture is exactly this checked compiler output and has unique receipt ownership. These source-program and bounded physical results are not universal compiled-Rust loop semantics and do not identify this prior-public relation with terminal `freshCheck`. The checked-in full-history snapshot remains stale: its 257-row prefix differs from the current owner by exactly the thirteen zero pins, and no theorem promotes it to current aggregate evidence. The constructive local completion and the bounded live-placement theorem are intentionally separate. Still missing are physical equality with the selected lowering recipes for `freshPublic`, `encodeInstance`, and `encodedEqual`, a generated aggregate for the whole current program or universal profile/batch placement, formal compiled-Rust interpreter semantics, and terminal-checker integration; the source-program XOut schedule and outgoing `EncInst` coordinate alignment are tracked by `CIR-FPR-NATIVE-XOUT-PROGRAM` | `FPrimeEncodingCanonicalBits.publicBit_eq_encodedBit`, `FPrimeTerminalLinkSound.{fPrimeTerminalLink_sound,fPrimeTerminalLink_complete,satisfies_iff_holds}`, `FPrimeTerminalLinkCanonicalRefinement.{check_of_satisfies,satisfies_of_check,satisfies_iff_logicalPaperLink,producerAligned_of_encodingRows,satisfies_iff_logicalPaperLink_of_encodingRows}`, `FPrimeTerminalLinkBatch.{rows_one_eq_artifact,physicalIndex_injective,physicalIndex_surjective,publicColumn_injective,publicColumn_surjective_interval,satisfies_iff_holds,satisfies_iff_logicalPaperLinks}`, `TerminalLink.Program.{compile_plain,accepts_plain_iff}`, `TerminalLink.ProgramRefinement.{generated_plain_eq_canonical,generated_plain_cost,generated_plain_expansion,generated_plain_compile,generated_plain_accepts_iff_selectedRows,generated_batchCost_eq_rowCount,generated_program_exact_row_ownership}`, `TerminalLink.PhysicalRowsRefinement.{generated_rowCount_eq,generated_columnCount_eq,generated_rows_eq_selected,generated_rows_eq_compiler_output,generated_rows_exact_receipt_ownership}`, `TerminalLink.LoweringRefinement.generatedPlain_accepts_iff_priorLinkAccepted`, `TerminalLink.PlacementRefinement.{generatedPlain_compile_eq_currentPlacement,generatedPlain_accepts_pulled_iff_generatedRows,generatedPlain_accepts_pulled_iff_loweringPriorLinkAccepted}`, `FPrimeFullHistoryTerminalLogicalLinkSound.{logicalCheck_of_holds,logicalCheck_of_rows}`, `FPrimeFullHistoryTerminalLinkDrift.{generatedSnapshot_ne_currentPlainOwner,generatedSnapshot_missingPlainPaddingRows,generatedSnapshotRows_ne_currentPlainOwnerRows}`, `FPrimeFullHistoryCurrentTerminalLinkCompletion.{currentRows_of_snapshotLinkRows,completedAssignment_producerAligned,output_and_snapshot_rows_construct_currentPlainOwner}`, `FPrimeFullHistoryCurrentTerminalLinkPlacementSound.{mapped_rows_eq_generated,producerBitColumnMap,producerColumnsAligned,generatedRows_iff_sourceProgram,generatedRows_iff_freshPublic_eq_encodeInstance,generatedRows_iff_loweringPriorLinkAccepted,generatedRows_iff_logicalPaperLink,output_and_generated_rows_construct_currentPlainOwner}` | `f_prime_terminal_link_program`, `engine::decider::enforce_terminal_latest_link`, `terminal_link_{program,rows}_export.rs`, current isolated output/terminal-link owners, bounded current full-history placement certificate, and the captured generated full-history prefix | `artifact-checked` |
| `CIR-FPR-NATIVE-PUBLIC-LINK-PROGRAM` | The shared lifecycle/paper-decider public-link predicate interprets one verifier-owned six-instruction Rust value in exact shape, affine-one, ordered 256-body, and trailing-padding order. The Rust drift gate emits that same value. Lean proves it equals the canonical typed source schedule, computes its cost from the program as 273 scalar obligations, proves the canonical program extensionally equals the source-shaped checker on every raw claim, and reduces generated-program acceptance to the logical HyperNova public-input equality. Runtime regressions mutate all 256 body positions, all thirteen plain padding positions, every shape field, and the separate suffix/padding boundary. This is concrete Rust-emitted program-data refinement, not formal semantics of compiled Rust, proof of production call arguments, the complete selected fixed-one `Context`, or R1CS-row equality | `CanonicalPublicInputLinkProgram.{plain_cost,run_plain_eq_sourceCheck}`, `CanonicalPublicInputLinkProgramRefinement.{generated_plain_eq_canonical,generated_plain_cost,generated_run_eq_sourceCheck,generated_run_reduces_to_logicalPaperLink}` | `f_prime_public_input_link_program`, `f_prime_public_input_link_matches`, and its generated Lean program drift gate | `artifact-checked` |
| `CIR-FPR-NATIVE-XOUT-PROGRAM` | Production `state_x_out_preimage_with_mode` now interprets one typed Rust source schedule instead of separately concatenating fields. The drift gate emits all four stateless/stateful × plain/Nebula programs. Lean proves every emitted program equals the independent typed schedule, begins with the exact `0x4e460002` domain, places the present-only `0x4e424c41` Nebula marker and lane last, and expands for every typed preimage to exactly `encodeStateXOutPreimage`. Program-derived field costs are exactly 23, 28, 27, and 32 respectively. A separate theorem composes frozen `XOut.compute` with the generated plain public-link program and proves acceptance of exactly its canonical affine-one plus 256 little-endian output bits and zero padding. The existing native receipt drift gate is unchanged, and all eleven native/circuit XOut regressions pass, including stateful, Nebula, u32-boundary, and coordinate mutations. Poseidon2 evaluation remains opaque and collision is the named `FPR-HASH` event. This establishes generated source-program ordering/domain/coordinate alignment, not compiled-Rust semantics, proof that every production call supplies the selected Lean state/context, or complete selected fixed-one/R1CS refinement | `StateXOutProgram.{execute_forPreimage,statelessPlain_cost,statelessNebula_cost,statefulPlain_cost,statefulNebula_cost}`, `StateXOutProgramRefinement.{generated_eq_canonical,generated_starts_with_exact_domain,generated_execute_eq_encodeStateXOutPreimage,generated_publicLink_accepts_computedXOut}` | `state_x_out_preimage_program`, `state_x_out_preimage_with_mode`, `compute_x_out_recorded`, and `state_x_out_program_export.rs` | `artifact-checked` |
| `CIR-FPR-XOUT-SPONGE-RECEIPTS` | The selected plain/stateless XOut source program computes 23 input fields before physical inspection. The rate-four sponge cost is definitionally `23 + 2 + 600 * 7 = 4225`: one zero row, 23 absorb-wrapper rows, one padding-wrapper row, and seven exact 600-row permutation calls. A nonoptional receipt checks the actual owner-row slice, ordered physical row interval, ordered fresh-column interval, source range availability, duplicate freedom, and row/column conservation. The captured base output, recursive prior consumer, and recursive output owners have exact receipts at rows/columns `[6533,10758)`/`[6344,10569)`, `[218,4443)`/`[868073,872298)`, and `[11,4236)`/`[1127811,1132036)` respectively; erasing physical column identities gives one identical pure sponge execution schedule. This is bounded artifact evidence for three nonoptional four-lane sponge cores. It does not implement the totalized optional-digest `paperHash` relation, check its iteration/state/running/program-counter alignment, emit or bind its presence coordinate, supply a typed `hashPrior` or `hashNext` `CallRecipe`, partition a current whole program, prove compiled-Rust/Poseidon2 parity, or discharge collision resistance | `Poseidon2Sponge.EmissionReceipt.{traceRows_length,rowIndices_nodup,allocatedColumns_nodup,row_column_conservation}`, `FPrimeFullHistoryXOutSpongeReceipts.{sourceProgram_eq_generated,inputFields_eq,physicalCost_eq,baseReceipt,priorReceipt,recursiveOutputReceipt,baseRows_exact_cost,priorRows_exact_cost,recursiveOutputRows_exact_cost,base_conservation,prior_conservation,recursiveOutput_conservation,pureExecutions_equal}` | Typed XOut source program, generated base/prior/output sponge traces, and their captured owner-row lists | `artifact-checked` |
| `CIR-POSEIDON2-SPONGE23-RECIPE` | One fused typed occurrence accepts an ordered 23-field auxiliary input bundle, emits the exact source-selected rate-four schedule (one zero definition, 23 absorb definitions, one padding definition, and seven 600-row permutation programs), allocates exactly 4,225 auxiliary temporaries, and adds only four activation-gated visible-output copies. Thus its nonoptional receipt contains exactly 4,229 rows and 4,229 auxiliary allocations, with no committed or public allocation. Active satisfaction fixes all four outputs to the pure sponge execution; deterministic completion changes only the receipt-owned temporaries and proves both active and inactive completeness. Every sparse dependency is classified as verifier-one, activation, ordered input, output, or temporary; row IDs and allocation IDs are duplicate-free; every allocation has the occurrence owner; and both normalized and typed receipts prove row/column conservation. The selected encoding is minimum in the exact 128-member rewrite class formed by independently retaining or eliminating each internal call's redundant eight output gates: fused cost is `(4229,0,0,4229)`, while retaining all seven blocks costs `(4285,0,0,4229)`. The optimization order is recurring rows, committed columns, public columns, then auxiliary columns. This does not establish that a particular bundle is the XOut serialization, implement optional-digest presence/alignment, select a complete hash call recipe, prove generated placement or native Poseidon2 parity, or discharge collision resistance | `ProductionPoseidon2Sponge23Recipe.{NumericSponge.trace_valid,NumericSponge.emissionReceipt,receipt_row_count,rowIds_nodup,active_sound,complete_changesOnly,completedNumeric_eq_execution,active_complete,inactive_complete}`, `ProductionPoseidon2Sponge23Audit.Recipe.{rows_supported,receipt_allocation_count,receipt_allocation_ids_nodup,receipt_allocations_owned,receipt_row_column_conservation,normalized_row_column_conservation}`, `ProductionPoseidon2Sponge23Audit.RewriteClass.{selected_cost,isolatedCalls_cost,selected_minimum}` | Source-selected fused typed sponge recipe over the exact production permutation program; no generated call-site placement | `artifact-checked` |
| `CIR-FPR-XOUT-SPONGE23-INPUT-ALIGNMENT` | For a plain/stateless `XOutPreimage`, four explicit lookup-availability premises fix the verifier digest, PiCCS header, current boundary, and Construction-2 accumulator to four fields each; canonicality fixes all 23 exported representatives below the Goldilocks modulus. Under those premises the selected Rust-emitted source program equals the independent protocol-shaped encoder, has exactly 23 fields, and an index-by-index typed bundle alignment makes normalized sponge columns `1..23` read exactly that vector without modular change. Active recipe satisfaction therefore fixes every visible output lane to the pure selected sponge on the exact source vector, and honest temporary-only completion is complete for the same relation. The availability premises are necessary at this interface: an empty raw-encoding table satisfies the existing table-well-formed predicate but produces only seven source fields. This result adds no rows or columns and does not implement the optional hash wrapper, state/running codecs, either hash call recipe, generated placement, native Poseidon2 parity, or collision resistance | `ProductionXOutSponge23InputAlignment.Source.{emptyTable_wellFormed_but_sourceWidth_seven,fields_eq_encodeStateXOutPreimage,fields_length}`, `ProductionXOutSponge23InputAlignment.{numericInputs_eq_sourceFields,semanticLane_eq_sourceLane,active_sound,active_complete}` | Generated plain/stateless XOut source program, independent XOut encoder, and selected fused 23-field typed sponge occurrence | `artifact-checked` |
| `CIR-FPR-NATIVE-FIXEDONE-ADAPTER` | A universal typed adapter now instantiates the frozen fixed-one Construction-2 `Setup`, `Machine`, input, and output directly from the native receipt semantics and its separate lifecycle boundary. The paper state and digest are reject-totalized with `Option`; `none` is produced only by the enumerated variant, entry-state, NIFS, empty-step, semantic, Nebula, or outgoing-link checks. The adapter proves exact initial-state uniqueness; prior and next hash-preimage alignment for iteration, initial state, selected running value, and one-based program counter; exact recursive nonempty/all-coordinate fresh-link factorization; exact NIFS call arguments; and both directions between the mapped paper transition and the closed direct `Step.Holds`. Consequently, native producer acceptance plus entry authority, incoming delayed link, stateful application, Nebula relation, and the consumer/terminal-owned outgoing link is equivalent to frozen `CanonicalVerifier.FixedOne.Accepts`. A second theorem replaces modeled native acceptance with the receipt's recorded result once `check` has proved exact control-flow/call conservation and oracle replay. Primitive hash/NIFS truth remains contract-supplied by the receipt, and this is not a semantics theorem for compiled Rust or a concrete codec/recipe instantiation of the selected lowering/R1CS `Context` | `FixedOneCanonicalAdapter.{application_eq_some_iff,priorHash_eq_computed,nextHash_eq_computed,active_freshPublic_eq_encode_prior_iff,transition_iff_holds,canonicalAccepts_iff_holds,nativeAccepted_with_boundaries_and_outgoing_iff_canonicalAccepts,checkedRecorded_with_boundaries_and_outgoing_iff_canonicalAccepts}` | native `verify_core` receipt schema, lifecycle `BoundaryReceipt`, frozen `CanonicalVerifier.FixedOne` | `model-level` |
| `CIR-FPR-STATE-LINK` | Satisfying the plain full-history state-link row program equates every verifier key/header lane, counter, boundary, program counter, semantic/accumulator digest lane, and public-trace lane across adjacent steps | `FPrimeStateLinkSound.fPrimeStateLink_sound` | `engine::decider::enforce_state_link` | `artifact-checked` |
| `CIR-FPR-BASE-PINS` | Satisfying the seeded plain base-state row program pins all 31 verifier-owned authority coordinates to preprocessing-derived constants | `FPrimeBaseStateSound.fPrimeBaseState_sound` | `engine::decider::enforce_base_state_constants` | `artifact-checked` |
| `CIR-FPR-BASE-PROGRAM` | The complete 12,498-row plain base-step artifact is a checked program with 10,900 deterministic definitions and 1,598 retained assertions; satisfaction fixes x_out and valid checked execution constructs a satisfying witness | `FPrimeBaseProgramSound.fPrimeBaseProgram_sound`, `fPrimeBaseProgram_xOut_unique`, `fPrimeBaseProgram_complete` | `enforce_f_prime_base_step_circuit` | `artifact-checked` |
| `CIR-POSEIDON2` | The exact 600-row production width-8 Goldilocks Poseidon2 permutation is deterministic and complete: satisfying assignments agree with its extracted SSA interpreter, equal inputs force equal outputs, and interpreting any canonical input constructs a satisfying witness. A typed activation-compatible occurrence now maps artifact columns `0..608` to verifier-one, eight visible inputs, and 600 receipt-owned auxiliary temporaries; preserves all 600 SSA rows; adds exactly eight gated visible-output copies; proves exact 608-row/600-temporary cost and ownership; and supplies active soundness plus temporary-only active/inactive completion. This occurrence does not yet own sponge absorption/padding, either optional hash wrapper, call-site placement, native parity, or collision resistance | `Poseidon2PermutationSound.{poseidon2Permutation_sound,poseidon2Permutation_outputs_unique,poseidon2Permutation_complete}`, `ProductionPoseidon2PermutationRecipe.{receipt_row_count,rowIds_nodup,active_sound,complete_changesOnly,completedNumeric_eq_execution,active_complete,inactive_complete}` | `r1cs_circuit::poseidon2` generated rows and the selected typed lowering receipt | `artifact-checked` |
| `CIR-PIRLC-CHALLENGE-MACHINE` | A pure implementation-semantics machine fixes the intended PiRLC scalar sampler's canonical eight-lane Goldilocks state and cursor, overwrite absorption, raw-pair length word, scalar/digest domain pairs, absorb-one squeeze, extracted Poseidon2 permutation, canonical lane conversion, and lane-major little-endian 16-bit chunk order. Successful fixed executions expose candidates and the successor transcript state from the same four digest blocks. This property does not claim parity with native Rust, the transcript gadget, generated rows, the reached post-PiCCS state, rejection/selection rows, or any cost | `PiRlcChallenge.TranscriptMachine.digestChunks_lane_part`, `digestBlock_absorbed_zero`, `successfulExecution_successorState` | Intended `neo-transcript`/`draw_alphabet_vector` and `alphabet_sampling` schedule, pending exact refinement | `specified` |
| `CIR-PIRLC-AGGREGATE-ACCEPTANCE` | For one Boolean 16-bit candidate, an independently defined balanced 14-edge product tree plus seven paired output-bit equations, one collision-free radix-three product aggregate, and one root binding accepts exactly the verifier-owned production rejection decision. The radix theorem proves both 14-digit images below the Goldilocks modulus before using their modular equality. Honest inputs have one canonical output/accept extension. Independent countermodels show that removing the output-bitness, product-aggregate, or root-binding family admits an invalid transition. Exactness is conditional on `EuclidPrime goldilocksP` and `SevenNonresidue`. Separately, the active schema-2 artifact fixes gate arity 56, 40 role bindings, nine normalized rows, and the exact 25-term specialization; a handwritten evaluator proves its three generated row families equivalent to the independent relation and verifier meaning. This property owns the normalized leaf mathematics. Its fixed-recursive source decoding and physical placement are separately owned by `CIR-PIRLC-AGGREGATE-OUTER-IMAGE`; neither property proves complete recursive R1CS satisfaction, global cost closure, or row-removal authority | `PiRlcChallenge.Sampler.Chunk.Acceptance.productTreeOutputBitRows_iff`, `productTreeAggregateRow_iff`, `aggregateAcceptanceRows_extension_exact`, `generated_aggregate_shape_exact`, `generatedProductTreeOutputBitRows_iff`, `generatedProductTreeAggregateRow_iff`, `generatedFinalAcceptanceRow_iff`, `generatedAggregateAcceptanceRows_iff`, `generatedAggregateAcceptanceRows_iff_verifierMeaning`, and the three necessity countermodels | `PiRlcSampler.ProductionAlphabet`; `gadget_native::acceptance`; `gadgets_aggregate_acceptance_lean_artifact` | `artifact-checked` |
| `CIR-PIRLC-AGGREGATE-OUTER-IMAGE` | The exact Rust-exported fixed-recursive artifact contains 15 challenge shards, 64 chunks per challenge, 15,360 decoded input positions, and 720 removed linear definitions. Its 15,120 singleton decoders and 240 sparse decoders (four exact 391-term patterns) cover every input position. The handwritten interpretation makes source-to-decoded equality and Boolean ownership explicit, then proves the nine active rows sound and complete for the independent product-tree and source-acceptance meanings. The artifact census reconciles 19,920 uniquely owned source rows and 16,560 unique physical rows: 7,920 Boolean-owner rows plus 8,640 active rows. This is fixed-profile artifact evidence with an exact Rust-generator drift gate; it does not prove complete recursive R1CS satisfaction, production selector placement outside this subtree, global F' soundness, or permission to remove constraints | `OuterImage.activeRowsHold_iff_sourceMeaning`, `generated_outer_image_shape_exact`, `generated_decoder_tree_exact`, `generated_definition_tree_exact`, `generated_physical_row_tree_exact`, `generated_source_row_tree_exact` | `gadget_native::acceptance::outer_image`; `f_prime_recursive_manifest::aggregate_acceptance_outer_image`; generated outer-image shards and handwritten correspondence | `artifact-checked` |
| `CIR-PIRLC-PACKED-MOD5` | For the isolated one-rho, 64-chunk sampler fixture, the generated artifact has exactly 20 candidate-zero source rows, 15 packed coordinates, 6 decoder definitions, 8 active row roles, an arity-56 polynomial with matrix roles `0/44/45/54/55`, and 12 sparse terms whose selector-inclusive degrees are exactly `[5,4,3,5,4,3,7,5,3,5,5,5]`. The generated source rows equal the readable candidate-zero rows up to sparse-term permutation, and the generated bit/residue polynomials equal the independently stated packed residuals at explicit role points. The second generated decoder is exactly the high quotient-bit projection and its 32-atom field expansion evaluates to the independently stated `derivedQuotientHigh` when the source one-column is one and the thirteen low coordinates satisfy the explicit alias predicate. Separately, eight packed residual equations are equivalent to sixteen scalar residual equations under explicit `SevenNonresidue`. This property does not discharge those aliases from recursive production placement, prove the eight emitted matrix rows implement the role points, cover selectors/inactive rows/full-F′ placement, discharge `SevenNonresidue`, or authorize row removal | `PiRlcChallenge.Sampler.Chunk.Mod5.packedRows_iff_directRows`, `generated_shape_exact`, `generated_polynomial_degrees_exact`, `generated_polynomial_degree_at_most_eight`, `generated_source_rows_exact`, `generatedSourceAccepts_iff_candidateZero`, `generated_bit_polynomial`, `generated_residue_polynomial`, `generatedHighDecoder_fieldTerms_exact`, `generatedHighDecoder_output_eq_derived` | `gadget_native::mod5`, packed-Mod-5 artifact generator, isolated sampler fixture | `artifact-checked` |
| `CIR-FPR-CHUNK-BIND` | All 6,661 exact chunk-shape digest rows form a deterministic and complete straight-line program from constant-one/start-step inputs to the four public digest lanes; the final-four-row binding theorem remains separately available | `FPrimeChunkDigestSound.fPrimeChunkDigest_sound`, `fPrimeChunkDigest_claim_unique`, `fPrimeChunkDigest_complete`, `fPrimeChunkDigest_binding_sound` | `f_prime::digest_circuit::enforce_f_prime_chunk_public_digest_circuit` plus the F' branch equality rows | `artifact-checked` |
| `CIR-FPR-CE-CONTINUITY` | The exact one-claim continuity artifact directly equates all 1,169 retained PiDEC-child/PiCCS-running CE-core coordinates. Its checked no-read relation omits child/running `y_zcol`; this describes current rows but does not itself prove that the omission is a sound authority boundary. The fixed terminal raw-old-block delayed bridge is closed separately by `CIR-FPR-PICCSNC-BLOCK-DELAYED-ACTIVE`; recursive-circuit and general authority remain open. | `FPrimeCeContinuitySound.fPrimeCeContinuity_sound` | `engine::decider::enforce_child_core_equal_running` | `artifact-checked` |
| `CIR-FPR-PICCSNC-PRODUCTION-TERMINAL-REFINEMENT` | For the exact `steady-recursive-block19-lane6-post-pidec` profile, satisfaction of the 1,545 selected combined-NC rows plus the generated constant/selector pins and exact terminal input-column bindings derives the complete 25-round claims-level NC acceptance predicate. The terminal scalar and final SumCheck claim are computed from the selected source-row consequences; they are no longer accepted as premises of `ColumnBindings`. The only executable schedule certificate is 25 proof-free `RawRoundMap` records, each with 43 mapped columns, 28 allocated columns, and five coefficient pairs. This selected-row property remains bounded artifact evidence. The separately generated raw-old-block row bridge now closes the fixed terminal projections at `pending.old_block`, supplies the terminal checker, and composes to the packed-only authority headline or typed parent-opening failure; the stronger paper wrapper keeps its independent paper/`y_ring` branch while retaining `BaseNc ∧ AllPacked`. Runtime refinement of the other input bindings, recursive-circuit parent authority, `y_ring` closure, commitment binding, and row-removal authority outside the exact terminal raw-old-block family remain open | `ProductionTerminalBridge.computed_finalSum_eq_messageTerminal`, `ProductionColumnBindings.columnBindings_imply_exactDataflow`, `SelectedRowsActiveComposition.generatedEmittedAssignmentSatisfies_implies_claimsAccepted` | Generated Combined-NC source/selected rows and fixed-profile column maps; fixed terminal raw-old-block execution is separately artifact-checked/Rust-conformant, while other runtime input bindings remain open | `artifact-checked` |
| `CIR-COMPLETE` | Every independent successful `CompilerWitness` for the exact fixed `CIR-SOUND` profile reassembles into an assignment satisfying all `FPrimeFullHistoryRows.fullRows`; the witness carries source/interpreter executions and direct semantic inputs, not `Satisfies` or a verifier conclusion. The strengthened theorem also constructs the isolated current 270-row plain terminal-link assignment, proves its typed claim and the final Construction-2 payload use the same selected digest, identifies the captured logical public input with the paper encoder, and proves the codec round trip. This honest completion is local and does not assert current full-history physical placement | `Nightstream.Assurance.FPrimeFullHistoryCircuit.{fPrimeCircuit_complete,fPrimeCircuit_complete_with_currentPlainDigest}` | Rust witness generation and full-history audit synthesis plus the current isolated terminal-link owner | `artifact-checked` |
| `ENC-CANON` | Byte/field encodings are canonical, length-checked, injective on accepted values, and enforced by the exact production `enc_inst` rows | `Encoding.FPrime.encInst_injective`, `FPrimeEncodingSound.fPrimeEncoding_sound`, `FPrimeEncodingSound.accepted_public_bits_injective` | Rust digest decoding, serializers, and `enc_inst` | `artifact-checked` |
| `TERM-CE` | Direct terminal acceptance uses verifier-derived children and binds witness cardinality, public width, commitment, public projection, verifier-owned norm, evaluation-point shape, all ring evaluations, constant terms, and supported sidecars | `TerminalCE.terminalCE_sound`, `TerminalCE.terminalCE_complete`, `Rust.Terminal.success_refines_terminalCE`, `Rust.Terminal.invalid_has_named_rejection` | native terminal CE verifier | `rust-conformant` |
| `DEC-SOUND` | Decider acceptance implies the terminal relation or a named decider failure | `decider_reduce` | Spartan/decider boundary | `planned` |
| `RUST-REFINE` | Every currently supported native Rust verifier success/rejection path refines the Lean executable F' and terminal models; the compact entrypoint is pinned to its current fail-closed `Unsupported` contract | `Rust.FPrime.verify_eq_ok_iff_checkLocal`, `Rust.FPrime.success_with_outgoing_refines_step`, `Rust.FPrime.invalid_has_named_rejection`, `Rust.Terminal.success_refines_terminalCE`, `Rust.Terminal.invalid_has_named_rejection` | `native.rs`, uncompressed/audit lifecycle verifier, direct terminal verifier, compact fail-closed seam | `rust-conformant` |
| `VERIFY-REDUCE` | Verifier acceptance implies `ValidExecution` or `BadEvent` | `VerifierReductionTarget` realization | public verifier | `specified` |
| `BAD-BOUND` | The union of named bad events—including SumCheck collisions, PiRLC projection roots, projection-preimage binding collisions, sampling failures, relaxed-binding collisions, and hash collisions—is negligible under explicit assumptions | final security theorem | complete security boundary | `planned` |

Current-shipping note for `CIR-FPR-TERMINAL-LINK`: the terminal-only current
diagnostic captures the exact coefficient-complete affine shell
`[9657286,9673659)` and proves
`FPrimeFullHistoryCurrentTerminalAffineShellSound.Captured.{rows_iff_holds,
priorLatest_iff_currentPlacementRows}`. The surrounding current NIFS,
continuity, accumulator, public-pin, and direct-CE ranges and all fourteen CE
column schedules are recorded by a fail-closed Rust diagnostic. That
diagnostic run uses the direct-R1CS Definition-11 three-matrix structure and
therefore has exactly three evaluation rows per terminal CE claim, while the
selected fixed-point production relation uses the thirteen-port selective CCS
structure. The separate count of fourteen terminal CE claims is not the
matrix arity;
`FPrimeProductionOwnerProgramBoundary.CurrentProfile.
diagnosticMatrixCount_ne_activeProduction` proves the arities differ. The
affine theorems therefore describe the current-source diagnostic fixture, not
the selected production relation. They also do not decode the surrounding
matrices or identify the prior/latest link with either frozen unary terminal
relation. Consequently this is bounded diagnostic evidence, not
current-shipping `CIR-SOUND` or `CIR-COMPLETE`; M4 remains current-shipping
open. A separate Rust-originated selected-prefix capture now confirms the
actual fixed-point path has thirteen matrices, fourteen CE claims, and
thirteen evaluations per claim, reaching the raw-old-block projection at row
22,834,865. Its diagnostic builder now retains 286 nested row-family
boundaries without retaining coefficients. The selected terminal NIFS is
exactly `[11306317,19258244)` (7,951,927 rows), followed by exact
running-link, parent-link, latest-link, and accumulator ranges. These family
labels are not semantic authority. The path is therefore selectable and its
physical frontiers are known, but the required
full-terminal-rows-to-decoded-slice theorem remains blocked on a narrow
coefficient/column-join decoder.

The table is normative. A new protocol-critical theorem must either discharge an
existing ID or add a reviewed property here before implementation.

For `FOLD-NIFS-FS`, `NifsExtractionErrorBudget.piDecTargetWitnessFailure`
means executable NIFS acceptance without `Pi_DEC` target child witnesses.
Rejected transcripts consume no extraction budget. This is distinct from
Theorem 7's zero intrinsic loss after target witnesses already exist.

## 8. Evidence states

These labels have fixed meanings:

| State | Required evidence |
|---|---|
| `planned` | Property and source surface identified |
| `specified` | Lean predicate exists and matches a reviewed human specification |
| `model-proved` | Lean theorem builds, has an axiom report, negative witness where relevant, and proof hash |
| `artifact-checked` | The theorem covers the exact generated R1CS/encoding artifact rather than only a handwritten surrogate |
| `rust-conformant` | State, transitions, success and rejection paths, runtime regression, and drift gate pass |
| `security-reduced` | Acceptance reduces to validity or named bad events, with probability bounds and explicit assumptions |

`verified`, `sound`, `aligned`, or `complete` may not appear in status reporting
without naming the property ID and its evidence state.

## 9. Permitted assumptions and trusted base

The expected trusted or assumed boundaries are:

- the Lean kernel and selected standard-library foundations;
- explicitly identified mathematical facts not yet reconstructed locally;
- Module-SIS/Ajtai binding or relaxed binding security games;
- Poseidon2 collision resistance or random-oracle modeling where required;
- Fiat-Shamir transform assumptions;
- the final SNARK/decider soundness game;
- the Rust-to-Lean translation or artifact importer until it is itself verified.

Poseidon2 and Ajtai are opaque selected primitives for the current
protocol-first work. Their internal construction and hardness are not proof
targets. Protocol theorems must still bind their exact inputs and parameters,
prove transcript/domain and commitment/recomposition dataflow, and leave hash
collision or binding failure as explicit named events; the primitive boundary
does not make a digest or commitment authoritative by itself.

At the M2 model boundary specifically:

- SumCheck receives an independent semantic polynomial path and proves false
  acceptance implies a sampled-root collision; it does not assume acceptance
  implies claim truth;
- PiRLC and PiDEC receive typed homomorphism, norm-growth, and exact
  split/recompose laws. Concrete production-algebra instantiation remains a
  later refinement obligation;
- Appendix-D.5 rewinding is represented by `WeakExtractor`, whose failure arm
  carries evidence of a fixed `SamplingBoundary.Failure` proposition, and by
  `UniquenessBridge`, whose failure result is a literal Definition-4 relaxed-
  binding collision;
- the deterministic composition theorem proves validity or those named events.
  It does not prove their probability bounds.

Cryptographic assumptions must be passed as typed parameters to the theorem that
uses them. They must state an adversary, experiment, event, and bound. A field
such as `accepted_implies_valid : Accepted -> Valid` is not a cryptographic
assumption; it is the local conclusion and is forbidden.

Every final theorem must publish its complete trusted computing base. Project
status must say which boundaries remain assumed.

## 10. Per-property proof contract

Before implementing a property, create or update a short spec containing:

```text
property_id:
claim:
assumptions:
non_goals:
paper_sources:
rust_surfaces:
circuit_or_encoding_artifacts:
failure_class:
counterexample_or_witness:
lean_theorems:
axiom_report:
proof_hash:
conformance_status:
retest_commands:
```

The claim must be understandable without reading the proof. Assumptions and
non-goals must appear in the Lean module contract header as well.

## 11. Proof and conformance workflow

Work one high-value property at a time:

1. Select one property ID and one concrete failure class.
2. Fill the per-property proof contract and source map.
3. Define the semantic relation before the checker or prover.
4. Construct a failing or forged witness for the missing check where possible.
5. Implement the smallest executable model that covers the mapped surface.
6. Prove completeness and/or soundness with the exact documented scope.
7. Run `#print axioms` and record a source hash.
8. Check statement, state, transition, encoding, and error-path parity.
9. Add or link a Rust/circuit regression before claiming conformance.
10. Append the result to `assurance/evidence-ledger.jsonl`.

If a proof blocks, classify it before editing:

- `code-first`: the implementation likely violates the intended property;
- `model-first`: the Lean representation is incomplete or wrong;
- `harness-first`: generated artifacts or tests are stale;
- `theorem-first`: the statement is false, too broad, or mis-scoped.

Proof difficulty is evidence. It is not automatically tactic debt.

## 12. Completion gates

A property may advance to `rust-conformant` only when all applicable checks pass:

1. Statement parity, including preconditions, postconditions, and failure modes.
2. State representation parity or a written proof that an abstraction preserves
   the property.
3. Transition parity for every relevant success and rejection branch.
4. Canonical encoding and generated-artifact parity.
5. A minimized counterexample, trace, or runtime regression.
6. Lean build, axiom report, and proof hash.
7. A drift check against the current mapped Rust and circuit sources.

Not-applicable checks must be justified, not silently omitted.

## 13. Required local gates

Every formal change runs:

```bash
cd formal/nightstream-lean
./scripts/validate.sh static
./scripts/validate.sh build
./scripts/validate.sh axioms
./scripts/validate.sh check
```

Additionally:

- `tests/Axioms.lean` fails closed via `#guard_msgs`: the build breaks if any
  completed theorem's axiom report differs from the recorded expectation;
- `lake exe check` computes every printed result (envelope probes including the
  empty-step regression, Rust symbol anchors) and exits nonzero on failure —
  no unconditional success strings;
- active modules are checked for imports from `formal/deprecated`;
- proof hashes and conformance status are recorded in the evidence ledger;
- Rust or circuit commands are property-specific and belong in the per-property
  spec rather than a generic command list.

## 14. Deprecated-code policy

The legacy packages `formal/superneo-lean`, `formal/direct-ccs-fprime-lean`,
and `formal/twist-shout-lean` are read-only reference material. They may be
physically consolidated under `formal/deprecated` later, but their path does
not grant them authority.

- No active module may import a legacy package or `formal/deprecated`.
- No mass port is allowed.
- A reused definition or lemma is copied into the active ownership layer,
  restated against the active semantic types, and re-proved or revalidated.
- Old theorem names and layouts carry no compatibility requirement.
- Counterexamples and low-level algebra are preferred reuse candidates; wrapper
  surfaces and assumption bundles are not.

## 15. Roadmap

| Milestone | Exit condition |
|---|---|
| M0: assurance foundation | Canonical spec, active relation/state types, one model-proved property, evidence ledger |
| M0.5: vertical artifact slice | Versioned artifact schema, one exact production-used composed R1CS block, universal theorem, Rust/Lean adversarial vectors, and drift gate |
| M1: concrete relations | `REL-CCS`, `REL-CE`, and `REL-CONCRETE` at least `model-proved` |
| M2: SuperNeo fold | `SUM-*`, `FOLD-PICCS-ARITH`, and all other `FOLD-*` properties at least `model-proved` |
| M3: F' semantics | `REL-CONCRETE-PRODUCTION`, `FPR-PAPER`, `FPR-BASE`, `FPR-RECURSIVE`, `FPR-NIFS-BRIDGE`, `FPR-OBLIGATION-EXACT`, `FPR-OBLIGATION-NECESSITY`, `FPR-TYPED-LOWERING`, `FPR-HASH`, and trace induction at least `model-proved` |
| M4: circuit correspondence | Historical captured-profile correspondence: `FPR-CANONICAL-ENCODING` at least `model-proved`; `CIR-SOUND`, `CIR-COMPLETE`, `CIR-OBLIGATION-TREE`, and `ENC-CANON` at least `artifact-checked`. Current-shipping correspondence additionally requires **currency**: the covered row range must include the shipping profile's rows. This is open and kernel-obstructed — `FPrimeProductionOwnerProgramBoundary.currentTerminalLink_starts_after_historicalProgram` proves the current terminal link begins strictly after the captured artifact's final row, so the captured range is disjoint from the current terminal rather than merely stale |
| M5: implementation conformance | `RUST-REFINE` and direct `TERM-CE` `rust-conformant`; every unsupported public path is explicitly fail-closed; and every `rust-conformant` claim must satisfy **provenance**: the executions and mutations must originate on the Rust side and be transported into Lean, not authored in Lean. Hand-authored acceptance bits, receipt-supplied primitive outcomes, and measured dimensions are not provenance evidence |
| M6: end-to-end security | `DEC-SOUND`, `VERIFY-REDUCE`, and `BAD-BOUND` `security-reduced` |

M0, M0.5, M1, M2, and M3 satisfy their stated local exit conditions. **M5 is reopened pending a provenance re-audit** (spec change 2026-07-25): its evidence currently includes receipt-supplied primitive outcomes and hand-authored `rustAccepted` bits, which the added provenance requirement excludes. No replacement percentage is asserted here; the re-audit produces it.
M4 is **historical-artifact complete and current-shipping open**. Its captured
profile satisfies the historical half of the row above; it does not satisfy
the current-row refinement half.
**M2's exit is paper/model-level only.** `FOLD-NIFS-FS` proves quantitative
non-interactivity for the explicit finite correlated prefix/post-prefix
random-oracle experiment. Challenge-sampling failure is zero because that
experiment samples directly from a selected valid support; the remaining
interactive and transcript-collision event bounds are explicit contracts.
This is a Fiat--Shamir security theorem at the charter-permitted
random-oracle-contract boundary, not a Poseidon2, bounded production-sampler,
or security-reduced theorem.

Here M1 only certifies the caller-parameterized executable relation model; it
does not close the separate production instantiation. M2 is model-proved under
the published algebraic, extraction, binding, strong-sampling-set, and
explicit random-oracle contracts. The paper finite SumCheck and alpha/gamma
mixing contracts are now constructed from root counting rather than retained
as premises. M2 does not claim concrete Goldilocks,
Poseidon2, bounded-sampler, Rust, or R1CS refinement.
Its obligation-1 asymptotic slice now constructs the specialized countable
finite-alphabet first-success trace law and security-parameter runtime family,
proves rational-epsilon tail vanishing, almost-sure termination, exact
conditioned-first/fresh-second distribution, and EPT, and reaches the exact
frozen `PiCcsStrong` target under the two named fixed-witness algebraic
contracts.
M2 contains four composable model-level slices: finite SumCheck
message/checker semantics plus the exact causal root-counting and multi-round
probability contract; PaperJoint residual tables, signed
coefficient/Horner identity, independent unsampled coefficient truth,
one explicit joint polynomial binding the initial sum, canonical expected
rounds, and terminal with table-truth/mixing-root/round-collision reduction,
output-point semantics, and the exact finite alpha/gamma mixing-root
probability contract; typed partial NIFS replay with explicit blindness
witnesses; and generic FirstAccepted semantics plus a transcript-chained PiRLC
coefficient-batch contract and exact reached-state bridge for that partial
carrier.
The paper one-joint PiCCS and typed NIFS now share one verifier-owned
fixed-width SumCheck gate, with exact syntax-derived width, exact replay, and
residual-event identities. The syntax ceiling is kernel-proved below Appendix
D.4's conservative degree expression, while the loose-width necessity model
is excluded from both asymptotic paper points and NIFS keys.
On 2026-07-25, `FOLD-PICCS-MIXING-BOUND` deliberately reopened the M2 property
set after the earlier 19/19 status exposed an instantiated
`MixingRootProbabilityContract` premise. The new twentieth property is now
`model-proved`; this is a recorded specification correction, not a silent
status regression. A selected transcript-ordered production Split-NC carrier
now separately derives the literal FE/NC mixing-plus-SumCheck failure bound.
The earlier arbitrary-carrier obstruction remains valid: the positive theorem
does not infer a distribution for an opaque `deriveCore` schedule and does
not bind the selected support to the bounded production sampler.
Concrete deployment still requires external Boolean-leaf/production-bit
ordering, concrete field/support instantiation of the root-counting theorem,
complete-carrier and concrete candidate-stream/fixed-bound/strong-set
production sampler refinement, distribution analysis, and
Poseidon2/Rust/R1CS refinement. Those are not M2 model-theorem exit
conditions. M3 now satisfies its local model-level exit condition. The
production relation is pinned to the complete 270-coordinate `L_in`, and the
paper-exact fixed-one checker, under an opening-derived source-authority
receipt, reaches Construction 2 or precise named output/algebraic failures;
source binding is not a generic failure branch. Its honest theorem returns an accepted
Construction-2 execution or the existing bounded-sampler shortfall. The
fixed-active production composition theorem separately reaches extraction or
its named cryptographic/algebraic events without a rewind-arithmetization
callback. None of these results claims a Rust decoder, R1CS, generated-row,
concrete Poseidon2, concrete support, or probability refinement. M4's
historical captured-profile correspondence is complete. Current-shipping M4
correspondence and the broader concrete obligation-11 refinement boundary
remain open.
Selected Step and Terminal certificates inhabit the source-aligned
whole-program contract, with exact receipt conservation and finite-class
minimum theorems. For arbitrary admissible codecs and certified call recipes,
physical satisfaction now implies the corresponding typed checker acceptance,
and every admissible accepted Step or Terminal execution constructs an
assignment satisfying exactly the selected receipts. What remains unproved is
the production codec/recipe instantiation, Rust-program equality,
generated-row equality, and complete materialized production-leaf ownership.
The current production selective-R1CS stage and rewrite ledgers describe a
different compiler relation; they are not a license to relabel that relation
as the selected canonical IR or to certify its rows before a concrete
refinement is proved. Existing artifact correspondence is useful local
evidence but does not close those connections. M2 is model-proved over typed
algebra, rewinding, concrete assignment-indexed PiCCS arithmetization,
generic first-accepted semantics with explicit production-sampling
boundaries, relaxed-binding boundaries, and the explicit random-oracle
contract. M3 is model-proved over explicit executable hash, application,
fresh-link, running-digest, chunk-digest, and Nebula semantics, plus the
paper-exact selected NIFS checker and exact 270-coordinate production
relation. Its theorem scope includes true initialization, exact
base/recursive local obligations, the one-step-delayed consumer/terminal
fresh-link closure, collision-explicit `x_out` authority, exact closed-trace
induction, and the Construction-2 bridge described above. M2 and M3 are not
artifact-checked or security-reduced. M4 has artifact-checked `CIR-SOUND` for
one exact historical profile: the 4,193,134-row plain/stateless `[1,1]`
full-history artifact with one recursive invocation, terminal fold, direct
terminal CE, and the minimal-supported-bit-carrier relation. Its 257-row-link
snapshot is historical evidence, not current shipping evidence. Satisfaction
of its exact
   `fullRows` list yields a two-edge `ValidExecution` relative to the row-decoded
   NIFS callback and direct terminal validity, or one of the separately named
   recursive/terminal PiRLC root events. In addition,
   `CIR-FPR-FULL-HISTORY-FROZEN-STEPS` exposes the exact base and recursive
   `Step.Holds` witnesses and composes them with the universal adapter: the same
   row satisfaction yields both concrete frozen fixed-one checker acceptances,
   or the existing `BadEvent`. This removes existential trace packaging from
   the bounded step-soundness statement. It does not identify the snapshot
   with the selected canonical receipt program, refine the terminal checker or
   current Rust, or connect this stale row snapshot to the model-proved
   `FPR-NIFS-BRIDGE`; the artifact is not a semantic oracle for SuperNeo and
   cannot by itself authorize retaining or removing a verifier obligation.
The probability of either root event is an M6 obligation; it is not hidden
inside deterministic circuit correspondence. `CIR-COMPLETE` is also
artifact-checked for this profile: independent successful compiler executions
reassemble into satisfaction of every exact `fullRows` row. Its strengthened
completion theorem additionally constructs a satisfying assignment for the
isolated current 270-row terminal owner by copying the captured 257
authoritative coordinates and fixing the thirteen new padding coordinates to
zero. This is an honest local completion, not a current full-history placement
map. `CIR-OBLIGATION-TREE` now expands the generated hierarchy to 59
materialized leaves plus two exact zero-cost nodes, proves complete and unique
ownership of every captured row and exact parent costs, and attaches typed
mathematical/Rust/Lean routes. It also retains the 257-versus-270 drift
obstruction. The current Rust manifest-regeneration test additionally fails
closed at `FPrimeFullHistoryProjectionRoles.lean`. Therefore the M4 local
milestone is closed only for this captured profile. Stateful, Nebula, current
270-row full-history emission, other
schedules, multiple recursive invocations, alternate carriers, and
parameterized circuit families are outside the claim. M5 independently closes
conformance for the supported
uncompressed/audit lifecycle and direct terminal CE verifier: universal Lean
success/rejection theorems, executable negative witnesses, real Rust replay,
and full-file drift hashes all pass. This does not broaden M4 beyond its fixed
profile. The compact Spartan decider remains explicitly `Unsupported`, so
`DEC-SOUND` and all M6 claims remain open; changing that Rust branch
automatically reopens M5 through the drift gate.

## 16. Change control

Any change to a mapped paper definition, Rust state field, transcript preimage,
circuit constraint, public encoding, or verifier branch reopens affected
property IDs. Their evidence state returns to the highest level still supported
by current artifacts.

Changes to this specification require the same review discipline as changes to
protocol-critical code. The property matrix and evidence ledger must remain
consistent.
