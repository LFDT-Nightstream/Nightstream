import Nightstream.Protocol.FPrime.ConcretePhi81.Deviations.BlockLaneCombinedNc.ProductionMixingBoundary.Algebra
import Nightstream.Protocol.FPrime.ConcretePhi81.Deviations.BlockLaneCombinedNc.ProductionMixingBoundary.ChallengeSupport
import Nightstream.Protocol.FPrime.ConcretePhi81.Deviations.BlockLaneCombinedNc.ProductionMixingBoundary.CoreSchedule
import Nightstream.Protocol.FPrime.ConcretePhi81.Deviations.BlockLaneCombinedNc.ProductionMixingBoundary.ExactEvents
import Nightstream.Protocol.FPrime.ConcretePhi81.Deviations.BlockLaneCombinedNc.ProductionMixingBoundary.IdealInteractiveSoundness

/-!
Production Split-NC mixing boundary and selected ideal-interactive repair.

Assurance tier: model-level obstruction and concrete algebraic boundary.

The unrestricted production carriers still lack a sampled finite support
aligned with the denominator and an internal causal order for the opaque core
challenge record. Those countermodels remain valid. The selected
ideal-interactive constructor separately owns an explicit ordered support and
derives the exact named-event bound; it makes no claim about an arbitrary
existing schedule.

Owns: one facade for the exact event inventory, concrete algebra bridge,
kernel-checked arbitrary-carrier obstructions, and the selected positive
ideal-interactive refinement.

Does not own: a law for an arbitrary opaque schedule, bounded production
sampling, Fiat--Shamir, frozen semantics, Rust/R1CS, encoding, or rows.

Emits constraints: no.

| Boundary | Exported evidence | Excluded claim |
|---|---|---|
| production carrier | exact support and schedule obstructions | obstruction to a future refined carrier |
| selected ideal carrier | exact `FeFailure ∨ NcFailure` probability theorem | inference to an arbitrary schedule or Fiat--Shamir |
| existing Split-NC | unchanged collision theorem remains owned separately | loss reassociation |
-/
