import SuperNeo.SecurityModel

/-!
Barrel interface for `SuperNeo.SecurityModel` (Section 6 (Security model)).

The authoritative export set is the import list of `SuperNeo/SecurityModel.lean`;
per-module theorem boundaries live in the colocated `*Interface.lean` files
next to each implementation module.
-/
