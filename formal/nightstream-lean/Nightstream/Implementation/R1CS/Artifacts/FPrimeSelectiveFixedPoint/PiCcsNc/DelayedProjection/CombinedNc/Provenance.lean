import Nightstream.Implementation.R1CS.Artifacts.FPrimeSelectiveFixedPoint.PiCcsNc.DelayedProjection.CombinedNc.Generated.Provenance

/-!
Stable artifact facade for complete combined-NC compiler provenance.

Owns: the handwritten import boundary for generated source columns, retained
slots, definitions, eliminated columns, derived products, rewrite steps,
retained steps, and decoder records.

Does not own: the semantic correctness of a rewrite, row satisfaction,
acceptance, costs, or permission to remove rows.

| Stable stage path | Generated owner | Authority class |
Emits constraints: no.

|---|---|---|
| `f_prime.pi_ccs_nc.delayed.combined.provenance` | `Generated.Provenance` | computed artifact |
-/
