import Nightstream.Implementation.R1CS.Artifacts.FPrimeSelectiveFixedPoint.PiCcsNc.DelayedProjection.CombinedNc.Generated.Execution.ProductionEmitterLayout

/-!
Stable artifact facade for the fixed production emitter placement.

Owns: the handwritten import boundary for the generated physical row interval,
column allocation, Ajtai child-base join, profile tag, and selector tag.

Does not own: row satisfaction, semantic projection authority, commitment
binding, transcript soundness, costs, or row removal.

| Stable stage path | Generated owner | Authority class |
Emits constraints: no.

|---|---|---|
| `f_prime.pi_ccs_nc.delayed.execution.placement` | `Generated.Execution.ProductionEmitterLayout` | computed artifact |
-/
