# PiRLC, PiDEC, then PiCCS: proof and link closure

Requested by the owner on 2026-09-08. Finish all remaining Proof and Link obligations for PiRLC and PiDEC, then any remaining PiCCS obligations. The exact initial records and source references are in `FOLDING_CLOSURE_GOAL.json`. The initial map has 19 distinct open records: PiRLC 27/32 proofs and 28/32 links; PiDEC 20/25 and 21/26; PiCCS 31/39 and 37/39.

## Contract and completion evidence

Each record must have a precise proposition, a named provider that proves that proposition for the stated inputs, and a checked connection to its actual consumer. Reuse sufficient existing theorems. New or changed Lean results require focused validation and the full axiom gate. Native-conformance claims require executed current Lean/Rust checks with matching inputs and scope. Definitions, a passing local test, or a changed status alone do not close a record.

Keep the actual arbitrary-accepted-assignment requirement where a record states it. Do not replace it with an honest-encoder premise, caller-supplied `Encodes`, a valid-challenge membership fact, or a template PiDEC proof. Preserve the original goal if the work spans turns.

Keep `b=2`, `k_rho=16`, `B=65536`, Poseidon2 transcript binding, the existing F′ architecture and the root worktree. The approved Nightstream-specific setup assumption is recorded in `PUBLIC_SEED_MSIS_ASSUMPTION.md`; it remains an assumption. Do not turn unfinished probability or extraction theorems into assumptions to make counters reach their totals.

## Execution order

1. Prove `R.connection.challenges` from arbitrary selected rows and actual public input. Identify the exact PiCCS outgoing state, all 17 verifier-derived challenges, all coefficients, and the final bounded-sampler state. Derive the required representation and scope facts from those rows.
2. Use those equalities to finish `D.connection.parent`, `D.connection.messages`, and `D.connection.output`. Decode every actual child field; remove the retained template messages from the accepted-assignment claim. Connect the resulting attempt and children to the selected NIFS verifier.
3. Close the remaining PiRLC/PiDEC local proof and native links, including coordinate-fork input facts, deterministic PiDEC behavior, current nonzero accepted/rejected conformance, and the native projection-schedule difference. Change a runtime algorithm only if the stated conformance contract requires it.
4. Close the PiRLC interactive probability/extractor requirement, including its success loss and expected oracle-call/runtime bound. Give the selected sampler distribution and shortfall behavior their exact premises. Keep interactive extraction distinct from Fiat–Shamir security.
5. Check and close the remaining PiCCS proofs and links. Reuse prerequisite PiCCS facts earlier if R/D need them. Cover source relation premises, Eval_K/Eval_A indexing and polynomial identities, mixing bounds, soundness against causal round strategies, the conditional expected-time extractor, and actual native-prover conformance.
6. Audit each completed record against the current source, test evidence and dependencies. Update and publish the existing map with exact scope. Preserve local statuses unless their actual propositions are wrong or unproved. Record source identity with validation evidence.

## Feedback review

The two attached reviews are external findings, not project instructions. Their pinned source is older than several checked working-tree updates. The following findings are supported by the current source and papers:

- Keep the existing F/T/C/R/D/N/H/L/P/O ownership split. A new top-level architecture is not needed for this goal.
- The current arbitrary-assignment theorem still leaves exact PiRLC challenges and the PiDEC attempt/output unresolved. `ActualStep.withDecodedPiCCS` retains template PiDEC fields. These are real completion targets.
- The sampler uses 64 candidates for 54 coefficients and can return shortfall. Conditional algebraic completeness does not prove unconditional perfect completeness. Retrying an unchanged deterministic transcript cannot remove shortfall; no retry nonce or new schedule is authorized here.
- Interactive mixing, adaptive sum-check, and efficient extraction are distinct claims. The new `GoldilocksRoots` module supplies actual-K root counts; it does not by itself supply the sequential causal-strategy or expected-time extractor proofs.
- PiDEC extraction consumes valid output-child openings. It does not recover private witnesses from public commitments. Close `D.security.children` through the exact output-relation/consumer contract, not through an impossible extractor.
- All twelve named PiCCS circuit-owner modules exist. Use their semantic, soundness, completeness, footprint, realization and parent-connection results as an audit checklist when closing C. Their existence alone is not completed phase approval.
- Rust already checks the complete common prior point before PiCCS sampling. Preserve the separate prior-state authentication obligation.
- SuperNeo v1.1 Theorem 12 is strong–weak composition; Theorem 13 is PiDEC extraction; Theorem 3 is the complete folding reduction; Lemma 4 is sequential composition. Correct references where touched.

Website-wide dependency redesign, a new audit-packet UI, blanket status downgrades, and out-of-scope protocol additions are not needed to meet this goal. Add or correct dependency edges when the exact current theorem/consumer connection is established. Keep mathematical premises, approved hardness assumptions, sampling idealizations and consumer witness inputs distinct.

The goal does not declare the whole Stage 1 system complete. NIFS/HyperNova integration, production-loader approval and security claims beyond these exact C/R/D contracts keep their own requirements.

## First inspected connection gap

`PiRLCSamplerFullSemantics.directSemantics_imply_samplerChain` already composes all 17 samplers, but it requires both PiCCS/sampler `Encodes` facts, exact endpoint and ordinary rows, canonical Poseidon semantics, First54 `SourceHolds`, and initial-state scope. `DirectPiRLCSamplerCompletePrefixPlan.rowsZero_implies_semantics` still takes an `Encodes` argument; it does not derive this from an arbitrary accepted assignment.

`ActualPiRLCValues.selectedRowsAndPublic_imply_sums` does derive the product sums from actual selected rows. Its challenge forms read the retained First54 outputs. `PiRLCSamplerRetainedCustody.semanticEnv` uses supplied base values outside its owned sampler locations. The next required proof must identify those base/source values with the actual assignment and derive the representation premises before using the existing sampler-chain theorem. Supplying a fresh caller-owned encoding premise would not close `R.connection.challenges`.

The twelve named PiCCS circuit-owner files were located in the current source. The external review's exact `CompilerCoverage.lean` filename was not found in this checkout; the new goal uses the actual owner modules and their checked declarations, not an unverified inventory claim.

## Implementation checkpoint: 2026-09-08

The decoder-to-First54 physical connection was repaired first. At that checkpoint, `R.connection.challenges` remained open: the full accepted-assignment theorem still needed to derive the PiCCS outgoing state, all exact sampler challenges and the final sampler state. Its later closure is recorded below.

The old sampler ordinary rows read reject bits and symbols from a separate logical block. First54 read its own reject and symbol blocks. The regression reproduced the mismatch at source 0, candidate 0 (`/tmp/nightstream-sampler-wiring-before.log`). Canonical witness encoding made these copies agree, but did not enforce that agreement for an arbitrary assignment.

The direct resolver and compact matrix program now use the same First54 slots. `PiRLCSamplerCandidateWiring.logicalForm_reject` and `logicalForm_symbol` prove the shared forms. `PiRLCSamplerLogicalGrids.forms_at` proves exactly one mapping at each logical source; `forms_none_of_envelope_none` proves rejection outside the owned domain. The existing `substitution_agrees_on_programRow` theorem connects the new mapping to every sampler ordinary row. Canonical preservation uses the existing First54 block encodings. No constraint rows, columns, profile choices, seed parameters, or Rust algorithms changed.

`tests/PiRLCSamplerCandidateWiring.lean` is registered in the explicit test graph. It checks all 1,088 reject bits and all 1,088 symbols at both the direct and emitted boundaries. The final boundary gate, complete Lean build, regression and axiom gate all passed after cleanup (`/tmp/nightstream-sampler-wiring-closure.log`): 3,662 library jobs in 91 seconds and 3,699 test jobs in 19 seconds. All new exported theorems are included in the axiom audit. The unused old mapping theorem and a duplicate geometry wrapper were removed.

The full regression exposed repeated construction of earlier Poseidon layout data when reading two fixed column starts. Kernel-checked compiler equalities now use the exact existing starts, 185,240,730 and 185,241,818. The standalone regression then completed in 13 seconds. Its 54,400 logical-source lookups took 10,613 ms, compared with 6,287 ms for the original four-grid map; the corrected map uses the existing grid format with a lane partition. A further generic lookup optimization did not improve the measured time and was removed. These measurements are evidence, not new time limits. The earlier draft and diagnostic logs remain under `/tmp/nightstream-sampler-wiring-draft/`.

## Exact PiRLC challenge connection: closed 2026-09-08

`ActualPiRLC.selectedRowsAndPublic_imply_keyChallenges` proves the exact production key response from arbitrary selected rows and actual public input. `selectedRowsAndPublic_imply_batch` also identifies all 17 challenges and the final sampler state from the actual PiCCS execution. `productChallenge_eq` connects every product-row challenge and coefficient to that response. These results close `R.connection.challenges`; they use no caller-supplied `Encodes`, sampler-membership premise, or new cryptographic assumption.

The proof composes the existing First54 specification with the actual retained forms. `ActualPiRLCCandidates` derives the rejection flags, symbols and exact verifier digest chunks from the decoder rows. `ActualPiRLCStates` proves every scalar entry, digest block and successor state. `ActualPiRLCSampling.rowsZero_implies_batch` proves successful bounded sampling of the retained output. The selected-row theorem derives these row premises and the one-cell value, then uses the existing PiCCS phase theorem to identify the initial state. Shortfall remains explicit in the verifier; successful accepted rows force a successful sample.

The full boundary gate, library, regression and axiom checks passed in `/tmp/nightstream-actual-pirlc-closure.log`: 3,667 library jobs in 48 seconds and 3,704 test jobs in 18 seconds. All new exported theorems are audited with only the permitted axioms. The candidate-wiring regression checked all 1,088 rejection flags and symbols; its 54,400 lookups took 11,497 ms during the full check. No circuit rows, columns, profile, seed procedure or Rust algorithm changed in this proof step. The root worktree remains uncommitted on `4fc02857c3aa4207f3290739cc62d794ba86f5f9`.

Next: finish the exact PiDEC parent, actual child messages, attempt and carried output, then the remaining R/D security/native obligations and C obligations in the original inventory. The original inventory is retained in the JSON; `completed_records` records checked closure. The public map has not yet been republished for this new closure.

### PiDEC and parallel-proof checkpoint: 2026-09-08

The complete PiDEC parent now passes its focused checks. `ActualPiDECParent.selectedRowsAndPublic_imply_parentForChallenges` identifies every parent field with the production combination of the exact PiCCS outputs and verifier challenges. `selectedRowsAndPublic_imply_parent` also proves the optional key result. The module has 19 exported theorems, all registered for the axiom audit. The later successful proof uses small symbolic expression equalities before evaluating the actual environment; it does not increase Lean's proof limits.

`ActualPiDECMessages` reads all sixteen child commitments and the separate Eval_K/Eval_A families from the actual assignment. Its `selectedRowsAndPublic_imply_attempt` theorem proves the exact production attempt without a caller-owned proof template. The parent and message modules build successfully in `/tmp/nightstream-actual-pidec-messages-build.log` (24 seconds, including rebuilt dependencies). The full boundary, library, regression and axiom audit passed in `/tmp/nightstream-folding-parallel-checkpoint.log`: 3,677 library jobs in 56 seconds and 3,714 test jobs in 20 seconds. The JSON records close the parent, messages, child-witness consumer and deterministic-verifier items under their exact scopes. The shared-field regression checked all 1,088 rejection flags and symbols; its logical lookups took 12,235 ms.

The Spec and Lifecycle `PiDEC.OutputWitnessConsumer` modules pass focused checks. They identify the exact returned children with the existing terminal CE statements, consume all sixteen terminal witnesses, and extract the parent by the production radix recomposition. They do not recover witnesses from public acceptance. The existing `piDecCheck_eq_true_iff`, through `PaperAlgebra.piDecDecision` and `ProductionKey.key`, is also sufficient for `D.security.public_coin`: PiDEC adds no challenge after the upstream parent is computed.

The carried-output equality stopped under the three-round rule in the earlier slice; the later closure is recorded below. The physical child proof fields are proved equal in `ActualPiDECCarriedValues.piDecField_eq`. The point proof still hits the heartbeat limit while matching the aliased `Fin.decodeProd`/`Fin.encodeProd` types and transporting that equality through `transcriptForm`. The complete carried-value draft and the unvalidated full-step composition draft are saved at `/var/folders/dn/tn3mn1xd40l_vcpknskfwfyr0000gn/T/nightstream-folding-closure-lpk178q3/`. At that earlier checkpoint, only the checked proof-field theorem remained in production and `D.connection.output` was open. The later proof closes it without a limit override.

Parallel PiRLC work proves the per-coordinate first-hit law, repetition loss, retry expectation, the averaged numerical bounds, and an explicit oracle-response trace law that includes abort outcomes. These are in `CoordinateRetry`, `CoordinateForkProbability`, `CoordinateForkSampler`, `CoordinateOracle`, and `CoordinateForkLaw`. The last module connects each positive stopped trace to the exact typed response returned by the selector. At that checkpoint, the joint event, `CompleteFork` support and aggregate invocation tails remained open. The later checked modules below close those claims; the operational runtime connection remains open.

The sampler fixture now records all seventeen source traces and selected positions. The first emitter run was stopped after its process sample showed eager enumeration of the entire extension field in a proof-only `Fintype K` instance, before `main`. That instance is now explicitly noncomputable. The new executable contains no such initialization, and complete fixture emission finished in 323 seconds, including rebuilding dependencies (`/tmp/nightstream-sampler-parity-17-emission-fixed.log`). All seventeen samples succeeded. Its initial state differs from the saved PiRLC phase fixture, so current-source phase data must be aligned before native conformance can close. The three changed Rust parity targets compile in release mode (10.83 seconds); `cargo fmt --all` and `git diff --check` pass. The native assertions were not yet run at that checkpoint. Matching current fixtures and the passed checks are recorded below.

The current-source artifact emitter is `validate.sh emit-poseidon2-hash-chain-v1 <external-path>`. The `per-application-streamed` action emits a different test application and is not a substitute. The four exact tracked LFS reference objects are available on `origin`, but their payloads and the sibling `nightstream-stage1-evidence` archive are absent locally. Restoring those reference bytes alone would not prove conformance with this changed source. Reuse the existing `base-positive` and `actual-child-recursive-positive` gates in `scripts/lean_graph/obligations.json`, then check the actual native prover entrypoint for the selected package. The existing small frontend test uses another matrix/key fixture.

The PiCCS audit found complete existing exponent-order, polynomial, and terminal-consumer proofs for `C.prover.eval_k_index`, `C.prover.eval_a_index`, `C.prover.eval_k`, and `C.prover.eval_a`; their map references still point to definitions. The two source-input records still need an exact `SourceHolds` characterization as the fresh CCS and prior CE witness products. Constructor and common-point checks do not supply those witness facts. The four index/polynomial records are now recorded as complete in the local goal JSON with their existing proved providers and production consumers. The two input records, C probability and C native conformance remain open. Public map statuses are unchanged.

At the 13:20 CDT checkpoint, both external review files were absent. The original full goal remains active. The public map has not been republished in this goal segment.

### External feedback checked against the current tree

The owner's earlier six-condition public-seed approval remains authoritative. The external review's unapproved note predates it; no further approval is needed. The review's 12:34 snapshot does not cover the later proof and fixture changes, which receive their own checks.

The sampler retains 2,176 redundant logical field slots: four positions (67, 82, 84, 99) in each of 544 lanes. At 41 private coordinates per field slot, the cost is 89,216 private coordinates. They are still owned by the `.samplerLogical` allocation and remain in the committed witness. The direct resolver and emitted rows read the corresponding shared First54 values, so the redundant copies have no authority over the sampler result. This layout debt is now explicit in `formal/nightstream-fprime/CONSTRAINT_TREE.md`; no layout compaction or shape/profile change was made. The old four-grid comment now describes the actual 35-grid substitution.

The default macOS Bash 3.2 failure was reproduced in `/tmp/nightstream-static-bash32-before.log`. `check-boundaries.sh` now re-executes an installed Homebrew Bash 4-or-newer before using associative arrays, or reports the required version clearly. `/tmp/nightstream-static-bash32-after.log` reached the real boundary checks under the default system PATH; its remaining failures were the unregistered proof modules then under development, not a shell failure. The final default-macOS-shell boundary check now passes in `/tmp/nightstream-static-bash32-final.log`. The complete boundary, library and axiom gate also passes after module registration.

`tests/Axioms.lean` and `CONSTRAINT_TREE.md` remain at 1,499 lines. New theorem audits use the existing smaller audit files. The automatic sampler regression remains part of the test graph. Its most recent checked lookups took 12,235 ms; moving it to another executable is not needed for the folding-proof contract and could require extra gate wiring.

### Current audited checkpoint: 2026-09-08, 15:20 CDT

Sixteen of the original nineteen records are now closed. The exact records, propositions, current source references and validation are in the JSON. The remaining records are `R.security.probability`, `C.security.probability`, and `C.connection.native`. The goal remains active.

`ActualPiDECCarriedValues` proves the exact carried field and point equalities. The point proof uses a symbolic `Fin.decodeProd_encodeProd` equality and transports it in a separately typed step. `ActualPiDECOutput.selectedRowsAndPublic_imply_output` identifies the recursive carried output with the exact production key output. `selectedRowsAndPublic_imply_step` proves the complete typed step from arbitrary accepted selected rows and the actual four-word public digest. It uses actual decoded proof messages and no honest encoding or caller-supplied acceptance premise. Canonical context authority remains its own contract. This closes `D.connection.output`.

`SourceMembership.sourceHolds_iff_memberships` identifies the fresh CCS and prior CE witness products, with one structure, the selected source order, b=2, a common prior point, and complete separate K/A families. `fixedWidthAcceptedProbe_implies_memberships_or_badEvent` connects that statement to the existing extraction result. These close `C.input.fresh` and `C.input.running` as conditional source-relation contracts. Constructor checks and carried digests do not establish input validity.

The new PiRLC `CoordinateOracleStar`, `CoordinateForkEvent`, `CoordinateOracleCost` and `CoordinateExtraction` modules are fully audited. The exact stopped-response event supplies the complete forks and the existing source-opening consumer, closing `R.security.fork`. Interactive success is at least ε−(K+k)/|C| and the expected oracle-call count is at most K+k+1, including failed retries and abort outcomes. `R.security.probability` remains open: the supplied NNReal work values in `bounds_and_openings` are not proved bounds on an actual implementation's work. The next proposition must connect the operational retry/extraction cost to the proved invocation tails. No uniform law or Fiat-Shamir transfer is asserted for the bounded Poseidon sampler.

Current canonical package, binding and setup data are saved in `../nightstream-stage1-evidence/folding-closure-2026-09-08` relative to the repository. The strict checker verified the separate structural identity, complete setup/context/key binding and every entry of all fourteen logical matrices. Metadata and binding took 18.67 seconds; exact logical conformance completed in 28.35 seconds. Candidate bytes and independent binding were copied to the existing repository artifact paths; production identity constants were not changed.

The PiCCS, PiRLC, PiDEC and application emitters now use the same explicit verifier context; R/D also receive the separate final package identity. They retain the computed PiCCS values, which removes the old mixed-context fixture cut. Current emission took 14.15, 18.49, 20.83 and 21.31 seconds respectively. The standalone sampler fixture and PiRLC phase now have the identical initial state.

All fourteen current release tests passed: PiCCS five (43.56 seconds), PiDEC three (21.72 seconds), PiRLC four (22.30 seconds), sampler two (under 0.01 seconds). Tests cover full public phase results, all seventeen indexed sampler traces, injected shortfall, authoritative family mutations, strict PiDEC bound rejection and all child fields. A stale schema-5 assertion failed against the current schema-6 candidate before correction. Rust formatting and whitespace checks pass.

These checks close `R.connection.native`, `R.connection.schedule` and `D.connection.native` under their stated public-phase scopes. The native R wrapper returns the same combined parent; it then mutates the caller's transcript through the projection schedule. Lean's outgoing value remains the state after sampling. PiDEC adds no challenge and the next PiCCS resets; the two exposed outgoing transcript states are not claimed equal.

The complete current gate passed in `nightstream-folding-current-full-audit.log`: 3,683 library jobs in 17 seconds and 3,720 test jobs in 10 seconds. Only `propext`, `Classical.choice` and `Quot.sound` occur in the new audits. The build reuses unchanged validated modules and regressions. Current source hashes and thirteen validation logs are saved with the external evidence. The root worktree remains uncommitted on `4fc02857c3aa4207f3290739cc62d794ba86f5f9`.

`C.security.probability` remains open. The causal sumcheck draft stopped after its third failed check, at an average-of-a-sum expression. Its core and unchecked trace-consumer drafts are saved outside production in the evidence directory's `causal-sumcheck-draft` folder. The next slice must finish the exact causal false-accept bound, then the mixing and conditional expected-time extraction results. No failed draft is imported or counted.

`C.connection.native` remains open despite passing public phase parity. The actual optimized driver still requires a selected cache and complete honest output openings. The default full-domain route would allocate at least 136 GiB of oracle tables for this profile. The bounded route can reuse the existing prefix oracle and the same optimized driver, provide all seventeen complete source openings, and preserve cache validation on the existing host/Metal paths. No dummy cache, zero-matrix stand-in or signed-copy substitute is sufficient. Reuse the registered base-positive and actual-child-recursive-positive checks and retain their inputs, assignments, commitments, matrices, rounds and opening data.

At 15:20:18 CDT both external review files were absent. The user's pasted feedback was still checked. The 89,216 redundant coordinates remain explicit layout debt. New audits use smaller files. Moving the automatic regression into a separate executable was rejected as unnecessary for this contract; its measured runtime is recorded above.

The website source now has Proof/Link counts C 37/39, R 31/32 and D 26/26. The D denominator includes the child-opening record that was previously excluded as an assumption. Public deployment status is recorded separately in the JSON; local completion counts alone do not prove publication.

The public requirements map was published successfully at 15:31:36 CDT, version 12. The separate site commit is `d1da6a654a71d8257dd7bbba52f96fd086aaf6ea`; the root worktree was not staged or committed. All seven static export tests pass. The first hosting attempt failed with a generic service error; the same checked upload succeeded on retry. Keep future map updates with each checked group closure so completed work is visible promptly.

For the next native slice, reuse `validate.sh recursive-step-fixture <context[4]> <PiCCS-input> <child-running> <output>`. `RecursiveStepFixture.valueIO` already constructs the exact recursive packet and checks prior/public/running alignment, PiCCS acceptance, the point, bounded sampler and PiDEC public split. Do not add another packet builder. Independent assignment and opening checks are still required.

### PiRLC and PiDEC local closure: 2026-09-08, 16:20 CDT

All PiRLC and PiDEC Proof and Link records are closed under their original stated contracts: R 32/32 and D 26/26 on both axes. Seventeen of the original nineteen records are complete. The full goal remains active for `C.security.probability` and `C.connection.native`.

The previous `CoordinateExtraction.bounds_and_openings` accepted two free NNReal work values. Its replacement computes `expectedTotalWork` from the same query and terminal execution used by the probability and source-opening statements. `CoordinateChargedOracle` retains the joint response/clock distribution with summable first moments. `CoordinateCheckedCalls.withChecker` adds the work of the same actual checker invocation whose Bool is proved equivalent to the CE response relation. `CoordinateRetryWork` proves the stopped search and its transition-work sum, including rejected and aborting calls. Its averaging argument permits correlation between response and runtime, and requires no worst-case bound on individual oracle calls.

`PaperForkExtractionWork` executes scalar subtraction, an explicit inverse implementation, assignment subtraction and scalar action, with correctness and work bounds for those primitives. `CoordinateTerminalProgram` consumes the observed endpoints, rejects absent or repeated endpoints, and calls that extractor. It charges control flow and work done before a later failure. `CoordinateTerminalLaw` includes all stopped endpoints and a separate rejected-base exit. `CoordinateTerminalSemantics` identifies every successful return with the original source extractor; a CompleteFork is used only as proof evidence, never chosen to compute output data.

The closing theorem proves witness-success probability at least ε−n/|C|, at most n+1 expected calls, and expected total work at most `(n+1) * E[checked query work] + n*(coordinateWork+3)+2`. `positive_return_implies_openings` proves that every positive-mass returned list is the exact corrected-ambient source-witness list. `expectedTotalWork_polynomial_bound` states the paper's computational premises explicitly: polynomial bounds on the computed checked-call mean and the correct bounded primitive implementations. Finite mean alone is not called polynomial time. The bounded Poseidon sampler's exact shortfall and the Fiat-Shamir transfer remain separate; no cryptographic assumption was added.

The final runtime composer passed its first focused check in two seconds without warnings. The full boundary, library and axiom audit then passed: 3,690 library jobs in seven seconds and 3,727 test jobs in five seconds, with only the permitted axioms. The logs and current source identity are saved as `nightstream-pirlc-runtime-full-audit.log`, `nightstream-coordinate-extraction-runtime-first.log`, and `pirlc-runtime-source-manifest.json` in the external evidence directory. A separate source review confirmed closure of the original interactive R record. No Rust or protocol computation changed, so the preceding fourteen current native phase tests remain the applicable evidence.

At 16:20:19 CDT both external review files were absent. The root worktree remains uncommitted. The public map update is recorded in the JSON publication field after deployment succeeds.

Before the user's R/D priority message, the selected honest base fixture was emitted in 18.06 seconds. Its actual witness, all 6,377,559 logical matrix rows, public projection and selected-key commitment were checked in 70.82 seconds. The external `base-cache` now contains `opening.json`, `carrier.i8`, and `matrix-images.u64`. This advances C native preparation but does not close its full honest-prover contract.

The repaired C causal core and trace-consumer drafts are held outside production in `causal-sumcheck-draft/next-slice`. They have not yet had their new focused checks. The existing original drafts are preserved in the parent folder. Resume those checks and the same-driver complete-oracle connection when continuing the two remaining C records; do not repeat closed R/D work.

The R/D map update was published successfully at 16:27:19 CDT, version 13, and its live JSON was verified equal to the checked source. Public counts are R 32/32 and D 26/26 on both axes. The separate site commit is `3f56cc1237c181b81226335c35da2098e33ee41e`. The full goal remains active for the two C records.

### PiCCS causal and honest-base checkpoint: 2026-09-08, 17:03 CDT

The public map still shows all R/D Proof and Link records closed. Its live JSON was checked against the site source. The original two C records remain open.

`GoldilocksCausal` now proves the degree-times-rounds error bound for arbitrary prefix-dependent messages over the actual extension field. `GoldilocksCausalTrace` connects that event to the existing decoded certificate, verifier acceptance and abort behavior. `SignedMixingProbability.mixingProbability_fullField_le` proves the exact paper numerator `(k*d*(t+1)+2*K+k-1+log2(m))/|Kfield|` for the protocol's signed coefficient object. A fixed coefficient is selected before the independent alpha/gamma draws; its actual Boolean table gives the alpha bound.

`CausalExecution.run_implies_receipt` connects the same prover program to its sampled coins and raw messages. `IndependentExecution.agreementProbability_le` then proves B.2 equation (16) for a fixed first witness and a fixed second prover tape. Success and witness agreement are events inside the unchanged sampling law. They do not condition the fresh coins. A separate source review found no defect in these local results. Arbitrary context/private-coin averaging, the global square-root error bound, and the actual one-call extractor with its valid-witness consumer and conditional expected-time bound remain required.

All eight new modules are reachable from the library root, and all 24 exported theorems have axiom audits. The complete boundary, library and test gate passed in `nightstream-causal-mixing-full-audit.log`: 3,698 library jobs in four seconds and 3,735 test jobs in seven seconds. Only the three permitted axioms occur. The source and fixture manifest is `piccs-causal-mixing-source-manifest.json` in the external evidence directory.

The selected honest base source produced all 28 scalar rounds in 2.35 seconds and all 15 complete ring families in 91.76 seconds. Lean accepted the assembled proof in seven seconds. The shared optimized driver now accepts the complete prepared oracle, rebinds its challenges and requires every output family. Its existing host/Metal route retains matrix-cache validation. No dummy cache or alternate verifier relation was added. The complete selected base native run passed in 20.58 seconds; the prover portion took 1.92 seconds. Its full result also matched Lean and both Rust verifiers in 18.63 seconds. Four driver tests and eleven oracle tests passed, covering challenge binding, required outputs, exact source order and arithmetic. Rust formatting and whitespace checks pass.

This native evidence is still partial. Independent source-opening checks, full physical/logical raw-assignment checks, actual-child recursive inputs and outputs at both points, and the remaining registered mutation/ownership gates must pass before `C.connection.native` closes. Use the existing inventory at `scripts/lean_graph/obligations.json` in the repository root. Next probability drafts stay outside the production library until their focused checks. The root worktree remains uncommitted; the website counters are unchanged because neither C record is complete.

### Global PiCCS probability accounting: checked 2026-09-08

`StrongProbability.source_error_le_sqrt` now proves the global B.2 error bound. It defines actual relaxed success, source validity, extraction error and successful witness disagreement from the same `CausalExecution.run`. Two independent executions resample the private tape and verifier coins under each fixed context. Arbitrary PMFs describe private tapes and setup/adversary contexts. The proof derives the pair bound from the actual event partition and equation (16), then uses weighted averaging and the squared-mean inequality. No caller-supplied independence or numerical pair bound reaches the exports. `source_success_ge` gives the corresponding source-success bound; `source_success_ge_of_disagreement_le` accepts the paper's separate bound on the named actual disagreement event.

The focused pair/context check passed in two seconds, and an independent source review found no soundness gap in equations (13)–(20). The complete boundary, library and axiom gate passed in `nightstream-global-probability-full-audit.log`: 3,739 library jobs in five seconds and 3,776 test jobs in eight seconds. All three new exports use only the allowed axioms. The current source/input manifest is `piccs-global-probability-source-manifest.json` in the external evidence directory.

`C.security.probability` remains open for the actual one-call extractor, its accepted-ambient check, fresh witness-tail projection and full running-witness preservation, and the conditional expected-time consumer. The new `WitnessProjection` draft failed three focused checks and stopped under the project rule. Its final errors concern normalization inside products and explicit `Fin.addCases` arguments. The exact draft and all diagnostics were saved outside production before removing that unregistered file. `causal-sumcheck-draft/next-slice/WitnessProjection-next-slice.md` records the remaining repairs and the concrete Phi81 public-prefix consumer. The unit-read projection is only a value/count reference; the final runtime must use a correct costed access implementation. The unchecked `StrongProbabilityClock.lean.inc` draft retains the same tape and public coins and must prove total-clock summability as well as the work bound.

Native checks also advanced. Current Lean physical expansion took eight seconds. Every Rust physical matrix entry matched it in 23.20 seconds, with nonzero counts `[93701820,39358148,28868018]`. The raw base assignment passed all 29,225,729 physical rows, every logical coordinate, all 37 alignment zeros, and all 6,377,559 logical rows in 35.39 seconds. Independent opening tests passed for Pad and A0–A7, including all 54 coefficients and the running input/output families. Their individual times were 19.71–38.45 seconds. Exact logs and completed gate names are recorded in `native-gate-results.json`.

Native work still required: base A8–A13, CCS and selected-commitment opening checks; the actual-child recursive case with all input/output families and raw assignments; and the remaining registered mutation/ownership gates. The external `native-conformance-requests` directory contains exact requests and commands from the existing 91-gate inventory. Those files are not completion evidence. Reuse matching passed gates and continue from the remaining ones. At the 17:20:10 CDT checkpoint both external review files were absent. Both C records remain open; the original goal and published counters are unchanged.

### Checked extractor providers and child commitments: 2026-09-08

The resumed canonical `WitnessProjection` proof now passes. It returns the exact fresh private tails and full running vectors, and reattaches only the statement-owned Phi81 public prefix. `CostedWitnessProjection` executes an accessor whose returned values and clocks stay together. Its correctness theorem identifies the same copied coordinates, and its bound includes the actual accessor work and every vector constructor. Arbitrary assignment functions are not treated as constant-time operations in the runtime path.

`CheckedWitnessExtraction` passes its first focused check. It invokes the exact accepted-ambient checker once on a returned candidate, handles abort and rejection explicitly, and uses the costed projection. Ambient opening validity derives the concrete Phi81 public prefix, so reconstruction yields the same returned witness. `finish_source_iff` identifies its returned-source event with the event in the proved probability model. `sourceReturned_iff_memberships` connects that result to the existing fresh CCS and prior CE witness products.

The review found that the first final-runtime draft imposed a common adversary-time bound on every context. That was stronger than the intended globally EPT experiment. The replacement draft uses the actual joint context/tape PMF and one global call/check summability and polynomial-work premise. A deterministic accessor's PPT bound remains explicit. The proved `jointTapeLaw_apply` identifies the exact conditional product mass; `joint_clockMean_eq_nested` proves bounded-event summability and equality with the nested probability experiment. Unbounded runtime must use the joint clock directly, so a divergent inner real sum cannot masquerade as a finite expectation.

The final `OneRunExtraction` integration is still open. Its third focused check failed at two event-indicator equalities; the events agree mathematically, but different decision constructions prevented the proposed definitional equalities. The exact OneRun and dependent StrongExtraction drafts, all three diagnostics, and peer-review notes are preserved outside production under `causal-sumcheck-draft/next-slice`. Only those new unregistered drafts were removed. No fourth attempt or limit override was made. The next repair must use explicit indicator normalization or true/false cases in the indicator's owner, then finish the actual global probability/runtime consumer.

The completed providers and joint-law helpers passed the full boundary, library and axiom gate: 3,748 library jobs and 3,785 test jobs, eight seconds each. All new exports use only the allowed axioms. Evidence is in `nightstream-checked-extractor-providers-full-audit.log` and `piccs-checked-providers-source-manifest.json` in the external evidence directory. Both C requirement records remain open.

All direct base-positive opening checks now pass, including A8–A13, all 6,377,559 CCS rows, and every selected-key commitment row and coefficient. The actual first-fold parent has maximum norm 62. Its six lower digit children were generated individually; all ten high digits were checked to be zero. All 16 commitments recompose to the actual parent, and the independent checker verified all 22 rows and 19,008 child commitment coefficients in 58.85 seconds. Parent recomposition took 18.75 seconds. All 15 child evaluation families at the prior point were generated in 103.83 seconds; their independent checks have not run.

`native-gate-results.json` now records 24 of the 91 registered gate executions. The pilot-row prerequisite remains explicit. The next native work is the independent child input-family checks and recomposition, then the existing recursive fixture builder, full recursive assignment checks, actual native recursive prover with all fresh/child output families, and the remaining pilot/mutation/ownership gates. `assemble_recursive_data.py` only reshapes checked JSON; it is not a witness or proof builder and has not been run. At the 18:20 checkpoint both external review files were absent. The root worktree remains uncommitted and the original full goal stays active.

### PiCCS interactive extraction: closed 2026-09-08

`C.security.probability` is closed. Eighteen of the original nineteen records are proved and connected. `C.connection.native` is the only open record.

The resumed `OneRunExtraction.successProbability_eq` passed its first focused check in two seconds. Explicit true/false cases, plus normalization inside the probability owner, resolved the indicator equality without changing the experiment or increasing proof limits. `StrongExtraction.probability_and_expected_work` also passed its first focused check in two seconds. It bounds the actual returned-source probability by relaxed success minus the square root of the actual disagreement probability plus the proved mixing and causal sumcheck errors. The same theorem proves total-clock summability and polynomial expected work for that execution. The actual global call/check mean and correct bounded access implementation are explicit computational premises; rare expensive contexts and individual calls remain allowed.

The public-prefix reconstruction and fresh CCS/prior CE source products use the existing Phi81 relation. All eight new exports and the probability normalization helper are registered in the axiom audit. The complete boundary, library and axiom gate passed: 3,750 library jobs in four seconds and 3,787 test jobs in seven seconds. Only the three permitted axioms occur. Independent source review found no remaining scope mismatch for this conditional interactive record. Fiat-Shamir transfer and setup hardness stay separate.

Evidence is saved in `nightstream-piccs-extraction-full-audit.log` and `piccs-extraction-source-manifest.json` in the external evidence directory. The map update raises PiCCS Proof and Link to 38/39; publication is recorded separately in the goal JSON after deployment succeeds. Native child-family and recursive conformance checks continue. The root worktree remains uncommitted.

### PiCCS native conformance: closed 2026-09-08

All nineteen original Proof and Link records are now closed. PiCCS is 39/39 on both axes, PiRLC 32/32, and PiDEC 26/26. Publication of the final map is recorded in the JSON after deployment succeeds.

The complete selected base and actual-child recursive native prover cases pass all 91 registered conformance checks. The recursive input returned by the native driver is byte-identical to the input already accepted by Lean. Both Rust verifiers agree with the complete Lean phase result. Independent checks cover the final expanded matrices, every physical/logical assignment row and column, all source openings and commitments, all sixteen children at both points, exact parent/caller handoffs, twelve-owner wiring, and every registered rejection branch. The slowest final gate was the complete recursive prefix check at 116.48 seconds, below the required 300-second cap. No test was skipped or changed to make the inventory close.

The original twelve-owner source map and the changed native driver received separate source reviews. The exact source cut and all retained logs are in the external evidence directory. All 166 entries in the checked extraction proof/source/input manifest still match; no Lean or Rust source changed during the final native checks. Both external review files were absent at the 19:20 checkpoint.

This closes the selected PiCCS native-conformance record. Universal Rust correctness, the complete Stage 1 production theorem and a production backend remain separate requirements. The approved fixed public-seed Nightstream MSIS premise retains all six owner conditions; no uniform-matrix reduction or paper security estimate is inherited. The root worktree and production pins remain unchanged by publication.

The final public map was published successfully at 19:28:14 CDT and its live JSON matches the checked source. The separate site commit is `e57c018ccc7c2ceb223d41afcb1dad156298db6b`, version 15. All seven export tests passed. `native-final-source-input-confirmation.json` retains the exact 91-gate completion, unchanged reviewed source, and 79 input hashes, including raw caches. All work required by this folding closure contract is complete.
