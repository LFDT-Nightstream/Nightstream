import Nightstream.Implementation.R1CS.Artifacts.FPrimeSelectiveFixedPoint.PiCcsNc.DelayedProjection.CombinedNc.Generated.Metadata

/-!
Stable artifact facade for fixed-profile combined-NC metadata.

Owns: the handwritten import boundary for generated dimensions, source and
emitted row counts, row ranges, and the delayed-NC boundary column map.

Does not own: coefficient decoding, row satisfaction, semantic acceptance,
transcript authority, commitment binding, costs, or row removal.

| Stable stage path | Generated owner | Authority class |
Emits constraints: no.

|---|---|---|
| `f_prime.pi_ccs_nc.delayed.combined.metadata` | `Generated.Metadata` | computed artifact |
-/
