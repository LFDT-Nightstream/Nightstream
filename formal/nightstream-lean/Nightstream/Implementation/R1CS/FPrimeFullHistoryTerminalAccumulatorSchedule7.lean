import Nightstream.Implementation.R1CS.FPrimeFullHistoryTerminalAccumulatorCheckCoverage

/-! Generated exact shifted-ternary row schedules, shard 7. -/

namespace Nightstream.Implementation.R1CS.FPrimeFullHistoryTerminalAccumulatorSchedule

open Nightstream.Implementation.R1CS

set_option maxRecDepth 1048576

def map112 :=
  FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD 112 default

theorem map112_rows_schedule :
    (FPrimeFullHistoryTerminalAccumulator.segment1Rows.drop 3474).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map112)) := by
  native_decide

def map113 :=
  FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD 113 default

theorem map113_rows_schedule :
    (FPrimeFullHistoryTerminalAccumulator.segment1Rows.drop 3598).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map113)) := by
  native_decide

def map114 :=
  FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD 114 default

theorem map114_rows_schedule :
    (FPrimeFullHistoryTerminalAccumulator.segment1Rows.drop 3722).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map114)) := by
  native_decide

def map115 :=
  FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD 115 default

theorem map115_rows_schedule :
    (FPrimeFullHistoryTerminalAccumulator.segment1Rows.drop 3846).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map115)) := by
  native_decide

def map116 :=
  FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD 116 default

theorem map116_rows_schedule :
    (FPrimeFullHistoryTerminalAccumulator.segment1Rows.drop 3970).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map116)) := by
  native_decide

def map117 :=
  FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD 117 default

theorem map117_rows_schedule :
    (FPrimeFullHistoryTerminalAccumulator.segment1Rows.drop 4094).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map117)) := by
  native_decide

def map118 :=
  FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD 118 default

theorem map118_rows_schedule :
    (FPrimeFullHistoryTerminalAccumulator.segment1Rows.drop 4218).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map118)) := by
  native_decide

def map119 :=
  FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD 119 default

theorem map119_rows_schedule :
    (FPrimeFullHistoryTerminalAccumulator.segment1Rows.drop 4342).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map119)) := by
  native_decide

def map120 :=
  FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD 120 default

theorem map120_rows_schedule :
    (FPrimeFullHistoryTerminalAccumulator.segment1Rows.drop 4466).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map120)) := by
  native_decide

def map121 :=
  FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD 121 default

theorem map121_rows_schedule :
    (FPrimeFullHistoryTerminalAccumulator.segment1Rows.drop 4590).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map121)) := by
  native_decide

def map122 :=
  FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD 122 default

theorem map122_rows_schedule :
    (FPrimeFullHistoryTerminalAccumulator.segment1Rows.drop 4714).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map122)) := by
  native_decide

def map123 :=
  FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD 123 default

theorem map123_rows_schedule :
    (FPrimeFullHistoryTerminalAccumulator.segment1Rows.drop 4838).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map123)) := by
  native_decide

def map124 :=
  FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD 124 default

theorem map124_rows_schedule :
    (FPrimeFullHistoryTerminalAccumulator.segment1Rows.drop 4962).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map124)) := by
  native_decide

def map125 :=
  FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD 125 default

theorem map125_rows_schedule :
    (FPrimeFullHistoryTerminalAccumulator.segment1Rows.drop 5086).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map125)) := by
  native_decide

def map126 :=
  FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD 126 default

theorem map126_rows_schedule :
    (FPrimeFullHistoryTerminalAccumulator.segment1Rows.drop 5210).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map126)) := by
  native_decide

def map127 :=
  FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD 127 default

theorem map127_rows_schedule :
    (FPrimeFullHistoryTerminalAccumulator.segment1Rows.drop 5334).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map127)) := by
  native_decide

theorem shard7_rows_schedule
    (index : Nat) (lower : 112 ≤ index) (upper : index < 128) :
    let map := FPrimeFullHistoryTerminalAccumulator.shiftedTernaryMaps.getD index default
    ((FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows map).drop
        (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart map)).take 124 =
      ShiftedTernaryCompiler.canonicalRows.map
        (Relabel.row
          (FPrimeFullHistoryTerminalAccumulatorCheckCoverage.columnMap map)) := by
  have cases : index = 112 ∨ index = 113 ∨ index = 114 ∨ index = 115 ∨ index = 116 ∨ index = 117 ∨ index = 118 ∨ index = 119 ∨ index = 120 ∨ index = 121 ∨ index = 122 ∨ index = 123 ∨ index = 124 ∨ index = 125 ∨ index = 126 ∨ index = 127 := by omega
  rcases cases with rfl | rfl | rfl | rfl | rfl | rfl | rfl | rfl | rfl | rfl | rfl | rfl | rfl | rfl | rfl | rfl
  · simpa [map112,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart] using
        map112_rows_schedule
  · simpa [map113,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart] using
        map113_rows_schedule
  · simpa [map114,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart] using
        map114_rows_schedule
  · simpa [map115,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart] using
        map115_rows_schedule
  · simpa [map116,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart] using
        map116_rows_schedule
  · simpa [map117,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart] using
        map117_rows_schedule
  · simpa [map118,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart] using
        map118_rows_schedule
  · simpa [map119,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart] using
        map119_rows_schedule
  · simpa [map120,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart] using
        map120_rows_schedule
  · simpa [map121,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart] using
        map121_rows_schedule
  · simpa [map122,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart] using
        map122_rows_schedule
  · simpa [map123,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart] using
        map123_rows_schedule
  · simpa [map124,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart] using
        map124_rows_schedule
  · simpa [map125,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart] using
        map125_rows_schedule
  · simpa [map126,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart] using
        map126_rows_schedule
  · simpa [map127,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedOwnerRows,
      FPrimeFullHistoryTerminalAccumulatorCheckCoverage.shiftedLocalRowStart] using
        map127_rows_schedule

end Nightstream.Implementation.R1CS.FPrimeFullHistoryTerminalAccumulatorSchedule
