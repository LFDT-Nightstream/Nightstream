# Shared foundation coverage

This source review supplies the `F` branch in `foundations.json`. It contains 104 nodes, including 87 reusable leaves. The scope is the shared mathematics and the selected primitive implementations. No build or test was run. No production file or frozen-project file was read or changed for this task.

The paper authority is the local SuperNeo v1_1 text:

- `docs/superneo-paper-v1_1/04_preliminaries.md`: field tower and dimensions (lines 19–39), coefficient maps and norms (45–61), modules and MSIS (63–65), low-norm invertibility and strong sets (67–73), and commitments and binding (75–101). Polynomial, MLE, and zero-test facts at lines 5–15 are also covered.
- `docs/superneo-paper-v1_1/05_embeddings_and_evaluation_homomorphism.md`: the embeddings, flattening, and packing definitions (3–47); the constant-term pairing and transform (53–81); inner-product, matrix, and coefficient-evaluation equations (95–143); the evaluation homomorphism (147–173); and the selected padding specialization (179–199).
- `docs/superneo-paper-v1_1/08_concrete_parameters.md`: the common specialization at line 3 and the Goldilocks reference parameters at lines 15–21. The BabyBear and Mersenne31 profiles are not requirements for the selected Nightstream profile.
- `docs/superneo-paper-v1_1/11_appendix_B_deferred_theorems_and_proofs.md`: the primitive norm calculation in B.3 (313–325), the split and recomposition calculation in B.4 (538–564), B.5's inversion and expansion scripts (566–715), and B.6's estimator setup and Goldilocks inputs (717–776). The scripts were read, not executed. The cited external analytic and lattice results were not independently proved or checked.

Each leaf is one reusable operation, equation, encoding condition, or proof fact. A quantified block or coordinate law remains one leaf. The hierarchy does not make a separate requirement for each repeated coordinate. Generic paper constructions that collapse under `L=F`, `tau=1`, and `A=K` are recorded as the selected specialization. They do not create work for an unused field tower or a general extension-flattening API.

Status meanings are local. `proved` identifies a source theorem for the stated local fact; it does not report a new kernel build. `definition` identifies a source construction without claiming a theorem for every property that a caller could need. `partial` preserves an explicit premise or an uncovered part of the requirement. `assumption` identifies a mathematical or computational boundary that the inspected source does not discharge. `not_required` applies only to the stated fixed-profile or local contract. `implemented` means that Rust source exists; no Rust test is marked as passed by this review. A `connected` value means that the local consuming definition or theorem uses the primitive. It does not mean full protocol, compiler, production, or Rust conformance approval.

The main qualifications are:

- `F.field.base.prime` has an unconditional Goldilocks primality and no-zero-divisor source proof. `F.field.extension.laws` supplies ring and interpolation laws. `F.field.extension.irreducible` remains separate: `SevenProjectiveNonresidue` is a premise of `extensionNoZeroDivisors_of_base_and_seven` in `formal/nightstream-fprime/NightstreamFPrime/Spec/Folding/PiCCS/PaperJoint/ConcreteCarrier/NoZeroDivisors.lean:51` and `:105`. No unconditional witness was found in the inspected active Spec sources. The existing Rust Euler-criterion test was not run and is not a Lean witness.
- `F.strong_set.low_norm_invertibility` is an explicit assumption at `formal/nightstream-fprime/NightstreamFPrime/Spec/Phi81StrongSet.lean:213`. The concrete number-theory conditions and the implication from that assumption to unit differences are separate proved leaves. The source's `theorem8` names use older numbering; the v1_1 statement is Theorem 4 at `04_preliminaries.md:67`.
- `F.norm.expansion` records the proved bound for a valid challenge acting on a fresh block with every coefficient magnitude below 2. The result is a coefficient bound of 216. It does not claim the paper's generic norm ratio for every ring value. This exact specialization supplies the inspected local consumer.
- `F.strong_set.cardinality` keeps the exact `5^54` set size separate from the 125-bit descriptor. The finite function type and injective embedding are present. No named theorem connecting the cardinality of `ProductionMember` to the verifier's quantitative set-size parameter was found in this review.
- `F.transform.rust_connection` is open. `crates/neo-math/tests/phi81_bar_lean_artifact.rs:18` names an artifact in the frozen project; its drift path can write there at lines 65–71. The test was not run and the target was not read. This test is not current F′ primitive conformance evidence. The active closed-form dual-matrix theorem and the Rust runtime inverse-Gram implementation remain distinct evidence surfaces.
- `F.profile.values` uses the selected Nightstream values `k=16`, `B=65536`, and `kappa=22`. The paper's Goldilocks estimate uses `k=14`, `kappa=18`, and a different message width. `F.profile.msis_assessment` does not transfer that estimate. `F.profile.combined_bound` distinguishes the proved numeric stage values from side conditions not separately proved by the cited stage theorem.
- `F.setup.prg_assumption` and `F.setup.reduction_bias` distinguish the paper's uniform matrix distribution from the selected fixed-seed indexed ChaCha20 construction and 256-bit modular reduction. Value-range proofs and vector tests cannot establish these distribution claims. The binding leaves also retain their exact opening bounds and setup assumptions.

The `C`, `R`, `D`, and `N` branches own protocol messages, acceptance checks, extraction, Fiat–Shamir sampling, and probability composition. HyperNova and circuit/compiler/production work are outside this file. B.1, B.2, and the protocol portions of B.3/B.4 are not claimed as foundation coverage. A theorem's source location under a protocol directory does not expand the scope: only its reusable algebraic fact is indexed here.

The JSON was checked with a read-only parser for valid syntax, unique IDs, local parent/dependency resolution, dependency cycles, existing referenced files, valid line numbers, and the presence of each code symbol on its cited line. This checks the report structure and references. It does not replace proof checking, code review, or primitive conformance execution.
