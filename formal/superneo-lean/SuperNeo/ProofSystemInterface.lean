import SuperNeo.ProofSystem

/-!
Barrel interface for `SuperNeo.ProofSystem` (Sections 6-7 (proof-system facade)).

The authoritative export set is the import list of `SuperNeo/ProofSystem.lean`;
per-module theorem boundaries live in the colocated `*Interface.lean` files
next to each implementation module.
-/
