import OpeningConvergence.SuperNeoExtensionBridgeInterface

/-!
# Module 6: SuperNeoExtensionBridge — Implementation

Owns the canonical split encoding from extension-field opening claims over
`SuperNeo.KExt` into the current base-field SuperNeo CE statement surface.

## Spec
See `specs/SuperNeoExtensionBridge.spec.md`
-/

namespace OpeningConvergence.SuperNeoExtensionBridge

-- The theorem-facing bridge currently lives in the Interface file.
-- Keep this file as the implementation owner if later extracted proofs or
-- concrete CE-bridge theorems need a separate home.

end OpeningConvergence.SuperNeoExtensionBridge
