# Blind source-only read-back

This review used a fresh context and source declarations only. No build was run. No paper or goal text was used as interpretive input.

**1. `Poseidon2HashChainV1Closure.rowsZero_implies_stepHoldsFor`.** This theorem quantifies over a raw packet and a proof that its bound, encoded assignment satisfies every row. A raw packet contains three arrays of Goldilocks field values: base values indexed by the physical package width; 52,326 groups of 33 values; and 17 × 64 product values. The field has modulus \(q=18446744069414584321\). The application is fixed: it hashes the 40 field-encoded bytes of `Nightstream/Stage1/Poseidon2HashChain/v1`, followed by four current-state words and four witness words. `bound` replaces four specified base columns with the fixed verifier-context digest. It preserves the other raw values. `assignment` places the encoded output digest in the public prefix, then places the prescribed block encodings in their fixed order. Bit and centered-value slots retain one field value; general field slots use 41 balanced ternary digits. `RowsZero` means that, for every live row, evaluation of the fixed 74-monomial polynomial on that row’s 14 sparse linear-form values equals zero in the field. The fourteenth form is zero. `structuralPlan` selects these forms for the fixed application; `relation` uses their coefficients as its matrices and uses zero matrices on padding rows. The theorem concludes `StepHoldsFor` for the input and output decoded from this same bound packet, with the fixed application, relation, Ajtai key, and verifier-context digest. The decoded input contains the natural representative of the prior iteration field word, four initial-state words, four current-state words, the prior running data, the decoded fresh commitment and public input, program counter 1, four application witness words, and the complete decoded NIFS proof. The decoded output contains four next-state words, next running data, function index 0, and the decoded four-word output digest. There is one outer running slot. Its value contains 16 commitments, 16 public inputs, 16 evaluation families, and one shared point in the quadratic extension \(F[X]/(X^2-7)\). `StepHoldsFor` asserts all of the following: the output function index is 0; the next application state equals the fixed application applied to the current state and witness; the output digest equals the Poseidon2 hash of the serialized tuple consisting of the fixed context digest, input iteration plus one, initial state, next state, next running data, and counter 1; and one of two branches holds. In the base branch, iteration is zero, initial and current states are equal, and every component of the next running data is zero. In the recursive branch, iteration is positive, the fresh public input equals the digest encoding of the prior tuple, and the concrete NIFS verifier returns exactly the decoded next running data. The digest encoding is 270 field cells: marker 1, 256 little-endian bits from four canonical field representatives, and 13 zero cells. The fixed Ajtai key has 22 rows and 4,708,530 ring columns; its coefficients are defined from the fixed 32-byte seed by the indexed ChaCha20 256-bit output reduced modulo \(q\). The context digest is a Poseidon2 hash of the fixed descriptor of the relation, application, NIFS key, commitment setup, profile, and schedule. This declaration has no `LowNormInvertibility` parameter.

Sources: [target:35](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Export/Stage1/Poseidon2HashChainV1Closure.lean:35), [raw packet:59](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Export/Stage1/PerApplicationCanonicalAssignment.lean:59), [binding:50](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Export/Stage1/PerApplicationVerifierBoundAssignment.lean:50), [decoded input/output:502](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Export/Stage1/PerApplicationDecodedIO.lean:502), [row predicate:21](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Layout/ProductionRelation/PlanComposition.lean:21), [relation construction:76](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Export/Stage1/PerApplicationFixedPoint.lean:76), [step definition:98](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Lifecycle/Relation.lean:98), [transition conditions:337](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Spec/HyperNova/Construction2/Paper.lean:337), [application:47](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Lifecycle/Stage1/Poseidon2HashChainV1.lean:47), [fixed setup:80](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Export/Stage1/Poseidon2HashChainV1Setup.lean:80), [context digest:533](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Export/Stage1/PerApplicationCanonicalPackage.lean:533).

**2. `ActualStep.selectedRowsAndPublic_step_iff_baseOrPiDec`.** This theorem quantifies over an application program, a `FitsTwoPow28` proof for that program, an arbitrary Ajtai key of the required dimensions, an arbitrary logical assignment, a template NIFS proof, a digest, and three hypotheses. The application program contains a witness width, a state-transition function, a circuit, and proofs that the circuit specification equals that function’s input/output equality, that earlier input wires satisfy the circuit assumptions, and that its constraints use only permitted wires. `FitsTwoPow28` requires the physical package’s row and column counts, and the completed carrier width, to be at most \(2^{28}\). The logical assignment is a function from its finite column index set to \(F\). The template contains 28 degree-at-most-9 sumcheck polynomials, the complete PiCCS output evaluations for 17 sources, and 16 PiDEC commitments and evaluation families. The three hypotheses are: the digest has four words; the public projection of the assignment, after zero extension to the complete 54-word-aligned carrier, equals the 270-cell digest encoding described above; and every live row satisfies `RowsZero` as defined above. Prior and output state words are evaluations of the prescribed sparse forms on the assignment, with zero returned outside the state-word range. The context key is the four-word slice of the decoded prior state. `decodedFresh` evaluates the fresh commitment and public-input expressions on the assignment. `withDecodedPiCCS` replaces the template’s sumcheck polynomials and PiCCS output evaluations with the values decoded from the assignment; it retains the template’s PiDEC commitments and evaluations. Let `running` and `next` be the running data decoded from the prior and output states. Let `key` be the production NIFS key constructed from the application’s derived relation and the supplied Ajtai key. The conclusion is an equivalence: the decoded input/output satisfy the complete step relation described in paragraph 1, using this application and decoded context, if and only if the decoded iteration is zero **or** both `piDecCheck key running fresh proof = true` and `key.output running fresh proof = some next`. The right side does not separately require positive iteration in its second branch. `piDecCheck` is false if no PiDEC attempt can be constructed. An attempt is constructed only when the PiRLC sampler returns its 17 ring challenges; these challenges combine the PiCCS output statements into a parent. A successful PiDEC check requires every parent public coordinate to have centered magnitude strictly below \(2^{16}\), the parent’s stage to be `.combined`, the prescribed evaluation-array sizes, and exact equality of the parent commitment and evaluations with the sums of the 16 child messages weighted by \(2^j\). `key.output` additionally requires the checked public-input split to succeed. It forms running data from the parent’s point, the 16 supplied child commitments and evaluation families, and the coordinatewise signed binary split of the parent public input. The equality to `some next` compares that complete computed value with the assignment-decoded next running data.

Sources: [target:350](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Export/Stage1/ActualStep.lean:350), [input/output and proof decoding:28](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Export/Stage1/ActualStep.lean:28), [program type:88](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Lifecycle/Stage1/Application.lean:88), [fit type:38](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Export/Stage1/PerApplicationFixedPoint.lean:38), [retained template fields:159](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Lifecycle/PiCCS/v1_1/Formal.lean:159), [PiDEC check:66](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Spec/Folding/Nifs/PaperNonInteractive/Verifier.lean:66), [PiDEC conditions:226](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Spec/Folding/PiDEC/PaperVerifier.lean:226), [computed output:704](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Spec/Folding/Nifs/PaperNonInteractive/Types.lean:704).

**3. `Poseidon2HashChainV1Closure.rowsZero_implies_base_or_securityOutcome`.** This theorem quantifies over the same raw packet as paragraph 1, a proof of `LowNormInvertibility`, and the same bound-assignment row hypothesis as paragraph 1. The extra assumption says: if the fixed `Theorem8Conditions` hold, then for every ring value \(v\) and natural bound \(h\), if \(v\ne0\), every coefficient has centered magnitude at most \(h\), and \(3h^2<q\), there is a ring value \(w\) such that \(vw=wv=1\). Ring values have 54 Goldilocks coefficients, with multiplication reduced modulo \(X^{54}+X^{27}+1\). Centered magnitude of a field representative \(a\) is \(\min(a,q-a)\). The fixed conditions are \(3\mid81\), \(q\bmod3=1\), exact multiplicative order 27 of \(q\) modulo 81, and \(27=81/3\). The conclusion is a conjunction: the complete step relation from paragraph 1 holds; and either the decoded input iteration is zero, or it is positive and `SecurityOutcome` holds for the fixed production NIFS key, the input’s only outer running slot, its fresh statement, and its decoded proof. The extraction algebra is the fixed ring/module algebra for this commitment key. The strong-set record, constructed from `LowNormInvertibility`, supplies an inverse for the difference between any two distinct challenges whose 54 coefficients belong to \(\{-2,-1,0,1,2\}\). `SecurityOutcome` is an inductive proposition with six possible constructors. Its **knowledge** constructor provides an aligned complete fork, the exact verifier-computed PiDEC attempt, valid openings of all 16 computed children, validity of all extracted source assignments, and equality between the fork’s base assignment and the sum of the child assignments weighted by \(2^j\). A complete fork consists of a base challenge vector and assignment, plus one challenge vector and assignment for each of the 17 coordinates; all challenges are members of the specified set, each coordinate fork changes that coordinate and agrees elsewhere, and every response assignment opens its corresponding computed combined statement with centered norm strictly below \(2^{16}\). Alignment means that the base challenge vector is exactly the vector returned by the verifier’s sampler. Extraction at coordinate \(j\) applies the inverse of the base-minus-fork challenge difference to the base-minus-fork assignment difference. Source validity requires all 17 extracted assignments to reproduce their commitments and public inputs with centered norm strictly below 2; the fresh assignment must satisfy the CCS polynomial on every Boolean row; and the prior running Pad and matrix evaluation claims must equal the evaluations of their corresponding coefficient matrices on the extracted assignments. A valid child opening similarly reproduces that child’s commitment, computed public input, and evaluation family at the parent point, with centered norm strictly below 2. The **mixingFailure** constructor provides an aligned fork for which the signed coefficient object is not identically zero but evaluates to zero at the verifier’s sampled \(\alpha,\gamma\); this object contains the Pad and matrix evaluation residuals, followed by the negated CCS and norm residual polynomials. The **sumcheckFailure** constructor provides an aligned fork and a fixed-width certificate decoded from the submitted rounds, containing a round whose claimed and expected polynomial functions differ but agree at that round’s challenge; both functions have degree-at-most-9 representations, and the expected function is computed by summing the protocol polynomial over the remaining Boolean coordinates. The **parentBindingFailure** constructor provides an aligned fork, the exact computed attempt, valid child openings, and two distinct assignments that produce the same parent commitment, both with centered norm strictly below \(2^{16}\). The **piRlcForkingFailure** constructor states that no aligned complete fork exists. The **piDecChildOpeningFailure** constructor provides the exact computed attempt and states that no family of valid openings for all its children exists. The theorem does not select one constructor in its type and states no probability bound.

Sources: [target:64](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Export/Stage1/Poseidon2HashChainV1Closure.lean:64), [invertibility assumption:213](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Spec/Phi81StrongSet.lean:213), [fixed extraction parameters:153](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Export/Stage1/PerApplicationSecurity.lean:153), [six constructors:466](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Spec/Folding/Nifs/PaperSecurityComposition.lean:466), [alignment and child openings:387](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Spec/Folding/Nifs/PaperSecurityComposition.lean:387), [complete fork:216](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Spec/Folding/PiRLC/PaperForkExtraction.lean:216), [extraction formula:283](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Spec/Folding/PiRLC/PaperForkExtraction.lean:283), [source validity:489](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Spec/Folding/PiCCS/PaperJoint/StrongReduction.lean:489), [mixing predicate:385](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Spec/Folding/PiCCS/PaperJoint/SignedCoefficientObject.lean:385), [sumcheck predicate:447](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Spec/SumCheck/FixedPhase.lean:447), [parent collision:422](/Users/nicarq/starstream/develop/nightstream-clean-up/formal/nightstream-fprime/NightstreamFPrime/Spec/Folding/PiDEC.lean:422).

Terms not fully expanded to scalar equations: the complete per-column sparse-form and environment maps; the complete 74-monomial row polynomial; Poseidon2 constant tables and ChaCha20 rounds; the basis-level assignment action and coefficient-matrix transforms used by extraction. Their roles and surrounding predicates are resolved above. I ran no builds and made no edits.
