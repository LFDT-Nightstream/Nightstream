import SuperNeo.EmbeddingTheory

/-!
Barrel interface for `SuperNeo.EmbeddingTheory` (Section 5 (Embedding products with evaluation homomorphism)).

The authoritative export set is the import list of `SuperNeo/EmbeddingTheory.lean`;
per-module theorem boundaries live in the colocated `*Interface.lean` files
next to each implementation module.
-/
